{
  "kind": "native",
  "client": "claude-code-cli-macos",
  "harness_version": "0.11.1",
  "source_commit": "af9693b97480c7b801c57c43863b116f4116808c",
  "runtime_version": "2.1.273",
  "client_version": "2.1.273",
  "platform": "macos",
  "observations": [
    "The 0.11.1 runner's disposable home carried its own default keychain at `<home>/Library/Keychains/login.keychain-db` (20 KB) before any client turn, and both `security default-keychain` and `security list-keychains` under that HOME named that file.",
    "`harness sync` from the frozen clone printed `sync complete`, and `harness doctor` exited 0 reporting `agent-harness 0.11.1`, `claude: 2.1.273 (Claude Code)`, `codex: not on PATH`, `claude hooks: 8 registered, script paths exist`, `codex hooks: 5 registered, script paths exist` and `drift: none`.",
    "Two separate fresh headless `claude -p` processes each answered from the synced files: the rendered personal identity, `YES` for the `spike-contract` skill, and a subagent-type list containing all twelve harness roles alongside the client's native ones.",
    "Cycling `delegation` tiered -> off in one home with a fresh headless session each time, the same unnamed `Agent` spawn ran under `tiered` (client reported `SPAWNED` / `tiered`, 1 subagent transcript) and under `off` returned `DENIED: Delegation is off; perform the work inline or change the selected stance.` with 0 subagent transcripts; the resolved `delegation.md` link moved `tiered.md` -> `off.md`.",
    "The 0.11.1 `delegation/off.md` text the client resolves now reads `A spawn attempted under this stance is denied outright, so changing the selected stance is the only way to delegate.`, which matches the deny the engine actually returned.",
    "Cycling `voice` answer-card -> scannable -> answer-card in the same home, the client reported the answer-card word target and parts, then the scannable parts, then the answer-card parts again, and the `voice.md` link followed each selection.",
    "A custom `proof` dimension supplied from an external primitive root appeared in `harness stances --json` after sync, and the native client's reply ended `PLAIN` under `proof=plain` and `TAGGED` under `proof=tagged`.",
    "Selecting a variant that does not exist made `harness sync` exit 1 with the single line `stance 'proof' has no variant 'nonesuch'; options: plain, tagged`, and the previously resolved `proof.md` link was left pointing at `tagged.md`.",
    "Enforced stances travelled the override ladder natively: a `HARNESS_PROJECT_CONFIG` file and a `HARNESS_STANCE_DELEGATION` session variable each selecting `delegation=off` produced `DENIED: Delegation is off; perform the work inline or change the selected stance.` with 0 subagent transcripts, while the same home on user config alone spawned and wrote 1.",
    "Cycling `permissions` manual -> bypass (unacknowledged) -> bypass (acknowledged) -> auto, the synced `permissions.defaultMode` read `default`, stayed `default` while unacknowledged, then `bypassPermissions` and `auto`; the unacknowledged sync exited 1 naming `permissions_bypass_acknowledged` as the confirmation key.",
    "Under `bypassPermissions` with `autonomy=ask` the client reported `BLOCKED: touch sentinel-hook.txt writes to the working tree \u2014 this changes files on this machine (grade-bash hook, autonomy=ask)` and the sentinel file did not exist, while the same command prefixed `HARNESS_CONFIRMED=1` returned `CREATED` and its sentinel did exist.",
    "After switching to `autonomy=execute` the same write returned `CREATED` unprompted and its sentinel existed, and a native turn completed under `permissions=auto`.",
    "A user-owned `PostToolUse` `Write` hook added by hand to `<home>/.claude/settings.json` survived `harness sync`: the merged table still held the harness coordinator entry (matcher `None`, the clone's `adapters/claude-code/hook.py`) and the user entry (matcher `Write`), with all eight harness hook events registered.",
    "One turn writing two plan files produced Review Card feedback naming `alpha.md` and `beta.md` separately (6 feedback occurrences across the two files), both files were written under `.agent-harness/plans/`, and the user hook's log carried exactly two `user-hook-fired` lines from that turn.",
    "Under `bypassPermissions` with `autonomy=ask`, `ls -1` returned `RAN` while `touch ./compose.txt` returned `BLOCKED: autonomy=ask setting requires explicit approval for local writes` and `compose.txt` did not exist: a hook deny wins over the bypass posture.",
    "A native spawn of `subagent_type: reviewer` was denied by the hook with `This constrained harness role requires an isolated worker. Use harness role run reviewer --runtime claude-code --workspace <repo> --prompt-file <brief-file>. Planner workers also require --artifact <new-plan.md>; native role defaults are not confinement.` and 0 subagent transcripts were written.",
    "0.11.1 spawn guard, observed in one native session: after that refusal the client re-issued the identical brief with no `subagent_type` and was denied again with `This work was refused as a native reviewer spawn in this session; dropping or changing the role name does not change that.`, still 0 subagent transcripts, and the session record carried one `denied_spawns` entry for `reviewer`.",
    "0.11.1 spawn guard, observed natively in a separate session: an `Agent` spawn with no `subagent_type` whose prompt began with a standalone `harness-role: reviewer` line was denied with the constrained-role refusal, and 0 subagent transcripts were written.",
    "`harness role run gatherer` exited 0 with `status: completed`, `mode: isolated-cli`, `read_roots` limited to the workspace and the frozen clone, and returned exactly `orange marmalade is the first line`; a brief ordering it to create a file returned `I had no tool able to write.` and the file did not exist.",
    "Planner publication stayed harness-mediated: `--artifact fresh-plan.md` published `<repo>/.agent-harness/plans/fresh-plan.md`, `--artifact ../escape.md` and `--artifact /etc/escape.md` both exited 1 with `--artifact must be a Markdown filename, not a path` with no file above the workspace, and re-using the published filename exited 1 with `artifact already exists; choose a new filename`.",
    "Every worker `status.json` carried `pid` and `pid_start` next to `status`, `mode` and `posture`, and `harness role status` printed the same fields.",
    "The repository's own acceptance runner, from the frozen clone with the by-name provider environment, exited 0 and recorded `cost-posture: passed` with `source_commit` `af9693b97480c7b801c57c43863b116f4116808c`, `harness_version` `0.11.1` and `client_version` `2.1.273`.",
    "Its observation: a non-default cost variant rewrote only the roles it changes (5 of 11) and left the rest byte-identical; in a new native session an unnamed spawn ran as the variant's default band worker on its row's model and effort with the budget sentence in its brief, the orchestrator's context carried a `usage-feed` line naming the worker's output tokens against its budget, `harness usage --rescan --by role` recorded the routed row, a session whose record predates the workers was not rerouted and its spawn still succeeded, and a null variant did none of it.",
    "With a trusted disposable repo whose `## Gate` block logged each run, a green tree ran the gate once and recorded `status: passed`; an unchanged tree reused that green result without rerunning it; adding an untracked file and then staging an edit each forced exactly one rerun (counter 1 -> 2 -> 3), both green.",
    "With the gate red, the Stop hook blocked the turn repeatedly \u2014 the run counter went 3 -> 11, eight further gate runs, matching `MAX_BLOCKS` \u2014 then released the turn with `status: unverified`, and the client's own answer was that it could not clear the gate because deleting the red marker was refused by the permission layer.",
    "A gate that writes a file while running released the turn with `{status: unverified, reason: 'working tree changed during the gate; result unverified', green_hash: None}` and was never recorded green.",
    "After `harness bmad apply` into a disposable BMad 6.12.0 framework checkout, all four resolved review layers carried the 0.11.1 role declaration as the first line of their brief: `harness-role: reviewer` for blind-hunter, edge-case-hunter and verification-gap, and `harness-role: spec-reviewer` for acceptance-auditor; `harness bmad check` reported 0 drift findings.",
    "The `bmad-code-review` run was confined on attempt 1 of 1 run: eight isolated worker records were written (six `reviewer`, two `spec-reviewer`), all `mode: isolated-cli`, `status: completed`, `effort: high`, each with `class_source`, `model_source` and `effort_source` of `role` and the new `pid` and `pid_start` fields, while the orchestrator session wrote 0 native subagent transcripts.",
    "Each worker's `read_roots` held exactly the four directories the layer instruction names \u2014 the assigned worktree, the frozen clone, the framework checkout and the review skill directory \u2014 and the review returned both seeded defects (`ports.py` missing the lower-bound guard, `test_ports.py` missing rejection-path tests) plus a whitespace-acceptance gap, with two findings rejected as out of spec scope.",
    "Claude leg: `harness task save --runtime claude-code --revision 0` wrote revision 1, and a native session read `.agent-harness/task.json`, appended `CLAUDE-WAS-HERE` to `progress.txt` and reported the objective with the record's verification status as `unverified` \u2014 the caller's reported `passed` retained as evidence only.",
    "Claude -> Codex: `codex exec -m gpt-5.6-luna` with stdin closed exited 0, read the same record, appended `CODEX-WAS-HERE` (its own diff showed the single added line) and printed the same objective and the same `unverified` status; one turn, `tokens used 17,433`.",
    "Codex -> Claude: a `--runtime codex` save at revision 1 produced revision 2, and a Claude session then reported `codex`, `2` and `data only; approvals and native permissions must be established in this session`; a further Claude session carried out the codex-written next step and reported both runtime lines present with codex having written last.",
    "Repeating the same `--revision 1` save exited 1 with exactly one stderr line, `task revision changed since --revision 1; read the current task, then save against its revision`, no stdout, no traceback and no absolute path; after an unrelated edit `harness task show` returned `status: stale` with `verification: {status: unverified, reason: working tree changed after handoff}`.",
    "`harness sync` without `--adopt` exited 2 and named both collisions (the pre-existing `rules/delegation.md` that would load twice and the non-link `CLAUDE.md`) without overwriting anything; `--adopt` then exited 0, made `CLAUDE.md` a harness link, kept the user's `env.MY_OWN_KEY` and allow rule, registered the eight harness hook events and left the user's own skill byte-identical.",
    "`harness uninstall` exited 2, restored the adopted `CLAUDE.md` and legacy `rules/delegation.md` byte-identical, preserved the hand-edited owned field with `settings.json: user changes preserved for [\"hooks\", \"Stop\"]`, removed the harness state directories and links, and a native session afterwards answered `PRESERVED` and `NONE` from the user's restored instructions.",
    "After uninstall `~/.claude/rules/harness-stances/` did not exist, `~/.claude/skills/harness-*` matched nothing, and no harness links remained under the client directory."
  ],
  "cases": {
    "installation": "passed",
    "stance-switch": "passed",
    "custom-stance": "passed",
    "permission-controls": "passed",
    "hook-composition": "passed",
    "role-confinement": "passed",
    "cost-posture": "passed",
    "gate-invalidation": "passed",
    "bmad-workflow": "passed",
    "bidirectional-handoff": "passed",
    "migration-uninstall": "passed"
  }
}
