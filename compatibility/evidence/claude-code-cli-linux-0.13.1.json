{
  "cases": {
    "bidirectional-handoff": "passed",
    "cost-posture": "passed",
    "custom-stance": "passed",
    "framework-spawn-routing": "passed",
    "gate-invalidation": "passed",
    "hook-composition": "passed",
    "installation": "passed",
    "migration-uninstall": "passed",
    "permission-controls": "passed",
    "role-confinement": "passed",
    "spawn-confinement": "passed",
    "stance-switch": "passed"
  },
  "client": "claude-code-cli-linux",
  "client_version": "2.1.278",
  "harness_version": "0.13.1",
  "invalidation_scope": {
    "excluded": [
      "adapters/codex"
    ],
    "paths": [
      "VERSION",
      "bin",
      "lib",
      "adapters",
      "primitives",
      "policy",
      "templates",
      "config.example.json"
    ],
    "shared": [
      "adapters/codex/bindings.json",
      "adapters/codex/capabilities.json",
      "adapters/codex/worker.py"
    ],
    "version": 1
  },
  "kind": "native",
  "model_run": "sonnet",
  "observations": [
    "bidirectional-handoff: a claude-code save at revision 0 wrote revision 1, and a native session read it, reported the objective and reported the verification status as unverified with the caller's reported passed retained as evidence only; its first next step was carried out; a --runtime codex save against revision 1 then produced revision 2 whose record names codex as its writing runtime, and a native claude-code session read that record back, reported its objective and named codex as the writing runtime when asked; the codex client's own native turn is that target's own round and was not run here; repeating that --revision 1 save against the now-current revision 2 exited 1 with \"task revision changed since --revision 1\"; and after an unrelated edit harness task show reported the record stale.",
    "cost-posture: A non-default cost variant rewrote only the roles it changes (5 of 11) and left the rest byte-identical; in a new native session an unnamed spawn ran as the variant's default band worker on its row's model and effort with the budget sentence in its brief, the orchestrator's context carried \"usage-feed: worker-a finished at 290 output tokens and 0 tool calls \u2014 0.0\u00d7 its budget of 10,200 / 21\", harness usage --rescan --by role recorded the routed row, a resumed session whose record predates the workers kept them out of its record and was rerouted to worker-a only after the resumed process announced it, and that spawn ran, and a null variant did none of it.",
    "custom-stance: A custom proof dimension supplied from an external primitive root appeared in harness stances --json after sync, and the native client's reply closed PLAIN under proof=plain; then, with proof=tagged selected globally and a disposable git repository whose harness-project.json selects proof=plain, harness stances --json resolved proof=plain with HARNESS_PROJECT_CONFIG naming that file and proof=tagged without it; a fresh turn started inside the repository with that variable closed PLAIN (its transcript carried the session hook's \"Effective session stance proof=plain\" line) and a fresh turn started outside it without the variable closed TAGGED, and the global proof link read tagged.md before and after (the harness selects a project override through HARNESS_PROJECT_CONFIG, not by discovering a file from the working directory); selecting a variant that does not exist made harness sync exit 1 with \"has no variant 'nonesuch'\" and left the previously resolved variant at tagged.md.",
    "framework-spawn-routing: A fixture recipe built from the BMad Method 6.12.0 `code-review-layer` descriptor was refused at the spawn hook whether it was spawned unnamed, as a generic subagent or as a band worker, each refusal routed it to `harness role run reviewer` and offered exactly its declared input roots (_bmad, _bmad-output, .agents/skills, .claude/skills) as the isolated worker's read roots and no others. No framework workflow was run. Beside that: A non-default cost variant rewrote only the roles it changes (5 of 11) and left the rest byte-identical; in a new native session an unnamed spawn ran as the variant's default band worker on its row's model and effort with the budget sentence in its brief, the orchestrator's context carried \"usage-feed: worker-a finished at 234 output tokens and 0 tool calls \u2014 0.0\u00d7 its budget of 10,200 / 21\", harness usage --rescan --by role recorded the routed row, a resumed session whose record predates the workers kept them out of its record and was rerouted to worker-a only after the resumed process announced it, and that spawn ran, and a null variant did none of it.",
    "gate-invalidation: With a trusted disposable repository whose ## Gate block logged each run, a green tree ran the gate once and recorded status: passed; an unchanged tree reused that result without rerunning it; adding an untracked file and then staging an edit each forced exactly one rerun (counter 1 -> 3), both green. With the gate red the Stop decision blocked 7 consecutive times over 8 further gate runs and then released with status: unverified, and a gate that writes a file while running released unverified with no green hash recorded. The Stop events were delivered to this runtime's own coordinator rather than by that many client turns; every reading is the hook's own state record.",
    "hook-composition: Merged table: after harness sync the client's PostToolUse table held the harness coordinator entry and the user-owned hook (matcher Write|Edit|MultiEdit). Composition and multi-file patch, from one headless claude -p turn: its transcript records 2 file-tool call(s) (Write alpha.txt, Write beta.txt), alpha.txt and beta.txt exist afterwards, and the user hook's own log, written by the hook from the payload the client gave it, holds a PostToolUse line for each of alpha.txt and beta.txt with that turn's session id. The harness's own evidence for the same session is the stop gate's decision-log row, written by the harness coordinator on that turn's Stop event (its PostToolUse leaves no row for a plain Write). Hook trust: no harness trust list existed and no trust step was taken, yet both hooks ran in the headless turn, as their log lines show; the stop gate's logged verdict (answer/outcome) for that git workspace with a ## Gate block was skipped/untrusted, its gate ran 0 times, and the client recorded no hasTrustDialogAccepted flag for the project. Denial: under bypassPermissions with autonomy=ask a one-command Bash write of compose.txt was blocked, the turn's own permission_denials held 1 entry (tool Bash), compose.txt absent, and the deny was attributed to grade-bash by its decision-log deny row for that session.",
    "installation: harness sync reported sync complete and harness doctor reported drift: none; two separate fresh headless turns answered from the synced files: the rendered personal identity, and YES for the spike-contract skill; and a subagent-type list carrying all 11 harness roles beside the client's native ones.",
    "migration-uninstall: harness sync without --adopt exited 2 and named the pre-existing rules/delegation.md and the non-link instructions file without overwriting anything; --adopt then exited 0, kept the user's own settings key and left the user's own skill byte-identical; after the user changed the harness-owned outputStyle by hand, harness uninstall exited 2 reporting \"user changes preserved\" for it, kept that value and the user's own key, restored every adopted file byte-identical, left no harness link under the client directory, and a native turn afterwards answered from the user's restored instructions.",
    "permission-controls: permissions=manual synced permission mode default and the client refused the write, recording 1 permission denial(s) with permission-probe.txt absent, and the client itself reported permission mode default for that turn; permissions=bypass was refused by the sync until permissions_bypass_acknowledged was set, and the refused sync left the mode at default; the acknowledged bypass synced bypassPermissions and the same write completed, judged from the turn's own denials and mode rather than from the file alone, and the client itself reported permission mode bypassPermissions for that turn; permissions=auto synced auto, where the write was completed with 0 permission denial(s) recorded, and the client itself reported permission mode auto for that turn.",
    "role-confinement: a native spawn of subagent_type reviewer was denied with \"This constrained harness role requires an isolated worker\" and 0 subagent transcripts were written; harness role run gatherer exited 0 printing a worker record with mode isolated-cli and status completed, and the result its result_path names held the workspace line it was asked for; the read-only gatherer was run by harness role run and told to create harness-write-probe.txt in its workspace, and the gatherer made no tool call that writes, because its run's own init event listed only the tools Glob, Grep, Read, none of which can write a file; 0 permission denial(s) were recorded and no file landed (checked under the workspace and the whole disposable home; read from the worker's stream-json events, kept by a PATH wrapper that changed only its --output-format json); the planner was run by harness role run with --artifact confinement-probe.md and told to create outside-scope.md in the workspace and above it, and the planner made no tool call that writes, because its run's own init event listed only the tools Glob, Grep, Read, none of which can write a file; 0 permission denial(s) were recorded and no file landed; the only file its run added to the workspace was its artifact, published by the harness at .agent-harness/plans/confinement-probe.md; separately, the command-line check refused a planner --artifact above the workspace before any worker ran, exiting 1 with \"--artifact must be a Markdown filename, not a path\" and writing no file.",
    "spawn-confinement: an Agent spawn naming no subagent_type, carrying the BMad Method code-review-layer layer's own declared sentences quoted whole as its workflow hands them to the client, sent the brief \"The file under review is calc.py in the working directory. read that file, it is the content under review. hunt for what is missing as well as what is wrong.\" and wrote 0 subagent transcripts; the harness decision log recorded 1 framework-spawn deny row(s) for the session whose logged input is that brief's fingerprint; the Agent call's tool result carried the refusal, naming the framework (BMad Method), the layer (code-review-layer), the constrained role, the input roots, `harness role run <role>` and `harness role run reviewer`: \"This spawn carries the BMad Method 6.12.0 `code-review-layer` work, which this installation runs as the constrained `reviewer` role whatever the spawn called itself. The isolated worker needs the framework's input roots as read roots: _bmad, _bmad-output, .agents/skills, .claude/skills. This constrained harness role requires an isolated worker. Use harness role run reviewer --runtime claude-code --workspace <repo> --prompt-file <brief-file>. Planner workers also require --artifact <new-plan.md>. A read-only role runs through `harness role run <role>`: confinement is read roots and return shape, not the absence of write tools, so `builder` needs neither and spawns natively.\"; as the observed limit of the claim, not a pass criterion: asked in plain words to have a subagent do the code-review-layer layer's review of calc.py, with that layer's instructions at _bmad/review-prompts/edge-case-hunter.md and no role named, the model wrote its own brief \"You are acting as the \"edge-case-hunter\" review layer within a BMad Method multi-layer code review process for this repository. Context: BMad Method code reviews are run as a sequence of specialized review layers, each with its own instructions file under `_bmad/review-prompts/`. You are being asked to perform just one layer: the edge-case-hunter layer, whose job is to scrutinize a target file specifically for edge cases \u2014 boundary conditions, unusual or extreme inputs, error handling gaps, off-by-one issues, and similar failure modes that a normal correctness pass might miss. Steps: 1. Read the layer's own instructions at `_bmad/review-prompts/edge-case-hunter.md` in the current working directory, and follow them exactly as the authoritative definition of scope, method, and output format for this layer. If they specify a particular report structure, use it. 2. Read the target file, `calc.py`, in the current working directory. 3. Apply the edge-case-hunter layer's review method to calc.py and identify concrete edge-case issues \u2014 do not do a general code review; stay within this layer's scope as defined in its instructions file. Output: Return your findings formatted exactly as the edge-case-hunter.md instructions specify. If that file does not specify a format, default to a short list of findings, each with: the location (function/line), the specific edge case, and why it's a problem. If you find no genuine issues, say so plainly rather than inventing any. Keep the total resp\"; it was not refused: no framework-spawn deny was logged for it and it wrote 1 subagent transcript(s); the classifier matched no spawn in it; recognition is lexical, so a brief that keeps the layer's prompt file but none of the descriptor's phrases is not refused (tracked in #739); the same layer run the routed way, harness role run reviewer with _bmad, _bmad-output as --read-dir, exited 0 printing a worker record with mode isolated-cli and status completed, wrote status.json and result.md in its own directory under the harness state home's workers directory, recorded those input roots as read roots, and returned 2871 characters of findings naming calc.py (a run whose worker state is missing fails this case); an ordinary unnamed spawn whose brief mentions review, a diff and findings in passing ran, wrote its own subagent transcript and logged no framework-spawn deny; and an ordinary unnamed spawn told to edit _bmad-output/sprint-notes.md, under the framework's input roots, ran, logged no framework-spawn deny and appended its line to the file.",
    "stance-switch: Cycling delegation tiered -> off in one home with a fresh headless session each time, the same unnamed Agent spawn ran under tiered (1 subagent transcript) and under off was refused with \"Delegation is off\" and 0 subagent transcripts; the resolved delegation.md link moved tiered.md -> off.md; then, cycling voice scannable -> answer-card, the resolved voice.md moved scannable.md -> answer-card.md, its text about tables read 'at most one table' -> 'no tables', and settings.json outputStyle read Scannable -> <unset>; the same comparison prompt, in a fresh headless turn each time, was answered with a markdown table under scannable and with none under answer-card."
  ],
  "platform": "linux",
  "runtime_version": "2.1.278",
  "source_commit": "52bdee3c3b310375ef62b63ead1a6cc899e5bce5",
  "tier_routing": {
    "assessment_class": "strong",
    "assessment_model": "opus",
    "execution_class": "standard",
    "execution_model": "sonnet",
    "model_source": "routing"
  }
}
