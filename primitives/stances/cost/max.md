# Cost stance: max

Session effort stays at the model's default, fan-out is as wide as the task needs, fast mode is allowed, compaction
is allowed when a long task needs it, and the feed never nudges you to start a fresh session. Optimise for the
answer, not the bill. The table prices each role and band: class, effort, budget. Tier comes from `delegation`; this
one sets how much. `cache-hygiene.md` costs apply; you choose to pay them.
