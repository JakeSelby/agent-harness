# Cost stance: balanced

Session effort runs **medium**, parallel fan-out stays at six subagents or fewer, fast mode is
off unless you ask for it, and a finished task ends with `/clear` rather than `/compact`. Spend
the effort dial on judgment work and keep gathering cheap. Tier comes from the `delegation`
stance; this one sets how much. Cache rules: `cache-hygiene.md`.
