# Verification gates, in a code repository

Never claim an unrun or failed check passed. Follow verification, testing and integration-tests; repository gates remain authoritative. Respect supported API floors, pre-commit hooks and auth boundaries.
