[workflow]
persistent_facts = ["file:{project-root}/docs/bmad-governance.md"]
activation_steps_append = [
  "`_bmad-output/implementation-artifacts/sprint-status.yaml` is derived from the issue map and the story files: never edit it by hand. Record progress in the story file, then run `python3 scripts/bmad_issue_sync.py sprint-status` to regenerate it; where this workflow says to update sprint status, run that command instead.",
]
