`/build` step 6 now says what a finished bot review looks like: the bot's commit status on the
head commit completing, such as CodeRabbit's `success: Review completed`. The `Review skipped`
status posted on every push and the empty review a bot's thread reply creates no longer read as a
pass, and the wait for a pass rises from ten minutes to fifteen, since a requested re-review can
take close to nine.
