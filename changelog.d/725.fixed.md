A hook you add beside the harness's own in `~/.claude/settings.json` or Codex's `hooks.json` is no longer reported by `harness sync` as a changed owned field, is not drift for `harness doctor`, and stays in place at `harness uninstall`, which removes only the harness's entries. Before, one such hook froze the harness's entries for that event.
