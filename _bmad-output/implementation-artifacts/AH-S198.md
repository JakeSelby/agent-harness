---
bmad_id: "AH-S198"
type: "story"
title: "Hook ids and the hooks switch kind"
lifecycle: "active"
provenance: "authored"
github_issue: 555
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/555"
parent_bmad_id: "AH-E011"
parent_github_issue: 552
updated: "2026-09-23"
---

# AH-S198 — Hook ids and the hooks switch kind

<!-- bmad-sync:begin -->
- **GitHub issue:** [#555](https://github.com/JakeSelby/agent-harness/issues/555)
- **Primary parent:** [AH-E011](https://github.com/JakeSelby/agent-harness/issues/552)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer using the harness, I want every lifecycle hook to have an id I can switch off, with the
core enforcement hooks behind an explicit acknowledgement, so that I can drop a hook I do not want
without being able to disable enforcement by accident.

## Context and value

Epic #552 makes every installed unit a switchable dimension; hooks are the kind that enforces, so they
need both an id and a guard. Its principle: nothing is claimed enforced without a hook id. On `main`
today, `lifecycle.invoke(name, event)` dispatches to the modules in `policy/hooks/` with no selection
check, and hooks are not a kind in `catalog.KINDS`. `permissions_bypass_acknowledged` is the existing
acknowledgement pattern this story copies.
[Source: https://github.com/JakeSelby/agent-harness/issues/555]

## Acceptance criteria

1. **Given** each hook id set `off`, **when** its event fires, **then** a lifecycle test proves the
   module is not invoked, for both native envelopes.
2. **Given** no acknowledgement, **when** `harness config set hooks.grade-bash off` runs, **then** it is
   refused; **given** the acknowledgement, **then** it is accepted and a grade-3 command passes ungraded
   in the test envelope.
3. **Given** the catalog, **when** `harness catalog` runs, **then** it emits kind `hooks` with the id
   list.
4. **Given** Codex, **when** its adapter dispatches, **then** it reads the same id map; there is no
   Codex-only id, and the docs say so.
5. **Given** the docs, **when** `docs/runtime-controls.md` is read, **then** it lists the ids and the four
   core ones.
6. **Given** the change, **when** the gate runs, **then** lint is clean and the suite green.

## Design

### Approach

- **Ids are module basenames:** `allow-plan-webfetch`, `allow-readonly-bash`, `brief-guard`,
  `filter-output`, `grade-bash`, `harness-session`, `neutralize-tool-output`, `stop-gate`,
  `tier-agent-spawns`, `usage-feed`, `usage-log`, `validate-plan-card`.
- **Libraries have no switch:** `decisions`, `posture`, `pricing`, `telemetry`, `rule-detectors`,
  `otel-headers`, `filter-lines`.
- **`lifecycle.invoke(name, event)` consults the selection first** and returns `{}` for an `off` id.
- **Inline dispatch is keyed to its owning id:** Bash grading and the ask decision to `grade-bash`;
  plan-mode allows to `allow-readonly-bash`; role deny and evasion deny to `tier-agent-spawns`.

### Decisions and alternatives

- **Four core ids** (`grade-bash`, `stop-gate`, `brief-guard`, `neutralize-tool-output`) may be `off`
  only when `core_switches_acknowledged` is true.
- **Refusal happens at validation, before any file changes:** both `config set` and a mode file that
  tries without the acknowledgement are refused, on the pattern of `permissions_bypass_acknowledged`.

### Do not implement

- Codex-only hook ids.
- Switches for the library modules.
- The selection model itself (#554), which this story is blocked by.

## Tasks

- [ ] Register `hooks` as a switch kind with the twelve ids (AC: 3)
- [ ] Check the selection in `lifecycle.invoke`; key inline dispatch to its id (AC: 1)
- [ ] Add `core_switches_acknowledged` validation to `config set` and mode files (AC: 2)
- [ ] Share the id map with the Codex adapter (AC: 4)
- [ ] One lifecycle test per id, both envelopes; the acknowledgement tests (AC: 1, 2)
- [ ] Document ids and core ids in `docs/runtime-controls.md` (AC: 5)
- [ ] Gate (AC: 6)

## Dev notes

### Bound decisions and requirements

- FR-15, Selection model with switches for every primitive: core hooks (command grading and the stop
  gate) can be disabled only when `core_switches_acknowledged: true` is set.
- AD-2, One resolver, fixed invariants: core hooks can be switched off only behind an explicit
  acknowledgement, and invariants sit outside every switch.
- AD-7, What a hook may and may not do: the event table in `lifecycle.py` is the one declaration of
  which events each runtime raises.
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-2]

### Files to touch

`lib/harness_core/lifecycle.py`, `lib/harness_core/catalog.py`, `bin/harness` (config validation and
`catalog`), the Codex adapter's dispatch, `docs/runtime-controls.md`, tests.
[ASSUMPTION: the issue names `lifecycle.invoke` and the docs file; the other paths are where the named
behaviour lives on `main`.]

### Testing

One lifecycle test per id for both native envelopes; acknowledgement refusal and acceptance for
`hooks.grade-bash`.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/555]
- [Source: lib/harness_core/lifecycle.py]
- [Source: docs/runtime-controls.md]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-15]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Not started. The twelve hook modules and seven libraries the issue names match `policy/hooks/` on
`main`; no switch or acknowledgement exists yet.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
