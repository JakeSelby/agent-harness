---
bmad_id: "AH-B111"
type: "bug"
title: "grade-bash grades a redirect-only segment after a subshell as 0"
lifecycle: "active"
provenance: "authored"
github_issue: 899
github_issue_url: "https://github.com/JakeSelby/model-citizen/issues/899"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-26"
---

# AH-B111 — grade-bash grades a redirect-only segment after a subshell as 0

<!-- bmad-sync:begin -->
- **GitHub issue:** [#899](https://github.com/JakeSelby/model-citizen/issues/899)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Reproduction

Grade `(ls) > out.txt` or `(ls)>out.txt` with `grade_text` from `policy/hooks/grade-bash.py`: both return grade 0, while `ls > out.txt` returns grade 1 (`redirect to out.txt`). `{ ls; } > out.txt` and `x=1 > out.txt` behave the same way. The read-only hook already refuses the line, but under the `ask` or `confirm-writes` autonomy stance the file write is not prompted.

## Root cause

`grade_tokens` strips redirections first and records the files they write, then returns grade 0 when no command tokens remain. A segment that holds only a redirect, which is what follows a subshell or a group once the parser splits it off, therefore lost the write it had just recorded. [Source: policy/hooks/grade-bash.py#grade_tokens]

## Acceptance criteria

1. **Given** the command `(ls) > out.txt` or `(ls)>out.txt`, **when** `grade-bash` grades it, **then** it grades 1 with verb `redirect to` and target `out.txt`.
2. **Given** a redirect-only segment whose target is in a temp directory, **when** it is graded, **then** it grades as the same redirect after a plain command does.
3. **Given** any segment with a redirect that writes a file, **when** it is graded, **then** the grade is at least 1; a `/dev/null` target stays 0.

## Design

The empty-segment case joins the read-only case: both return grade 1 with `redirect to` and the written target when a write was recorded, and 0 otherwise. Device targets keep returning grade 3 before this point. Rejected: special-casing subshells in the parser, since any segment reduced to a redirect has the same defect. The policy-file guard and its mention detection are not changed.

## Regression test

`GradeTests.test_a_redirect_only_segment_after_a_subshell_still_writes_its_file` in `tests/test_grade_bash.py` pins both spellings, a group and an assignment-only segment at grade 1, a temp target equal to its plain-command grade, and a `/dev/null` target at 0. It fails before the fix, where each grades 0.

## Tasks

- [x] Grade a redirect-only segment by its written target (AC 1, 3).
- [x] Keep temp and `/dev/null` targets on their existing treatment (AC 2, 3).
- [x] Add the regression test and the changelog fragment.

## Dev notes

- Scope: `grade_tokens` in `policy/hooks/grade-bash.py`, `tests/test_grade_bash.py`, `changelog.d/899.fixed.md`.
- Temp-directory targets carry no redirect-specific exemption; `ls > /tmp/x` grades 1, so the subshell form now matches it.
- Tests run with `python3 -m unittest discover -s tests -p test_grade_bash.py`. [Source: AGENTS.md#Gate]

## Dev agent record

- Model: Claude Opus 5.5.
- Completion: fix and regression test done; the module suite passes.
- Review (PR #929): `_redirects` now drops a numeric or `-` target only for descriptor duplication (`>&`, as in `2>&1`), so `ls >2` records and grades a write to a file named `2`; `grade_tokens` and `_written` no longer drop numeric targets. Test: `test_a_numeric_file_target_is_a_write_but_a_descriptor_duplication_is_not`.
- Files: `policy/hooks/grade-bash.py`, `tests/test_grade_bash.py`, `changelog.d/899.fixed.md`, this story, `_bmad-output/issue-map.json`, `_bmad-output/implementation-artifacts/sprint-status.yaml`.

## Review findings

- CodeRabbit, PR #929, `grade-bash.py` 737-738 (minor): numeric targets were dropped for every redirect, so `ls >2` graded 0. Fixed as recorded in the dev agent record.

## Change log

- 2026-09-26: story filled with the fix design and regression test.
- 2026-09-26: review finding on PR #929 fixed; a numeric redirect target is a file unless the operator duplicates a descriptor.
