---
bmad_id: "AH-S214"
type: "story"
title: "Acceptance runner: use the Codex session login in its disposable home"
lifecycle: "active"
provenance: "authored"
github_issue: 612
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/612"
parent_bmad_id: "AH-E014"
parent_github_issue: 633
updated: "2026-09-25"
---

# AH-S214 — Acceptance runner: use the Codex session login in its disposable home

<!-- bmad-sync:begin -->
- **GitHub issue:** [#612](https://github.com/JakeSelby/agent-harness/issues/612)
- **Primary parent:** [AH-E014](https://github.com/JakeSelby/agent-harness/issues/633)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As the maintainer running a qualification round, I want the acceptance runner's disposable
`CODEX_HOME` to use the Codex session login when I ask for it, so that a Codex target can be driven by
`scripts/native_acceptance.py` rather than by hand.

## Context and value

The issue says #408 records that every Codex round so far used the ChatGPT-session login that
`codex login` writes to `~/.codex/auth.json`. `docs/compatibility.md` and
`docs/qualification-runbook.md` ("Before a run", step 2) describe that login at `origin/main`.

The runner's disposable home never receives that file. So the credential probe can prove the login
exists, but the runner cannot use it, and the Codex targets stay hand-qualified. The issue says the next
verification round needs this to script the Codex cases #336 committed.

At `origin/main`:

- `adapters/codex/worker.py` `prepare` symlinks `auth.json` from the caller's `CODEX_HOME`, or
  `~/.codex`, into each role worker's disposable Codex home;
- `scripts/native_acceptance.py` sets `CODEX_HOME` to the run's own client directory and passes through
  only the variables in `AUTH_PASSTHROUGH`, which are Anthropic and cloud settings. Nothing gives that
  home a Codex login;
- its Codex surface says "no round has been run through it", and keeps every verdict `unverified`
  until `--home-confirmed` records that an operator compared one against a hand run.

PR #651, which opened the 0.13.0 candidate, says `codex-cli-linux` "ships as a stated limitation until
#612 lets the acceptance runner drive it". The issue's own scope names the owner's Mac. [ASSUMPTION: the
two are compatible, since a scripted login serves both hosts; neither record says so.]

## Acceptance criteria

From the issue's "Done when" section:

1. **Given** an explicit flag, **when** the runner prepares its disposable `CODEX_HOME`, **then** the
   home has the session login, copied or pointed to.
2. **Given** no flag, **when** the runner prepares the home, **then** it receives no login.
3. **Given** any run, **when** records and logs are written, **then** none contains the credential.
4. **Given** the owner's Mac, **when** one Codex case runs end to end through the runner, **then** its
   observation is compared against the hand run, per the runbook.

## Design

### Approach

From the issue: give the disposable home a working login "the way `adapters/codex/worker.py` already
does for role workers", behind an explicit flag that is never on by default.

### Decisions and alternatives

- Opt-in only: the issue requires "an explicit flag, never by default".
- Copy or point: the issue allows either, and the runner points, with a symlink, as the worker
  adapter does. A kept home (`--keep-home`) then holds no credential of its own, and a token the
  client refreshes lands in the operator's one login file rather than forking it. The alternative,
  a copy, would keep the client from ever writing to the operator's file, at the cost of a second
  credential on disk for a kept home and a refresh-token fork that can leave the operator's login
  stale.
- Flag name and source: `--codex-session-login` reads the login from the caller's `CODEX_HOME`, else
  `~/.codex`, before the disposable home replaces either, matching `adapters/codex/worker.py`.
- Fail closed: the flag is refused before any case runs for a Claude Code target, which passes its
  credential by name, and when no `auth.json` exists.
- Redaction: every string of eight or more characters in the login file is removed literally from
  each observation, ahead of the path and shape rules, because a token need not match any shape in
  `SECRET_SHAPES`. The progress log and the record are built only from those observations.

### Do not implement

- Any default that exposes the login without the flag (AC 2).
- Writing the credential into a record or log (AC 3).

## Tasks

- [x] Add the opt-in flag to `scripts/native_acceptance.py` (AC 1, 2).
- [x] Place or link `auth.json` into the disposable `CODEX_HOME` when it is set (AC 1).
- [x] Prove the redaction keeps the credential out of every record and log (AC 3).
- [ ] Run one Codex case end to end and compare it against the hand run (AC 4). This is the owner's
  native run on the Mac; the commands are under Completion notes.

## Dev notes

### Bound decisions and requirements

- FR-52: One qualification round per release, whose status names "drivers for all eleven native cases
  (#336)". [ASSUMPTION: FR-52 does not name this issue; the fit is through its drivers.]
- AD-4: Evidence-backed compatibility.
- AD-18: Shared-machine concurrency and writes: "Every runtime launched under a substituted HOME gets a
  throwaway keychain, or fails closed."

### Files to touch

- `scripts/native_acceptance.py`: the flag and the Codex home.
- `tests/test_native_acceptance_login.py`: the flag, the default and the redaction, in a new file
  so it merges beside other work on `tests/test_native_acceptance.py`.
- `docs/qualification-runbook.md`: the Target hosts paragraph that said the runner has no login.

### Testing

Unit tests prove the login is placed only under the flag, and that no record or log carries it. AC 4 is
a native run on the owner's Mac, recorded per `docs/qualification-runbook.md`.

### References

- [Source: adapters/codex/worker.py#prepare]
- [Source: scripts/native_acceptance.py#env]
- [Source: docs/qualification-runbook.md#Before a run]
- [Source: docs/compatibility.md]
- PR #651: https://github.com/JakeSelby/agent-harness/pull/651
- Issue: https://github.com/JakeSelby/agent-harness/issues/612

## Dev agent record

### Agent model used

Claude Opus 5.5, as the builder of the v0.14.0 run.

### Completion notes

- Default taken: none was named for this story. The copy-or-point choice the story left open is
  decided as point, following the worker adapter; see Decisions and alternatives.
- ACs 1 to 3 are implemented and unit-tested with a fake login file; no test launches a client.
- AC 4 is not done: it is a native run on the owner's Mac, and it is the owner's. From a clean
  checkout of the merged commit, with `codex login` done and `~/.codex/auth.json` present:

  ```sh
  python3 scripts/smoke_tier.py --only credentials --targets codex-cli-macos
  python3 scripts/native_acceptance.py --client codex-cli-macos --cases installation \
      --codex-session-login --dry-plan
  python3 scripts/native_acceptance.py --client codex-cli-macos --cases installation \
      --codex-session-login --model <cheapest> --out /tmp/codex-cli-macos-installation.json
  ```

  The verdict is `unverified` by design. Compare its observation with a hand run of the same case,
  record the comparison with the round's observations per `docs/qualification-runbook.md`
  (Client surfaces), and only once the two agree rerun with `--home-confirmed codex-cli-macos`.
- The catalog limitation in `compatibility/catalog.json` that says the runner cannot drive Codex
  until #612 is left for that qualification round to amend, since it is release metadata.

### File list

- `scripts/native_acceptance.py`
- `tests/test_native_acceptance_login.py`
- `docs/qualification-runbook.md`
- `changelog.d/612.added.md`
- `_bmad-output/implementation-artifacts/AH-S214.md`

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
- 2026-09-25: implemented ACs 1 to 3; recorded the point-not-copy decision and the owner's AC 4 run.
