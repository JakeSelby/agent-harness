---
bmad_id: "AH-S234"
type: "story"
title: "Attribute context tokens and hook decisions to the module that caused them"
lifecycle: "active"
provenance: "authored"
github_issue: 790
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/790"
parent_bmad_id: "AH-E013"
parent_github_issue: 632
updated: "2026-09-25"
---

# AH-S234 — Attribute context tokens and hook decisions to the module that caused them

<!-- bmad-sync:begin -->
- **GitHub issue:** [#790](https://github.com/JakeSelby/agent-harness/issues/790)
- **Primary parent:** [AH-E013](https://github.com/JakeSelby/agent-harness/issues/632)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a harness maintainer, I want each row to say which module put which tokens into the model's context and which module's hook made which decision, so that a module's cost and effect can be reported per module.

## Context and value

A module's cost and effect can be reported only if each row says which module put which tokens into the model's context, and which module's hook made which decision. AD-23 requires both: per-module token attribution, carrying the soft-estimate label because context is shared, and a module name on every decision-log row.

Depends on #788 and #789. Traces to AD-23 and AD-12.

## Acceptance criteria

1. Given a session, when its context is assembled, then the ledger records the tokens each selected module contributed, labelled as a soft estimate with its method named (AD-12).
2. Given a hook decision (allow, deny, ask or injected context), when it is logged, then the decision-log row names the module that owns the hook.
3. Given one module switched off and everything else unchanged, when the same task is replayed, then only that module's attributed rows change, and the profile fingerprint differs.
4. Given the unit tests, then they cover every switch kind in the selection document.

## Design

### Approach

- One definition: `posture.context_attribution()` resolves the selection non-strict, from the
  ladder `profile()` reads, and gives every switched-on module and every stance with resident
  text an entry `kind/unit: tokens`, beside `estimand: "soft estimate"` and the `method` (AD-12).
- Resident text is what loads before the first prompt: a rule or a stance's selected variant
  whole; a skill, role or workflow its listing entry, `name: description`, since its body loads on
  demand. Tokens are characters over four, the approximation `harness lint` already uses.
- The usage ledger's session rows carry it as `context_attribution`, Claude Code and Codex alike,
  through `attributed()` in `usage-log.py`. A replay row carries its arm's, through
  `arm_attribution()` in `scripts/cost_bench.py`, with no module for the bare arm.
- Every decision-log row names `module`, the `hooks/<id>` owning its point, stamped in
  `decisions._append()` from `POINT_MODULES`, outcome and sampled-allow rows included.

### Decisions and alternatives

- **A field on the session row, not a row per module:** a new row kind would be summed by every
  reader that groups rows today. An entry per module in one map still changes alone when that
  module is switched off, which is what criterion 3 reads.
- **Resident text only:** a listed kind's body and a hook's per-event context depend on what the
  session does, which a SessionEnd read of the selection cannot see. Counting them would pass a
  guess off as an estimate; they stay out and the docs say so.
- **Point ownership follows the dispatch split #555 proposes:** Bash grading to `grade-bash`, and
  role confinement, framework and evasion refusals, the Workflow launch guard and the integration
  notice, all on the spawn path, to `tier-agent-spawns`. `decision-provider` rows come from no
  hook and name `null` rather than an invented owner.
- **A rescan never guesses:** it keeps the attribution the ledger holds for that session and
  otherwise leaves the field out, the rule the fingerprint follows.
- **Schema version 1 stays:** it is unreleased until v0.14.0 and ships with every field that
  release adds.

### Do not implement

- A soft-estimate report or `harness usage --by module` (AD-12 places the report in v0.15.0).
- Attribution on subagent, worker or provider rows; the session row is where context is assembled.
- Estimating a hook's injected context or a skill body loaded on demand.
- Switching hooks in the dispatcher (#555), the observation entry point (#791), adherence events
  (#792), or any change to `lib/harness_core/lifecycle.py`.

## Tasks

- [x] `posture.context_attribution()` over the resolved selection, labelled (AC: 1, 3)
- [x] Stamp session rows, Claude Code and Codex; a rescan keeps the ledger's or omits (AC: 1)
- [x] Stamp replay rows, empty for the bare arm (AC: 1, 3)
- [x] `module` on every decision-log row from `POINT_MODULES` (AC: 2)
- [x] A test per switch kind: one unit off changes only its entry and the fingerprint (AC: 3, 4)
- [x] Docs, changelog, pinned row keys

## Dev notes

### Bound decisions and requirements

- AD-23: usage rows carry per-module attribution of context tokens as a soft estimate; decision
  rows carry the attribution of hook decisions, not token counts.
- AD-12: every figure carries an estimand label; a soft estimate names its estimator.
- AD-22: the fingerprint and the selection come from `posture.py`, the one resolver.
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-23]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-12]

### Files to touch

- `policy/hooks/posture.py`: `context_attribution` and its constants.
- `policy/hooks/usage-log.py`: `context_attribution`, `attributed`, session rows.
- `policy/hooks/decisions.py`: `POINT_MODULES`, `module_of`, stamping.
- `scripts/cost_bench.py`: `arm_attribution`, stamped on each replay row.
- `tests/test_module_attribution.py`, `tests/test_completion_claim.py`.
- `docs/usage.md`, `changelog.d/790.added.md`.

### Testing

`tests/test_module_attribution.py`: the label and method, each kind's resident text, and a stance
at its selected variant (AC 1); live, rescanned, Codex and resolver-less session rows (AC 1);
every answer kind, outcome and sampled allow naming its hook, `null` for an unowned point, and
every point a call site writes having an owner that exists (AC 2); for each switch kind one unit
off changing only its entry and the fingerprint, and the replay arm doing the same (AC 3); a
check that the fixture covers every switch kind the catalog declares (AC 4). The pinned
decision-row keys in `tests/test_completion_claim.py` gain `module`.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/790]
- [Source: https://github.com/JakeSelby/agent-harness/issues/789]
- [Source: https://github.com/JakeSelby/agent-harness/issues/555]
- [Source: docs/usage.md#what-is-recorded]

## Dev agent record

### Agent model used

Claude Opus 5.5, as the builder of the v0.14.0 run.

### Completion notes

- Built as designed; no default was named for this issue.
- The hooks kind has no units until #555 lands, so a hook switched off changes no attribution
  entry and, today, not the fingerprint either; the test holds the first and waits on #555 for
  the second.
- `lib/harness_core/lifecycle.py` is unchanged: the point-to-module map lives in the log's
  writer, so it cannot overlap #555's dispatch changes.

### File list

- `policy/hooks/posture.py`
- `policy/hooks/usage-log.py`
- `policy/hooks/decisions.py`
- `scripts/cost_bench.py`
- `tests/test_module_attribution.py`
- `tests/test_completion_claim.py`
- `docs/usage.md`
- `changelog.d/790.added.md`
- `_bmad-output/implementation-artifacts/AH-S234.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`

## Review findings

<!-- fill: each review finding and how it was resolved. -->

## Change log

<!-- fill: dated entries for material changes to this file after it was first written. -->
