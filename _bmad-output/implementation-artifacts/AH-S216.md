---
bmad_id: "AH-S216"
type: "story"
title: "harness usage: separate workflow rows and stop pricing them against a confined role budget"
lifecycle: "active"
provenance: "authored"
github_issue: 577
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/577"
parent_bmad_id: "AH-E015"
parent_github_issue: 634
updated: "2026-09-23"
---

# AH-S216 — harness usage: separate workflow rows and stop pricing them against a confined role budget

<!-- bmad-sync:begin -->
- **GitHub issue:** [#577](https://github.com/JakeSelby/agent-harness/issues/577)
- **Primary parent:** [AH-E015](https://github.com/JakeSelby/agent-harness/issues/634)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer reading `harness usage`, I want workflow subagent spend reported apart from spawned
subagents, and never priced against a confined role's budget, so that compliance and budget figures
describe only the spawns the harness actually routed.

## Context and value

The #540 spike, `docs/spikes/2026-09-22-workflow-tool-band-routing-and-ledger.md`, found that the usage
ledger is not bypassed ("`usage-log.py` records every workflow agent") but that "its reroute join is
empty for those rows, so compliance cannot be measured from them".

The issue adds two consequences:

- every workflow subagent row the spike examined carries an empty `tool_use_id` and `rerouted: false`,
  so any compliance figure over the ledger silently counts them as compliant spawns;
- a row labelled with a role name picks up that role's soft budget, although it ran unconfined.

At `origin/main`:

- `policy/hooks/usage-log.py` sets a row's `workflow` field when the agent's transcript sits under a
  `wf_` directory, and `docs/usage.md` documents it;
- `harness usage --by role` in `bin/harness` groups rows by `agent_type` alone, so a workflow row named
  for a role is grouped with that role's spawns.

## Acceptance criteria

From the issue's "Done when" section:

1. **Given** a ledger with workflow and spawned rows, **when** `harness usage --by role` runs, **then**
   workflow rows are reported under their own bucket, not as spawns, in the text and the JSON.
2. **Given** a workflow row named for a role, **when** it is priced, **then** it carries no
   `budget_output_tokens` from a role it did not run under, or it is marked `unconfined`.
3. A fixture ledger with workflow and spawned rows proves both.

## Design

### Approach

From the issue: separate rows whose `workflow` field is set from spawned-subagent rows in the `--by role`
report, and stop attaching a role's soft budget to a workflow row.

### Decisions and alternatives

The issue leaves one choice open for AC 2: drop the budget fields from a workflow row, or keep them and
mark the row `unconfined`. No choice is recorded.

### Do not implement

- Guarding the `Workflow` launch, which is #576 (AH-S217).
- Rewriting existing ledger rows. AD-11 allows only the usage worker's `upsert` to rewrite the ledger,
  and requires additive schema changes. [ASSUMPTION: the issue does not raise ledger rewrites; the
  constraint is read from AD-11.]

## Tasks

- [ ] Give workflow rows their own bucket in `--by role`, text and JSON (AC 1).
- [ ] Stop attaching a role budget to a workflow row, or mark it `unconfined` (AC 2).
- [ ] Fixture ledger with workflow and spawned rows, and tests (AC 3).
- [ ] Update `docs/usage.md` for the new bucket.

## Dev notes

### Bound decisions and requirements

- FR-23: Cross-runtime usage ledger. `harness usage` supports `--by role`.
- FR-31: Soft budgets in every brief. [ASSUMPTION: FR-31 covers stating budgets in briefs; its fit to
  pricing ledger rows is partial.]
- AD-11: Local ledgers are the system of record, and grow compatibly.

### Files to touch

- `bin/harness`: the `--by role` report.
- `policy/hooks/usage-log.py`: where budget fields attach to a row.
- `docs/usage.md`.
- A fixture ledger and a test module under `tests/`.

### Testing

A fixture ledger holding spawned rows and workflow rows (some named for a constrained role), asserting
the separate bucket in text and JSON, and the absent or `unconfined` budget.

### References

- [Source: docs/spikes/2026-09-22-workflow-tool-band-routing-and-ledger.md]
- [Source: docs/usage.md]
- [Source: policy/hooks/usage-log.py]
- [Source: bin/harness#cmd_usage]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-11]
- Spike PR: commit 0454d78 (#578)
- Issue: https://github.com/JakeSelby/agent-harness/issues/577

## Dev agent record

### Agent model used

Not started.

### Completion notes

Not started. The issue's milestone is v0.14.0.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
