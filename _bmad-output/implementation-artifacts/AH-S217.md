---
bmad_id: "AH-S217"
type: "story"
title: "Guard the Workflow tool launch: log it, honour delegation off, refuse constrained roles in agentType"
lifecycle: "active"
provenance: "authored"
github_issue: 576
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/576"
parent_bmad_id: "AH-E015"
parent_github_issue: 634
updated: "2026-09-23"
---

# AH-S217 — Guard the Workflow tool launch: log it, honour delegation off, refuse constrained roles in agentType

<!-- bmad-sync:begin -->
- **GitHub issue:** [#576](https://github.com/JakeSelby/agent-harness/issues/576)
- **Primary parent:** [AH-E015](https://github.com/JakeSelby/agent-harness/issues/634)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer who relies on the harness's spawn guards, I want a launch of the Claude Code `Workflow`
tool to be logged, refused under `delegation: off`, and refused when its script names a constrained
role, so that a workflow script cannot run a confined role's work in session, unconfined.

## Context and value

The #540 spike, `docs/spikes/2026-09-22-workflow-tool-band-routing-and-ledger.md`, recorded its result
as "partially bypassed, measured 2026-09-22 on Claude Code 2.1.280":

- a workflow script's `agent()` calls produce no `Agent` tool call in the session, so
  `tier-agent-spawns`, `brief-guard` and the constrained-role refusal in `lib/harness_core/lifecycle.py`
  never see them;
- band routing and role confinement are both bypassed, and a script can run a read-only harness role in
  session by naming it.

The issue adds that `agentType: 'reviewer'` runs the read-only role in session on a frontier-class
model. The launch itself is a tool call the harness can match, and its input is the script text
(`script` or `scriptPath`), so the check reads text the session already has.

At `origin/main`, `dispatch` in `lib/harness_core/lifecycle.py` has `PreToolUse` branches for `Bash`,
`Agent` and `WebFetch`, and none for `Workflow`.

## Acceptance criteria

From the issue's "Done when" section:

1. **Given** a `Workflow` launch, **when** the `PreToolUse` hook runs, **then** a decision row is
   written to `decisions.jsonl`.
2. **Given** `delegation: off`, **when** a `Workflow` launch arrives, **then** it is refused with a
   reason.
3. **Given** a script whose text names a constrained role in `agentType`, or carries a `harness-role:`
   marker for one, **when** it is launched, **then** it is refused with the same `role_instruction`
   sentence the `Agent` refusal carries.
4. **Given** a script naming only band workers, **when** it is launched, **then** it is allowed.
5. Tests drive `lifecycle.dispatch` with a `Workflow` payload for each case.

## Design

### Approach

From the issue: add a `PreToolUse` branch for `Workflow` in `lib/harness_core/lifecycle.py` beside the
`Agent` branch. It records the launch, refuses it under `delegation: off`, and scans the script text,
inline or at `scriptPath`, for a constrained role in `agentType` or a `harness-role:` marker. A refusal
reuses `role_deny` and `role_instruction`, so the sentence matches the `Agent` refusal.

### Decisions and alternatives

- Guard the launch, not the `agent()` calls: the spike found the calls never reach a hook, and the
  launch is the one point the harness can match.
- Reuse the `Agent` refusal's wording rather than a new message, so every refusal of a constrained role
  reads the same.

No rejected alternative is recorded.

### Do not implement

- Band routing for the script's `agent()` calls. The issue scopes the launch check only.
- The ledger separation, which is #577 (AH-S216).

[ASSUMPTION: the issue does not list out-of-scope items; these two follow from its scope and the sibling
issue.]

## Tasks

- [ ] Add the `Workflow` branch to `dispatch` (AC 1 to 4).
  - [ ] Write the decision row (AC 1).
  - [ ] Refuse under `delegation: off` (AC 2).
  - [ ] Scan `script` or `scriptPath` for constrained roles and markers (AC 3, 4).
- [ ] Tests through `lifecycle.dispatch` for each case (AC 5).
- [ ] Update the FR-41 status and the AD-8 named gap once shipped.

## Dev notes

### Bound decisions and requirements

- FR-41: Confinement however the spawn is named. Its status line says "The Workflow tool still bypasses
  it (#576, v0.14.0)".
- FR-30: Band routing for unnamed spawns, whose text in the PRD cites this issue. [ASSUMPTION: the
  issue does not route the script's spawns, so the FR-30 fit is partial.]
- AD-8: Constrained roles run only as isolated role workers. It names "the Workflow tool's `agent()`
  (#576)" as an unguarded path.
- AD-7: What a hook may and may not do. Enforcement may rely on a pre-tool deny.

### Files to touch

- `lib/harness_core/lifecycle.py`: the `Workflow` branch.
- A new test module under `tests/`, driving `lifecycle.dispatch`.

### Testing

Unit tests call `lifecycle.dispatch("claude-code", payload)` with a `Workflow` payload for: a plain
launch (row written), `delegation: off` (refused), a constrained role in `agentType` (refused), a
`harness-role:` marker (refused), and band workers only (allowed).

### References

- [Source: docs/spikes/2026-09-22-workflow-tool-band-routing-and-ledger.md]
- [Source: lib/harness_core/lifecycle.py#dispatch]
- [Source: lib/harness_core/lifecycle.py#role_instruction]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-8]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-41]
- Spike PR: commit 0454d78 (#578)
- Issue: https://github.com/JakeSelby/agent-harness/issues/576

## Dev agent record

### Agent model used

Not started.

### Completion notes

Not started. The issue's milestone is v0.14.0.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
