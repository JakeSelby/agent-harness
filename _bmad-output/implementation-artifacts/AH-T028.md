---
bmad_id: "AH-T028"
type: "task"
title: "Close the round-5 assessment's should-fix gaps in the native case drivers"
lifecycle: "active"
provenance: "authored"
github_issue: 742
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/742"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-25"
---

# AH-T028 — Close the round-5 assessment's should-fix gaps in the native case drivers

<!-- bmad-sync:begin -->
- **GitHub issue:** [#742](https://github.com/JakeSelby/agent-harness/issues/742)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

Close the should-fix gaps the strong-class assessment of 0.13.0's round 5 (source commit
`d86695b`) found between the native case drivers and the qualification procedure in
`docs/compatibility.md`. Each driver must observe its procedure clause from the run itself, or the
procedure must state the narrower claim the run does show, so a qualification record never claims
more than a driver read. [Source: https://github.com/JakeSelby/agent-harness/issues/742]

## Acceptance criteria

1. **Given** `cost-posture`, **when** the non-default variant is synced, **then** every role it
   leaves byte-identical is still installed as it was before the switch, the same link to the same
   target, and at least one such role is a link; a link replaced by an identical copy fails.
2. **Given** `cost-posture`'s routed spawn, **when** its brief is read, **then** it ends with the
   budget sentence `posture.budget_sentence` writes for the variant's default band row, not merely
   contains "Expected spend:".
3. **Given** `spawn-confinement`'s routed run, **when** it has passed, **then** the driver judges the
   same printed record again with the run's worker-state directory moved aside, requires that
   judgement to fail, and restores the directory.
4. **Given** `hook-composition`'s write turn, **when** it is read, **then** the transcript carries the
   harness coordinator's own `PostToolUse` notice for a Write, beside the user hook's log; its
   absence is `unverified`.
5. **Given** step 4, **when** it is read, **then** it states that on a client with no multi-file patch
   tool a multi-file patch is one turn writing each file with its file tool.
6. **Given** `migration-uninstall`, **when** a harness-owned setting is hand-edited, **then**
   `harness diff` names it as drift and exits 1 before uninstall runs, having not named it before
   the edit.
7. **Given** step 3 and `permission-controls`, **when** they are read, **then** both state that the
   auto posture is claimed as a mode, not as a restriction its classifier was seen to impose.
8. **Given** each change, **when** the unit suite runs, **then** a unit test covers it; **and** a live
   run of each changed case is recorded (owner-run, see the dev agent record).

## Tasks

- [x] `role_links`, `link_change` and `link_verdict` in the cost-posture driver (AC: 1)
- [x] `budget_ending` against the row's sentence (AC: 2)
- [x] `worker_state` and `missing_state_fails` in the routed layer (AC: 3)
- [x] `FLAGGED_LINE` in the patch prompt, `harness_post_notice` in the composition case (AC: 4)
- [x] Step 4 narrows the multi-file patch claim; the observation states it (AC: 5)
- [x] `drift_named` and `drift_verdict` before uninstall (AC: 6)
- [x] Step 3 and `AUTO_LIMIT` narrow the auto posture claim (AC: 7)
- [x] `tests/test_native_acceptance_driver_gaps.py`; the hook-composition fake writes the notice
      (AC: 8)
- [ ] Live run of the five changed cases on `claude-code-cli-macos` (AC: 8, owner-run)

## Dev notes

- **Gaps and dispositions.** Observed from the run: unchanged roles keep their link (AC 1), the
  brief ends with the row's sentence (AC 2), the missing-state rule on live output (AC 3), the
  harness's own `PostToolUse` entry (AC 4), drift before uninstall (AC 6). Narrowed in the
  procedure: the multi-file patch on Claude Code (AC 5), which has no single multi-file patch tool,
  and the auto posture (AC 7), because asking the provider's classifier to refuse something is
  neither deterministic nor safe to script.
- **Why the scanner notice.** The coordinator's `PostToolUse` writes no decision-log row for a
  Write. Its tool-output scanner (`policy/hooks/neutralize-tool-output.py`) returns additional
  context for a response matching `settings-json`, so the second patch file carries the benign line
  `beta keeps settings.json hooks` and the driver reads the notice from the raw transcript text. A
  unit test drives the real `lifecycle.dispatch` with a Write payload to pin that the line trips it.
  Whether the client records the context in its transcript is what the live run confirms.
- **Files.** `scripts/native_acceptance.py`, `docs/compatibility.md` (steps 3, 4, 7, 9),
  `tests/test_native_acceptance_driver_gaps.py` (new),
  `tests/test_native_acceptance_hook_composition.py` (fake writes the notice),
  `changelog.d/742.changed.md`.
- **Not changed.** The `compatibility/catalog.json` limitation naming #742 describes what the 0.13.0
  and 0.13.1 rounds read, which stays true of those records; retire it when a round under these
  drivers qualifies.
- [Source: docs/compatibility.md#qualification-procedure]
  [Source: docs/qualification-runbook.md#running]

## Dev agent record

- **Model:** Claude Opus 5.5, builder role.
- **Default taken:** none.
- **Completion notes.** Unit gate green. The live runs are the owner's: a Bash-tool subprocess has
  `CLAUDE_CODE_OAUTH_TOKEN` stripped, so no agent session can drive the client. From a clean
  checkout of this branch, in the owner's own shell with the token exported:

  ```sh
  python3 scripts/native_acceptance.py --client claude-code-cli-macos \
      --cases cost-posture,spawn-confinement,hook-composition,migration-uninstall,permission-controls \
      --out ../native-742.json
  ```

  Read each observation for the new clauses: "as the same link to the same target" and "ending in
  that row's budget sentence" (cost-posture), "state directory moved aside" (spawn-confinement),
  "The harness's own PostToolUse entry fired" (hook-composition), "harness diff, which had not named
  it" (migration-uninstall), and the auto-posture limit (permission-controls). A hook-composition
  `unverified` naming the missing notice means the client keeps no hook context in its transcript,
  and the driver needs another reading.
- **File list.** `scripts/native_acceptance.py`, `docs/compatibility.md`,
  `tests/test_native_acceptance_driver_gaps.py`, `tests/test_native_acceptance_hook_composition.py`,
  `changelog.d/742.changed.md`, `_bmad-output/implementation-artifacts/AH-T028.md`.
- **Live run, 2026-09-26.** `claude-code-cli-macos` on Claude Code 2.1.280, source commit 71ee0db
  (this branch with main merged). `cost-posture`, `spawn-confinement`, `migration-uninstall` and
  `permission-controls` passed, and each observation carries its new clause. `hook-composition`
  came back `unverified`: the user-owned hook fired for both writes, but the write turn's transcript
  carries no notice from the harness's own PostToolUse entry, so that entry was not observed
  firing. Per AC 8 the driver needs another reading before this merges.
- **Second reading of the hook-composition notice, 2026-09-26.** The run's disposable home was
  discarded, so its transcript could not be read; the cause was narrowed by elimination. The
  scanner does read the Write's content: in the case's own seeded home and git project the
  coordinator answers a `PostToolUse` Write of the flagged line with the notice in 0.07 s. Claude
  Code 2.1.280 records that notice in the shape the driver reads, as a `hook_additional_context`
  attachment with `hookName` `PostToolUse:Write` and the call's `toolUseID`: every flagged Write in
  the past six days of this machine's transcripts has one, parallel writes included. The record is
  written after the tool results of its batch, at the tail of the turn, and the case read the
  transcript once, the moment the headless turn returned, where `await_feed` and `await_gate`
  already wait for a late write; nor did it check that the model wrote the flagged line at all.
  The driver now fails the reading as `unverified` when `beta.txt` lacks the flagged line, waits
  up to `NOTICE_WAIT` (20 s) for the notice, and counts it only from the record whose `toolUseID`
  is the Write of `beta.txt`; a miss names the hook records the client kept for that call. A late
  write is the likelier cause but is unproven until the next live run.

## Review findings

- **CodeRabbit round 1, thread on `harness_post_notice` (minor).** The notice was matched as a
  substring of the raw transcript, so a prompt, tool result or reply quoting it would have passed
  the case. Fixed: the driver now reads it only from an `attachment` record of type
  `hook_additional_context` whose `hookName` is `PostToolUse:Write`, the shape Claude Code writes
  for a hook's additional context; a test pins that quoted, `Bash` and `hook_success` records do
  not count.
- **CodeRabbit round 1, review body (trivial).** The hook-composition fake appends the notice
  itself, so it proves the transcript reading, not that the client emits `PostToolUse:Write`
  context. Not changed: that is the owner's live run under AC 8.

## Change log

- 2026-09-25: story filled at build time; tasks, dev notes and dev agent record written.
- 2026-09-25: review round 1 recorded; the harness notice is read from its hook-context record.
- 2026-09-26: hook-composition awaits the harness notice, ties it to the flagged Write and checks
  the flagged line was written, after the live run's `unverified`.
