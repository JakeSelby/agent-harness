---
bmad_id: "AH-C066"
type: "chore"
title: "Release 0.13.0: fold the changelog, record the qualification, set the catalog released"
lifecycle: "completed"
provenance: "authored"
github_issue: 723
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/723"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-24"
---

# AH-C066 — Release 0.13.0: fold the changelog, record the qualification, set the catalog released

<!-- bmad-sync:begin -->
- **GitHub issue:** [#723](https://github.com/JakeSelby/agent-harness/issues/723)
- **Primary parent:** None
- **State:** completed

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

Publish 0.13.0. The candidate opened in #651 and its contract narrowed to `claude-code-cli-macos`
and `claude-code-cli-linux` in #701; what remains is the dated changelog section, released status
prose, a runbook that matches how the round was actually run, and the native evidence that lets
`scripts/release_preflight.py` publish. This release pull request is also the `changelog.d/`
cut-over that ends the lint's Unreleased exemption.

## Acceptance criteria

1. **Given** `CHANGELOG.md`, **when** the release is read, **then** `## [Unreleased]` is empty and
   `## [0.13.0] — 2026-09-23` holds one Added, one Changed and one Fixed block carrying every
   previously Unreleased entry verbatim and in order, followed by every `changelog.d/` fragment.
2. **Given** `changelog.d/`, **when** the release lands, **then** it holds only its README.
3. **Given** `README.md`, `docs/compatibility.md` and `docs/releasing.md`, **when** they are read,
   **then** each states 0.13.0 as released on the two Claude Code CLI targets and 0.11.1 as the last
   release qualifying the Codex CLI, and `tests/test_docs.py` pins the README wording.
4. **Given** `docs/qualification-runbook.md`, **when** an operator follows it, **then** the round
   example passes no `--model` and says what is recorded instead, and Target hosts states that a
   Linux target is refused on a macOS host and gives the container sequence that ran the round.
5. **Given** the round's evidence records, **when** the catalog is set `released` and pinned to the
   qualification source commit with each record's digest, **then** `release_preflight.py` passes.

## Tasks

- [x] Fold Unreleased into the 0.13.0 section, merging duplicate headings (AC 1).
- [x] Assemble the fragments with `release_notes.py --changelog 0.13.0` and merge them in (AC 1, 2).
- [x] Released status prose and the README test pin (AC 3).
- [x] Runbook: routed model, host refusal, container sequence, subscription token (AC 4).
- [x] `cost_bench.py static --write`: no change against the candidate's figure.
- [x] Evidence records, catalog `released`, source commit, digests, versions, regenerated
      compatibility block, `**Qualified:**` pin flipped (AC 5).
- [x] Fold the nine fragments merged after the cut (#710, #725, #728-#733) into the 0.13.0 blocks
      and drop the #719 waiver (AC 1, 2).
- [x] Record the round-5 assessment's limitations in the catalog and the permission-controls hand
      comparison with each record's observations (AC 5).

## Dev notes

Files: `CHANGELOG.md`, `changelog.d/`, `README.md`, `docs/compatibility.md`, `docs/releasing.md`,
`docs/qualification-runbook.md`, `tests/test_docs.py`; the evidence commit adds
`compatibility/catalog.json` and `compatibility/evidence/*-0.13.0.json`. Gate: `bin/harness lint`
and the unit suite.

The evidence is round 5 at `d86695b`: 12 of 12 on `claude-code-cli-macos` (client 2.1.280) and on
`claude-code-cli-linux` (client 2.1.278, in the target image), sonnet executing and opus assessing.
Linux `stance-switch` was unverified on its first pass and rerun alone into the same durable log;
each record carries that and the hand comparison as round-level observations. The assessor ruled
the targets qualified with the stated limitations once limitation (b) was widened and (e) added;
the catalog carries four new limitations and amends the recognition one with #739. Follow-up
#741 (v0.13.1) owes the other eleven cases' hand comparisons.
[Source: docs/releasing.md#source-and-qualification]
[Source: changelog.d/README.md]
[Source: _bmad-output/implementation-artifacts/AH-C064.md]

## Dev agent record

Claude Opus 5.5 as a builder subagent, worktree `release-0.13.0`. `release_notes.py` refuses while
Unreleased holds hand-written entries, so Unreleased was set aside, the fragments assembled by the
script, and the hand-written entries merged in ahead of them under each heading.

## Change log

- 2026-09-23 — first written with the release pull request, ahead of the qualification evidence.
- 2026-09-23 — evidence commit: round-5 records, catalog released and pinned, limitations,
  fragments merged since the cut folded in.
