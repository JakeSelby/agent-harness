---
bmad_id: "AH-B092"
type: "bug"
title: "A release pull request cannot pass the changelog rule without leaving a waiver behind"
lifecycle: "active"
provenance: "authored"
github_issue: 773
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/773"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-25"
---

# AH-B092 — A release pull request cannot pass the changelog rule without leaving a waiver behind

<!-- bmad-sync:begin -->
- **GitHub issue:** [#773](https://github.com/JakeSelby/agent-harness/issues/773)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Reproduction

1. Past the 0.13.0 cut-over, branch off `main` and run `python3 scripts/release_notes.py --changelog <version>`, then edit the status prose under `docs/`.
2. Commit and run `bin/harness lint`.
3. Observed: `changelog: this branch changes docs/... but adds no fragment`. The 0.13.1 release answered it with `changelog.d/769.none.md`, which stayed on `main` after the release.

## Root cause

`findings` in `lib/harness_core/changelog.py` accepts only a newly added fragment, or, before the cut-over, an edit to `## [Unreleased]`. A release branch deletes every fragment and adds a version section, so it matches neither and must add a waiver the assembly has already run past. [Source: lib/harness_core/changelog.py#findings]

## Acceptance criteria

1. **Given** a base past the cut-over carrying fragments, **when** a branch runs the assembly and edits `docs/`, **then** the lint reports no changelog finding and adds no waiver.
2. **Given** the same assembly left uncommitted, **when** the lint runs, **then** it passes the same way.
3. **Given** a branch that adds a version section by hand without deleting fragments, or deletes fragments without adding a version section, **when** it touches a governed root, **then** the fragment finding stands.
4. **Given** the release merged, **when** a later branch touches a governed root without a fragment, **then** the finding returns.

## Design

`assembles_release(root, fork)` returns true when the working tree's `CHANGELOG.md` has a `## [<version>]` heading the base's lacks and the branch deletes at least one fragment directly under `changelog.d/`. `findings` returns early on it, after the waiver-length check. Both halves are required so a hand-written section or a dropped fragment alone does not exempt a branch. Rejected: documenting the waiver as a release step with the assembly dropping it, since the assembly runs before the waiver is written and the waiver would still outlive the release. The pre-cut-over Unreleased exemption is unchanged. `changelog.d/769.none.md` stays on `main`; the next assembly consumes it.

## Regression test

`tests/test_changelog_release_branch.py`: `test_a_release_branch_that_assembles_the_fragments_needs_no_waiver` and `test_an_uncommitted_assembly_passes_the_same_way` fail on the previous rule and pass after the fix; the other three pin the rule's limits.

## Tasks

- [x] Add the release-branch exemption to the fragment rule (AC 1, 2).
- [x] Keep the finding for a partial release shape and after the release merges (AC 3, 4).
- [x] Tests for each criterion in a new test file.
- [x] State the behaviour in `changelog.d/README.md` and `docs/releasing.md`.
- [x] Add the `changelog.d/773.fixed.md` fragment.

## Dev notes

- Files: `lib/harness_core/changelog.py`, `tests/test_changelog_release_branch.py`, `changelog.d/README.md`, `docs/releasing.md`, `changelog.d/773.fixed.md`.
- No user-visible capability, so `product.json` is unchanged.
- [Source: changelog.d/README.md] [Source: docs/releasing.md#step-4]

## Dev agent record

- Model: Claude Opus 5.5, builder role.
- Default taken: none.
- Completion: the rule exempts a branch that assembles a release; five new tests; gate run on the worktree.
- File list: `lib/harness_core/changelog.py`, `tests/test_changelog_release_branch.py`, `changelog.d/README.md`, `changelog.d/773.fixed.md`, `docs/releasing.md`, this story.

## Review findings

- CodeRabbit, `tests/test_changelog_release_branch.py` line 77 (minor): the uncommitted-assembly
  test was said to return before reaching `assembles_release`, because the empty commit changes no
  ROOTS path. Not valid: `branch_changes` diffs the fork against the working tree, so the
  uncommitted `docs/releasing.md` edit counts. With `assembles_release` stubbed to return False the
  test fails with the missing-fragment finding. Answered in the thread and resolved; no change.
- The review body carried no findings beyond that inline comment.

## Change log

- 2026-09-25: story filled with the design and the implementation record.
- 2026-09-25: review findings recorded; the one CodeRabbit finding was answered with no change.
