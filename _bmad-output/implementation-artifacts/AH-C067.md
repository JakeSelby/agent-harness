---
bmad_id: "AH-C067"
type: "chore"
title: "Reserve IDs and write story files for the cache-rebuild epic"
lifecycle: "active"
provenance: "authored"
github_issue: 746
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/746"
parent_bmad_id: "AH-E018"
parent_github_issue: 745
updated: "2026-09-23"
---

# AH-C067 — Reserve IDs and write story files for the cache-rebuild epic

<!-- bmad-sync:begin -->
- **GitHub issue:** [#746](https://github.com/JakeSelby/agent-harness/issues/746)
- **Primary parent:** [AH-E018](https://github.com/JakeSelby/agent-harness/issues/745)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

Give the cache-rebuild epic (#745) and its six children a BMad ID each and a story file that passes the
depth check, in one docs pull request, so that each item can later be built from its own story
([#746](https://github.com/JakeSelby/agent-harness/issues/746)). Nothing is built here. It follows the
pattern of #684, which carried the re-baseline follow-ups in #709.

The breakdown comes from an approved plan. As `docs/bmad-governance.md` asks, the plan's addendum is
distilled into these story files, which become the durable record.

**The seven items, in filing order:**

| Issue | BMad ID | Kind | Title |
| --- | --- | --- | --- |
| #745 | AH-E018 | epic | Cut cache-rebuild spend in long interactive sessions |
| #746 | AH-C067 | chore | Reserve IDs and write story files for the cache-rebuild epic |
| #747 | AH-SP010 | spike | Spike: does a UserPromptSubmit block hold the prompt in the CLI and VS Code? |
| #748 | AH-S227 | story | harness usage --by rebuild: attribute cache rebuilds to causes |
| #749 | AH-S228 | story | Refresh the handoff at task boundaries in /land and /close-out |
| #750 | AH-S229 | story | Cold-resume guard: hold one prompt after a long idle on a large context |
| #751 | AH-SP011 | spike | Spike: one-week trial of an earlier auto-compact window |

All seven are on the backlog, with no milestone, and the six children are native sub-issues of #745.

## Acceptance criteria

From the issue's Acceptance section ([#746](https://github.com/JakeSelby/agent-harness/issues/746)):

1. **Given** each of #745 to #751, **when** the map is read, **then** it has a BMad ID, and
   `python3 scripts/bmad_issue_sync.py audit --delivery <N>` passes its depth check.
2. **Given** the corpus after this change, **when** `audit` and `sprint-status --check` run, **then** both
   are clean.
3. **Given** `epics.md`, **when** it is read, **then** it lists AH-E018 with its six children, and its
   requirement coverage names the epic.

## Tasks

- [x] File the seven issues with `scripts/bmad_issue_sync.py new`, the epic first (AC: 1)
- [x] Write the seven story files from the plan's addendum, the issues and the code at `origin/main` (AC: 1)
- [x] Add AH-E018 and its children to `epics.md`, with its requirement coverage (AC: 3)
- [x] Run `audit --delivery` for each issue, then `audit` and `sprint-status --check` (AC: 1, 2)

## Dev notes

- **Bound:** FR-9 Traceable work items; FR-64 Story files as design records; FR-65 A current corpus.
  AD-5 Public planning with the design in the story file.
- **Files:** `_bmad-output/issue-map.json`, `_bmad-output/implementation-artifacts/sprint-status.yaml`,
  `_bmad-output/planning-artifacts/epics.md`, and the seven story files AH-E018, AH-C067, AH-SP010,
  AH-SP011 and AH-S227 to AH-S229.
- **Filing.** `new` filed each issue, and each reservation failed on the first try because the list
  endpoint had not yet returned the new issue, the failure AH-B040 (#392) records. `reserve --issue N`
  then mapped each one, with its parent. `apply` projects the Planning block and the parent only once the
  artifacts are on `main`, so the six parent links were set through the sub-issues API to match the map;
  `apply` after the merge adds the Planning blocks.
- **Public evidence only.** The story files carry the 2026-09-23 investigation's percentages and token
  thresholds, not its dollar figures, local paths, sessions or transcripts (`docs/bmad-governance.md`).
- **Testing:** `python3 bin/harness lint`, `python3 -m unittest discover -s tests`,
  `bin/harness generate --check`, `python3 scripts/bmad_issue_sync.py audit`, `sprint-status --check`,
  and `audit --delivery` for each of #745 to #751.
- [Source: https://github.com/JakeSelby/agent-harness/issues/746]
- [Source: https://github.com/JakeSelby/agent-harness/issues/745]
- [Source: docs/bmad-governance.md]
- [Source: docs/bmad.md]

## Dev agent record

- **Agent:** the filing session filed the issues and wrote the story files; a read-only gatherer
  collected the code facts they cite.
- **Completion:** each file passes `audit --delivery` at the pull request's head. Nothing else was built.
- **File list:** the seven story files, `issue-map.json`, `sprint-status.yaml` and `epics.md`.

## Change log

- 2026-09-23: written for the filing.
- 2026-09-25: merged main after roadmap step 3 (#809), which moved #745 and five children to v0.17.0 and #748 to v0.15.0; the no-milestone note above is as of 2026-09-23, and the epic's window in `epics.md` is now v0.15.0 to v0.17.0.
