---
bmad_id: "AH-S200"
type: "story"
title: "Modes: named selection bundles"
lifecycle: "active"
provenance: "authored"
github_issue: 557
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/557"
parent_bmad_id: "AH-E011"
parent_github_issue: 552
updated: "2026-09-23"
---

# AH-S200 — Modes: named selection bundles

<!-- bmad-sync:begin -->
- **GitHub issue:** [#557](https://github.com/JakeSelby/agent-harness/issues/557)
- **Primary parent:** [AH-E011](https://github.com/JakeSelby/agent-harness/issues/552)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer using the harness, I want to select a named mode that bundles a set of selections, for
my configuration or one session, so that I can switch the harness into a known shape without setting
each unit by hand, while any key I typed myself still wins.

## Context and value

Epic #552 defines a mode as a named set of selections, with users keeping their own keys above any
mode [Source: #552]. This story adds the mechanism and two shipped modes; #558 adds the `superpowers` mode on top of
it [Source: #558]. On `main` there is no `primitives/modes/` directory and no `docs/modes.md`.
[Source: https://github.com/JakeSelby/agent-harness/issues/557]

## Acceptance criteria

1. **Given** the five sources, **when** selections resolve, **then** a test proves the order default,
   mode, user, project, session.
2. **Given** an unknown mode, or a mode naming an unknown unit or a non-selection key, **when** it is
   selected, **then** it is an error before sync touches a file.
3. **Given** a mode file that switches a core hook off without the acknowledgement, **when** it is
   loaded, **then** it is refused.
4. **Given** a user primitive root, **when** it adds a mode under `modes/`, **then** the mode is
   available; a duplicate name is an error.
5. **Given** `harness config set mode minimal`, **when** `harness sync` runs, **then** it produces the
   documented state and `harness diff` is clean.
6. **Given** the docs, **when** `docs/modes.md` is read, **then** it lists what each shipped mode
   changes, with the precedence rule stated once.

## Design

### Approach

- A mode file is `primitives/modes/<name>.json`: `schema_version`, `description`, then selection keys
  in the #554 selection shape.
- `mode` in `config.json`, or `HARNESS_MODE` for one session, selects a mode; `harness selection`
  reports `mode:<name>` as a source.
- A mode sits below the user's explicit keys.
- Shipped modes:
  - `full`: empty apart from its description; the implicit default.
  - `minimal`: every hook on; the cost, delegation and autonomy stances kept; every other stance `off`
    or `light`; every workflow off.

### Decisions and alternatives

- **A key you typed always wins over a mode** (issue text).
- **User roots may add modes**, with duplicate names refused.

### Do not implement

- The `superpowers` mode and the `delegated` variants (#558).

### Recorded gap against the spine and PRD

AD-2 requires that "a developer's explicit choice shadows a mode key, while a value `harness init` wrote
as a default does not", and names #557 as the story that picks the mechanism. FR-16 makes it testable:
after a default `init`, selecting the `superpowers` mode changes `plan-ceremony`, and `harness selection`
names every mode key a user key shadowed. This issue's acceptance states only that a user's explicit key
wins; it has no criterion that separates init-written defaults from typed keys, and none for reporting
shadowed keys. Recorded here; the issue was not changed.

## Tasks

- [ ] Mode file schema and loader under `primitives/modes/` and user roots (AC: 2, 4)
- [ ] Insert the mode layer between default and user in the resolver; `HARNESS_MODE` sugar (AC: 1)
- [ ] Validate modes before sync, including the core-hook acknowledgement (AC: 2, 3)
- [ ] Ship `full` and `minimal` (AC: 5)
- [ ] Write `docs/modes.md` (AC: 6)
- [ ] Separate init-written defaults from typed keys, and report shadowed mode keys (AD-2, FR-16; not in
      the issue's acceptance)

## Dev notes

### Bound decisions and requirements

- FR-16, Modes: `HARNESS_MODE` selects a mode for one session without changing stored selections.
- AD-2, One resolver, fixed invariants: the mode-shadowing rule above.
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-2]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-16]

### Files to touch

`primitives/modes/full.json`, `primitives/modes/minimal.json`, `docs/modes.md` (named by the issue);
the resolver in `policy/hooks/posture.py` and config validation in `bin/harness`.
[ASSUMPTION: the resolver and validation paths are inferred from #554's design, not named here.]

### Testing

A resolution-order test across all five sources; refusal tests for unknown modes, unknown units,
non-selection keys, the missing core acknowledgement and duplicate names; a sync-and-diff test for
`minimal`.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/557]
- [Source: https://github.com/JakeSelby/agent-harness/issues/554]
- [Source: https://github.com/JakeSelby/agent-harness/issues/552]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Not started. No mode files, mode loader or modes documentation exist on `main`.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
- 2026-09-23: corrected after sample review: explicit sources for claims taken from the epic or a sibling
  issue, and the fit of each binding and current-state claim rechecked against the record.
