---
bmad_id: "AH-S231"
type: "story"
title: "Adopt CodeRabbit review and clear review-bot threads before merge"
lifecycle: "active"
provenance: "authored"
github_issue: 785
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/785"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-24"
---

# AH-S231 — Adopt CodeRabbit review and clear review-bot threads before merge

<!-- bmad-sync:begin -->
- **GitHub issue:** [#785](https://github.com/JakeSelby/agent-harness/issues/785)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As the maintainer of agent-harness, I want CodeRabbit to review every pull request into `main` and
`/build` to clear its threads before I approve, so that each pull request gets a second reviewer and
`/land` never meets a thread block by surprise.

## Context and value

CodeRabbit is free for public repositories, and on BMAD-METHOD#2957 it caught a real bug in a small
prose change. The `main-review` ruleset sets `required_review_thread_resolution: true`, and `/land`
merges with a plain `gh pr merge --squash --delete-branch`, which refuses a blocked merge unless
given `--admin`. `/land` never reads threads, so without this change the first bot comment would
only surface when `gh pr merge` refuses. Outside contributors also need to know the loop, because
`main` won't merge their PR while a review thread is open.

## Acceptance criteria

1. **Given** `.coderabbit.yaml`, **when** it is validated against
   `https://coderabbit.ai/integrations/schema.v2.json`, **then** it has 0 schema errors and 0 keys
   missing from the schema.
2. **Given** `/build`, **when** it has opened a pull request in a repository that runs a review
   bot, **then** step 6 waits up to ten minutes, fixes or answers each unresolved bot thread,
   resolves it, asks for one more review after a fix, stops after two rounds, and never resolves
   a human's thread.
3. **Given** `/land`, **when** the pull request has an unresolved review thread, **then** step 1
   names it and stops.
4. **Given** `CONTRIBUTING.md`, **when** an outside contributor reads `## Review`, **then** it tells
   them how to work with CodeRabbit's comments and how to ask for another pass.
5. **Given** the change, **when** the gate runs, **then** `bin/harness lint`, the unit tests and
   `bin/harness generate --check` pass.
6. **Given** the app is installed, **when** a pull request into `main` opens, **then** CodeRabbit
   reviews it.

## Design

### Approach

A schema-validated `.coderabbit.yaml`: the chill profile, one review per pull request into `main`,
no summary written into the PR description, prose linters off, path instructions for
agent-executed prose and the Python 3.9 floor, and generated projections and planning records
excluded. `/build` gains step 6, which answers the bot's threads (fix and push, or reply, then
resolve) before a pull request goes up for approval, and its report line names the bot threads
answered and any still open. `/land` step 1 stops on an unresolved review thread, by name.
`CONTRIBUTING.md` gains a paragraph on working with the bot.

```text
/build ── PR ──▶ GitHub ── PR opened ──▶ *CodeRabbit (.coderabbit.yaml)
                                               │
                                        review threads
                                               ▼
*/build step 6 ◀┄┄ waits up to 10 min ┄┄ unresolved threads
     │ fix and push, or reply; then resolve
     ▼
you approve ──▶ */land step 1: no open thread ──▶ squash merge
```

### Decisions and alternatives

- **No automatic re-review after a fix is pushed**, as BMad has it (`auto_incremental_review:
  false`). A contributor still gets a full review when the PR opens, asks for another with
  `@coderabbitai review`, and uses their own rate budget. The alternative, automatic re-review,
  confirms each fix unasked but roughly doubles review volume at about 35 PRs a day.
- **Fixes happen in `/build`, before approval, in one PR** with the config. `/land` only checks;
  a human's thread is theirs to resolve.
- **Two rounds at most** in step 6; whatever remains is reported rather than looped on.
- **`/build` asks for one re-review after pushing a fix**, so step 6's second round has a review
  to answer. Automatic re-review stays off, so contributors' pushes cost nothing extra.
- **Step 6 answers threads only.** Findings CodeRabbit leaves in the review body, such as
  outside-diff-range comments, are not read, because only threads block a merge; revisit with the
  hit-rate measurement.
- **CodeRabbit never commits to the branch on its own.** Unit-test generation, the CI fixer and
  the merge-conflict resolver are off: `/build` pushes to the branch, a bot commit there rejects
  its next push, and the issue map's conflicts need their own recipe. Docstrings and autofix stay
  available, because they run only when someone asks.
- **Greptile deferred.** It is free only through an open-source programme that requires attesting
  the repo is not part of a commercial product, and one bot is enough to start.

### Do not implement

- Greptile, any change to the `main-review` ruleset, CodeRabbit as a required check, landing copy
  or `product.json`.
- A `.coderabbit.yaml` in `templates/repo/`; wait until this one has run for a couple of weeks.
- Codex automatic review as a second reviewer; reconsider after measuring CodeRabbit's hit rate.

## Tasks

- [x] Copy the validated config to `.coderabbit.yaml` (AC: 1)
- [x] Add `/build` step 6 and extend its report line (AC: 2)
- [x] Extend `/land` step 1 with the thread check (AC: 3)
- [x] Add the contributor paragraph to `CONTRIBUTING.md` (AC: 4)
- [x] Regenerate `claude/commands/` and run the gate (AC: 5)
- [x] Raise the one-screen command body cap in `tests/test_commands.py` from 35 to 40 lines (AC: 5)
- [x] Pin step 6 and the `/land` thread stop in `tests/test_commands.py` (AC: 2, 3)
- [x] Add the changelog fragment
- [x] Add the CodeRabbit reviews badge to `README.md` (AC: 6)
- [x] Install the CodeRabbit app on JakeSelby/agent-harness and confirm a review (AC: 6)

## Dev notes

### Bound decisions and requirements

- PRD 2026-09-23 FR-42 Delivery-loop workflows: `/build` and `/land` text stays runtime-neutral,
  and `/land` merges only after proving the merge.

### Files to touch

- `.coderabbit.yaml`
- `primitives/workflows/build.md`, `primitives/workflows/land.md`
- `claude/commands/build.md`, `claude/commands/land.md`, only through `bin/harness generate`
- `CONTRIBUTING.md`
- `README.md`, the CodeRabbit reviews badge
- `changelog.d/785.changed.md`
- `tests/test_commands.py`, the one-screen body cap and two wording tests
- `_bmad-output/implementation-artifacts/AH-S231.md`, `_bmad-output/issue-map.json`,
  `_bmad-output/implementation-artifacts/sprint-status.yaml` (the last two as the sync tool wrote
  them)
- `.agent-harness/plans/2026-09-24-coderabbit-review.md`

`claude/agents/`, `claude/commands/` and `claude/CLAUDE.md` are generated
(`lib/harness_core/catalog.py`), and `claude/rules`, `claude/skills` and `claude/stances` are
symlinks into `primitives/`. That is why the config's path filters drop the first three and review
`primitives/`. `mode: "off"` is quoted in the config because YAML 1.1 reads a bare `off` as false.

### Testing

The change is instruction text and configuration, plus `tests/test_commands.py`. Its one-screen cap
rises from 35 to 40 lines, because `land.md` already sat at 35 and the new text brings `build.md` to
40 and `land.md` to 37. Two tests there pin step 6 and the `/land` thread stop; each asserted phrase
is absent from the commands on `main`, so both fail without the change. The gate is `python3
bin/harness lint`, `python3 -m unittest discover -s tests` and `python3 bin/harness generate
--check`. The config was validated during planning with jsonschema against the live schema; because
the schema allows extra properties, a key walk found 0 unknown keys, and its controls flagged
`reviews.not_a_key` and `profile: loud`.

### References

- https://github.com/JakeSelby/agent-harness/issues/785
- https://coderabbit.ai/integrations/schema.v2.json
- https://github.com/bmad-code-org/BMAD-METHOD/pull/2957
- [Source: .agent-harness/plans/2026-09-24-coderabbit-review.md#addendum]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#fr-42-delivery-loop-workflows]

## Dev agent record

### Agent model used

Not recorded.

### Completion notes

Delivered as designed, with two departures from the plan:

- The one-screen cap in `tests/test_commands.py` rises from 35 to 40 lines. `land.md` already sat
  at the cap, so the approved `/land` sentence could not fit without it, and the re-review
  sentence takes `build.md` to 40; the alternative was to trim approved text.
- The committed plan copy differs from the approved plan in three lines that lint rejects outside
  `docs/`: a project name in the two governance lines, and the trailer's email address. The
  meaning is unchanged.
- Review patches change three approved texts: the config's Python 3.9 glob and one more path
  filter, step 6's "and say so if none arrives", and `/land`'s "(GraphQL `reviewThreads`)". The
  config still validates against the schema with 0 errors and 0 unknown keys.

Installing the CodeRabbit app is the maintainer's step and is still open.

### File list

As under Files to touch.

## Review findings

`bmad-code-review` on 2026-09-24, in full mode against this story, with four layers: Blind Hunter,
Edge Case Hunter, Verification Gap and Acceptance Auditor. The Verification Gap layer found no gaps.
The result was 3 decision-needed, 7 patch, 0 defer and 7 rejected. The maintainer resolved all
three decisions the same day.

- [x] [Review][Decision] Round two of step 6 cannot happen — `auto_incremental_review: false`
  means a pushed fix draws no second review, and step 6 never asks for one. Options: ask the bot
  for one re-review after pushing fixes, or make it one round and report what remains. Resolved:
  re-review; step 6 now asks the bot for one more review after pushing a fix.
- [x] [Review][Decision] Step 6 reads only threads — CodeRabbit puts outside-diff-range and
  nitpick comments in the review body, where step 6 never looks. Options: extend step 6 to the
  latest bot review body, or keep it to the threads that block a merge. Resolved: threads only.
- [x] [Review][Decision] "Every PR into `main`, once when it opens" overstates — with
  `drafts: false`, a draft is reviewed only when it is marked ready, and `chore(release)` titles
  and Dependabot are skipped. The claim is in `CONTRIBUTING.md` and AC 6. Options: add "or when a
  draft is marked ready" to the approved paragraph, or keep it. Resolved: added.
- [x] [Review][Patch] The Python 3.9 path instruction matched no hook code [.coderabbit.yaml:45]
  — `claude/hooks` is a symlink to `policy/hooks`, so a diff never carries `claude/hooks/**`. The
  glob now names `policy/hooks/**`, `adapters/**` and `.github/scripts/**`.
- [x] [Review][Patch] Step 6 reported a missing review as a clean one
  [primitives/workflows/build.md:37] — it now says so when none arrives, as the plan's risks
  intended.
- [x] [Review][Patch] `/land` named no way to find unresolved threads
  [primitives/workflows/land.md:18] — it now names GraphQL `reviewThreads`.
- [x] [Review][Patch] The new behaviour had no test [tests/test_commands.py:167] — two tests pin
  step 6 and the `/land` thread stop, beside the existing `/land` wording test.
- [x] [Review][Patch] The changelog fragment led with this repository's adoption and justified
  `/land` by this repository's ruleset [changelog.d/785.changed.md:1] — rewritten for harness
  users.
- [x] [Review][Patch] The generated `claude/CLAUDE.personal.template.md` was reviewable
  [.coderabbit.yaml:36] — added to `path_filters`.
- [x] [Review][Patch] Testing said "No code changes" while `tests/` changed
  [_bmad-output/implementation-artifacts/AH-S231.md] — corrected.

### Rejected

- `/land` stops on an unresolved thread even where branch rules do not require resolution — AC 3
  requires the stop, so changing it edits the spec.
- "A fix belongs to `/build`" leaves `/land` at a dead end — false: `/land` stops and names the
  thread before step 4 removes the worktree, so the fix has a place to happen.
- `/build` waits ten minutes in a repository with the config but no app — low: the window closes
  when the app is installed, and keying on a review already seen adds a branch.
- `Closes #785` closes the issue with AC 6 unchecked — false: the plan installs the app and has
  CodeRabbit review this pull request before it merges.
- The body cap now equals `build.md`'s length — false: any ceiling sits at some body's length;
  `land.md` sat at the old cap, and Testing records why it moved.
- Fix commits pushed in step 6 skip step 4's commit check — low: the squash merge takes the pull
  request title, and step 6 reruns the gate before each push.
- Neither the plan nor this story records a `Landing copy:` line — false: the plan's Step 6 gives
  it verbatim.

### CodeRabbit

The app's first review of #786, on 2026-09-24, confirmed AC 6 and opened one thread.

- [x] [CodeRabbit][Round 1] Step 6 asked for a follow-up review but never waited for it, so a fix
  could reach approval before the bot saw it [primitives/workflows/build.md:40] — step 6 now waits
  again after asking, and a test pins it.

The second review, after the fix, opened no thread. It left three findings in the review body,
which step 6 does not answer (threads only); none was acted on:

- [x] [CodeRabbit][Round 2] Qualify `CONTRIBUTING.md`'s review promise for `chore(release)`
  titles and Dependabot — no change: neither affects an outside contributor.
- [x] [CodeRabbit][Round 2] Step 6 also answers Greptile — no change: step 6 is bot-agnostic by
  design, for repositories that run Greptile.
- [x] [CodeRabbit][Round 2] The tests do not assert that the wait follows the re-review request —
  no change: the neighbouring command tests pin wording, not order.

## Change log

- 2026-09-24: written at build from the approved plan.
- 2026-09-24: review findings recorded; the config's Python glob, step 6 and the `/land` thread
  check corrected, and two tests added.
- 2026-09-24: the three review decisions resolved: step 6 asks for one re-review after a fix and
  stays thread-only, `CONTRIBUTING.md` covers drafts, and the body cap moves to 40.
- 2026-09-24: the maintainer installed the app; `README.md` gains the CodeRabbit reviews badge.
- 2026-09-24: CodeRabbit's first review recorded; step 6 now waits for the follow-up review.
- 2026-09-24: CodeRabbit's second review recorded; its three review-body findings were not acted on.
- 2026-09-24: finishing touches that commit to the branch on their own turned off.
