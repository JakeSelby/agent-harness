---
bmad_id: "AH-S199"
type: "story"
title: "Rules, skills, workflows and roles as switch kinds, with per-file links"
lifecycle: "active"
provenance: "authored"
github_issue: 556
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/556"
parent_bmad_id: "AH-E011"
parent_github_issue: 552
updated: "2026-09-23"
---

# AH-S199 — Rules, skills, workflows and roles as switch kinds, with per-file links

<!-- bmad-sync:begin -->
- **GitHub issue:** [#556](https://github.com/JakeSelby/agent-harness/issues/556)
- **Primary parent:** [AH-E011](https://github.com/JakeSelby/agent-harness/issues/552)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer using the harness, I want to switch individual rules, skills, workflows and roles off
and have sync project exactly what I selected, so that a unit I do not want is absent from every
runtime rather than merely ignored.

## Context and value

Epic #552 turns every installed unit into a switch [Source: #552]. Sync today links the harness rules into the Claude
home as one directory symlink (`rules/harness` pointing at `claude/rules` in `bin/harness`), so a single
rule cannot be withheld. This story replaces that link with per-file links and makes sync and the Codex
projections honour `off`. The PRD's FR-15 states the outcome: a disabled primitive is absent from every
projection.
[Source: https://github.com/JakeSelby/agent-harness/issues/556]

## Acceptance criteria

1. **Given** `harness config set rules.decisions-and-plans off`, **when** `harness sync` runs, **then**
   no link for that rule remains and `harness diff` is clean; setting it back restores the link.
2. **Given** an existing directory symlink, **when** sync migrates it to per-file links, **then** both
   states are in the ownership journal and `harness uninstall` restores the prior state.
3. **Given** an `off` skill, workflow or role, **when** sync runs, **then** the skill has no link under
   either skills home, the workflow has no command projection and no generated Codex skill, and the
   role has no agent file in either home.
4. **Given** a role that a cost row moves and that is `off`, **when** sync and routing run, **then** it
   is neither rendered nor linked, and the routing hook does not route to it.
5. **Given** any selection, **when** lint runs, **then** the lint cap figure is unchanged, while
   `harness selection` reports the effective always-loaded line count.
6. **Given** the change, **when** the suite runs, **then** `tests/test_harness.py` covers per-file links,
   retirement, migration, uninstall and `diff`.

## Design

### Approach

- `rules`, `skills`, `workflows` and `roles` are switch kinds; sync reads the resolved selection.
- The harness rules directory in the Claude home becomes a real directory of per-file links; an `off`
  unit is not linked or rendered, and a stale link is retired.
- Codex projections skip the same units.

### Decisions and alternatives

- **The lint cap counts the distribution's worst case**, not the selection in force; the effective
  count is reported by `harness selection` for information.
- **Migration is journaled both ways**, so uninstall can restore the directory symlink.

### Do not implement

- Hooks as a switch kind (#555) and the selection document (#554).
- Trimming a layered library's own skills; FR-16 puts that out of scope.

## Tasks

- [ ] Replace the rules directory symlink with per-file links; retire stale links (AC: 1)
- [ ] Journal the migration; restore on uninstall (AC: 2)
- [ ] Skip `off` skills, workflows and roles in Claude and Codex projections (AC: 3)
- [ ] Keep `off` roles out of rendering and routing (AC: 4)
- [ ] Report the effective always-loaded line count in `harness selection` (AC: 5)
- [ ] Tests in `tests/test_harness.py` (AC: 6)

## Dev notes

### Bound decisions and requirements

- FR-15, Selection model with switches for every primitive: a disabled primitive is absent from every
  projection.
- AD-3, Reversible configuration ownership: record prior and applied values, and restore only while the
  current value still matches what the harness applied.
- AD-14, For cost, hooks only route and refuse frontier: the routing hook must not route to an `off`
  role. [ASSUMPTION: AD-14 covers routing generally; its text does not mention switches.]
- NFR-9, Standing-context budget: the cap the lint enforces stays a distribution-wide figure.
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-3]

### Files to touch

`bin/harness` (sync, uninstall, `diff`, `selection`), the Codex projection code, the routing hook
`policy/hooks/tier-agent-spawns.py`, and `tests/test_harness.py` (named by the issue).
[ASSUMPTION: only the test file is named by the issue; the other paths are where the behaviour lives
on `main`.]

### Testing

`tests/test_harness.py`: per-file links, retirement, migration, uninstall and `diff`.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/556]
- [Source: bin/harness]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-15]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Not started. Sync still links the rules as one directory symlink.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
- 2026-09-23: corrected after sample review: explicit sources for claims taken from the epic or a sibling
  issue, and the fit of each binding and current-state claim rechecked against the record.
