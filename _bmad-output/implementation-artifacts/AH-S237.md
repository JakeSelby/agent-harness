---
bmad_id: "AH-S237"
type: "story"
title: "Replay: fix the six defects that keep the two arms from being comparable"
lifecycle: "active"
provenance: "authored"
github_issue: 793
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/793"
parent_bmad_id: "AH-E013"
parent_github_issue: 632
updated: "2026-09-25"
---

# AH-S237 — Replay: fix the six defects that keep the two arms from being comparable

<!-- bmad-sync:begin -->
- **GitHub issue:** [#793](https://github.com/JakeSelby/agent-harness/issues/793)
- **Primary parent:** [AH-E013](https://github.com/JakeSelby/agent-harness/issues/632)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a harness maintainer, I want the six replay defects that leave the harness and bare arms unequal fixed, so that a replay result can test SM-2's hypothesis.

## Context and value

The 2026-09-24 review of the live replay found six defects, listed in FR-56's status, that leave the harness and bare arms unequal or a task unfair. Until they are fixed, no replay result can test SM-2's hypothesis.

Traces to FR-56. Sibling fixes: #428 and #482.

## Acceptance criteria

1. The benchmark docs and the task manifest agree on the task count, and a test fails when they diverge.
2. Each task's `max_turns` reaches the CLI in scored runs, and a test asserts it is in the launched command.
3. The stop gate can fire in a replay snapshot: the snapshot is trusted for its run, and a run that ends with a red gate records stop-hook feedback.
4. Both arms get identical web access, whatever the fence allows.
5. The usage-prices task is passable by an arm that follows its prompt, or it leaves the set with the reason recorded.
6. Runs are captured as `stream-json`, so hook events and per-turn usage are in the saved record.
7. Given a fake launcher, the unit tests cover each fix.

## Design

### Approach

Every fix lands in `scripts/cost_bench.py`, the one runner both arms go through, so each fix reaches
both arms by construction.

- **Task count (AC 1).** The docs state the count as digits (`# 7 tasks x 2 arms x 2 reps`, `2 of the
  7 tasks are synthetic`), and a test reads every such phrase in `docs/*.md` against the manifest.
- **Turn cap (AC 2).** `arm_command` takes `max_turns` and emits `--max-turns`; `run_one` passes the
  task's own value, and the pre-flight passes its fixed three through the same parameter.
- **Stop gate (AC 3).** `trusted_run` lists the resolved snapshot in `~/.config/agent-harness/trusted.txt`
  for the length of the launch, the file `harness trust` writes and every stop-gate hook since v0.4.0
  reads, then removes exactly that line through `atomic_write`. A file the run created is removed.
- **Web access (AC 4).** The fence settings every arm launches with carry
  `permissions.deny: ["WebFetch", "WebSearch"]`. The web tools run outside the command sandbox, so the
  empty network allowlist never reached them; a deny outranks the harness profile's allow rules.
- **Usage-prices (AC 5).** The task moves, whole and unedited, to a new top-level `retired` list in
  `benchmarks/tasks.json` with a date and its reason. `load_tasks` reads only `tasks`.
- **Stream capture (AC 6).** Runs launch with `--output-format stream-json --include-hook-events`.
  `cli_messages` reads either the stream or a single JSON document kept earlier; `stop_hook_counts`
  counts `hook_response` events for `Stop` and those that blocked, as the new `stop_hooks` field and
  the existing `hook_blocks` field.

### Decisions and alternatives

- **Deny the web to both arms rather than allow it to both.** The command fence is network-off, and a
  task set that needs the web would not be reproducible offline. Allowing both the harness profile's
  domains would tie the bare arm to one profile's list.
- **Retire usage-prices rather than rework it.** Its held-back tests pin the shipped rates and call
  helpers (`price_for`, `load_prices`, `row_cost`) the prompt never names, while the prompt forbids
  prices from memory. A prompt supplying both would hand the arm its check.
- **Trust through the harness's own trust file, not the CLI's state file.** `.claude.json` is a
  credential-named file the runner never writes, and an environment switch would need a hook change
  that pinned tags older than it would not carry.
- **Trust every arm's snapshot, not only the harness arm's.** The bare profile has no hook to read it,
  and treating both arms alike keeps the arms different by profile only.
- **The pre-flight stays untrusted.** It runs lint in three turns; a full gate there would spend its cap.
- **A turn-capped run stays an error,** as a budget-capped run already is (AD-12).
- **No text fallback for hook blocks.** Block reasons also appear in the prompt and files the agent reads.

### Do not implement

- No change to the stop-gate hook or `harness trust`.
- No change to the publish threshold, δ, or the verdict arithmetic; those are SM-2's.
- No new live result, and no history row.
- The sibling fixes #428 and #482 stay separate.

## Tasks

- [x] State the task count as digits in `docs/benchmarks.md` and `docs/caught-in-the-act.md` (AC: 1)
  - [x] Test every stated count against the manifest, and prove the check fails on a stale one (AC: 1, 7)
- [x] Pass each task's `max_turns` as `--max-turns` in scored runs (AC: 2)
  - [x] Assert it in both arms' launched commands with a fake launcher (AC: 7)
- [x] Trust each scored snapshot for its run in the trust file, and remove exactly that line after (AC: 3)
  - [x] Test trust during the launch, removal after, survival of other roots, and removal on timeout (AC: 7)
  - [x] Count Stop hook runs and blocks from hook events, and test a red-gate stream (AC: 3, 7)
- [x] Deny `WebFetch` and `WebSearch` in every arm's launch settings (AC: 4)
  - [x] Test both arms launch with the same deny and the same network block (AC: 7)
- [x] Retire `usage-prices` to the manifest's `retired` list with its reason (AC: 5)
  - [x] Test it is gone from the loaded set and its reason is recorded (AC: 7)
- [x] Launch as `stream-json` with hook events; read both stream and single-document output (AC: 6)
  - [x] Test parity with the old form, a truncated last line, the pre-flight readers and backfill (AC: 7)

## Dev notes

### Bound decisions and requirements

- FR-56, Live replay against a bare arm: arms differ only by environment; the six defects are its
  v0.14.0 status. [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-56]
- AD-12, Measurement invariants: arms differ only by environment; unknown values are never zero; an
  errored run is never a failure.
  [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-12]

### Files to touch

- `scripts/cost_bench.py` — the command line, the fence, trust, stream parsing.
- `benchmarks/tasks.json` — `usage-prices` moved to `retired`.
- `docs/benchmarks.md`, `docs/caught-in-the-act.md` — task count, new behaviour.
- `tests/test_replay_parity.py` (new), `tests/test_cost_bench.py` — tests.
- `changelog.d/793.fixed.md`.

### Testing

`tests/test_replay_parity.py` holds one class per acceptance criterion, all on a fake launcher that
replays recorded output. `tests/test_cost_bench.py` changes the expected output format, the task
counts and one docstring.

### References

- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-09-24.md#Evidence from the review]
- [Source: claude/hooks/stop-gate.py#trusted] — the trust file and the folder-trust flag.
- [Source: bin/harness#cmd_trust] — the trust file's format.
- [Source: `claude --help`, CLI 2.1.280] — `--include-hook-events` works only with `stream-json`.
- [Source: a Claude Code bundle's message schema, read 2026-09-25] — `hook_response` carries `hook_event`, `stdout`,
  `output` and `exit_code`.
- [Source: claude/settings.template.json] — the harness profile's WebFetch and WebSearch allow rules.

## Dev agent record

### Agent model used

Claude Opus 5.5 (claude-opus-5-5), as the builder in an unattended run.

### Completion notes

All seven criteria implemented as designed. Default taken: none. Hook-event parsing is written to a
Claude Code bundle's message schema and a fake launcher; no live run has confirmed it, and no live run was made.
The stop-gate hook's own 240-second budget may release the turn unverified at a snapshot whose full
suite runs longer; that is the hook's behaviour, measured as it stands.

### File list

- `scripts/cost_bench.py`
- `benchmarks/tasks.json`
- `docs/benchmarks.md`
- `docs/caught-in-the-act.md`
- `tests/test_replay_parity.py`
- `tests/test_cost_bench.py`
- `changelog.d/793.fixed.md`
- `_bmad-output/implementation-artifacts/AH-S237.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`

## Review findings

None recorded yet.

## Change log

- 2026-09-25: design, tasks and dev notes filled at implementation.
