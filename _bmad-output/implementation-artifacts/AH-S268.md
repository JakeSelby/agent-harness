---
bmad_id: "AH-S268"
type: "story"
title: "Rename the plugin to model-citizen and keep recognizing the old ID"
lifecycle: "active"
provenance: "authored"
github_issue: 878
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/878"
parent_bmad_id: "AH-E020"
parent_github_issue: 875
updated: "2026-09-25"
---

# AH-S268 — Rename the plugin to model-citizen and keep recognizing the old ID

<!-- bmad-sync:begin -->
- **GitHub issue:** [#878](https://github.com/JakeSelby/agent-harness/issues/878)
- **Primary parent:** [AH-E020](https://github.com/JakeSelby/agent-harness/issues/875)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a Claude Code user who installs the plugin, I want it listed and installed as `model-citizen`, and doctor
to recognize my existing `agent-harness` install, so that the plugin carries the product's name and my
current install is neither broken nor silently doubled.

## Context and value

The rename (AD-24) changes the plugin and marketplace ID. Claude Code keys an installed plugin by
`<plugin>@<marketplace>`, so existing installs keep `agent-harness@agent-harness` until the user reinstalls.
Whether Claude Code carries an install across a renamed plugin is unverified, which is the rename's main
risk: doctor must recognize both IDs, and warn when both are enabled, because the skills would then load
twice. This is PR B of the plan.

## Acceptance criteria

1. **Given** `.claude-plugin/plugin.json` and `marketplace.json`, **then** the plugin `name` and the
   marketplace `name` are `model-citizen`, the `displayName` is Model Citizen, and the descriptions name
   Model Citizen. Their `homepage` and `repository` URLs are unchanged here.
2. **Given** a settings file enabling `agent-harness@agent-harness` only, or `model-citizen@model-citizen`
   only, **when** doctor runs, **then** it reports the marketplace plugin as installed, under either
   marketplace name.
3. **Given** both IDs enabled, **when** doctor runs, **then** it warns that the skills would load twice.
4. **Given** `docs/runtime-installation.md`, **then** it carries the new install command and the migration
   steps for an existing plugin install.
5. **Given** a scratch profile, **when** the v0.13.1 marketplace install is updated to this branch, **then**
   whether Claude Code keeps the old ID is recorded for the release notes.

## Design

### Approach

- Rename the plugin and marketplace in both manifests; leave URLs and the `version` field.
- In doctor's plugin install state, accept `agent-harness@agent-harness` and `model-citizen@model-citizen`
  as the enabled key and either marketplace name in the known marketplaces and the plugin cache; add the
  both-enabled warning.
- Add the install and migration steps to `docs/runtime-installation.md`:
  `/plugin marketplace add JakeSelby/model-citizen`, `/plugin install model-citizen@model-citizen`,
  `/plugin uninstall agent-harness@agent-harness`, `/plugin marketplace remove agent-harness`. Skills move
  from `/agent-harness:<skill>` to `/model-citizen:<skill>`.
- Add a changelog fragment `changelog.d/878.changed.md`: "The plugin is now `model-citizen@model-citizen`;
  reinstall it."

### Decisions and alternatives

- **Recognize both IDs rather than migrate the install.** The harness does not own Claude Code's plugin
  state, and the checkout install is unaffected by the plugin change.
- **Warn, do not fail, when both are enabled:** a doubled skill set is noisy, not unsafe.
- **URLs wait for AH-C079,** after the GitHub rename, so no manifest points at a repository that does not
  yet exist.

### Do not implement

- No `homepage` or `repository` change, and no fix to the stale plugin `version` fields; the plan defers
  that to a follow-up.
- No hooks in the plugin (AD-19).
- No command rename (AH-S267) and no copy outside the install doc (AH-S269).

## Tasks

- [x] Rename the plugin and marketplace in both manifests. (AC: 1)
- [x] Teach doctor both IDs and both marketplace names. (AC: 2)
- [x] Add the both-enabled warning. (AC: 3)
- [x] Add the install and migration steps to the install doc. (AC: 4)
- [ ] Run the scratch-profile check and record the result in this story and the release notes draft.
  (AC: 5)
- [x] Add the changelog fragment.

## Dev notes

### Bound decisions and requirements

- AD-24 (the plugin ID and recognition of the old one), AD-19 (the plugin carries no hooks).
- FR-17 and FR-18.

### Files to touch

- `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`.
- `bin/harness`: doctor's plugin install state and its report line.
- `docs/runtime-installation.md`, the plugin install section.
- `tests/test_plugin_manifests.py`.
- `changelog.d/878.changed.md`.

### Testing

- `tests/test_plugin_manifests.py`: the old ID only, the new ID only, and both enabled, the last asserting
  the warning.
- The scratch-profile check is manual: a signed-in empty profile, a working directory outside `$HOME`, and
  a scrubbed environment.

### References

- [Source: .agent-harness/plans/2026-09-25-model-citizen-rename.md#step-4--pr-a-and-pr-b] PR B. Its line
  numbers were taken at `a9968d9` and have drifted; doctor's check is `plugin_install_state` and
  `_plugin_install_line` in `bin/harness`.
- [Source: .agent-harness/plans/2026-09-25-model-citizen-rename.md#migration-for-existing-installs] the
  migration steps.
- [Source: .agent-harness/plans/2026-09-25-model-citizen-rename.md#risks] the old-ID risk.

## Dev agent record

### Agent model used

Agent: Claude Code.

### Completion notes

- Both manifests now name the plugin and marketplace `model-citizen`, displayName Model Citizen;
  `homepage`, `repository` and `version` are unchanged.
- `bin/harness` gains `PLUGIN_NAMES`; `plugin_install_state` accepts either plugin name as the
  enabled key, either repository name for a registered marketplace, and either name in the plugin
  cache. With both IDs enabled it records the older one under `doubled`, and
  `_plugin_install_line` adds a `plugin warning` line naming the `/plugin uninstall` to run.
- The install doc carries the new commands and a migration subsection.
- The scratch-profile check (AC 5) is not run in this change; it is scheduled separately and its
  result is still to be recorded here and in the release notes.

### File list

- `.claude-plugin/plugin.json`
- `.claude-plugin/marketplace.json`
- `bin/harness`
- `docs/runtime-installation.md`
- `tests/test_plugin_manifests.py`
- `changelog.d/878.changed.md`
- `_bmad-output/implementation-artifacts/AH-S268.md`

## Review findings

<!-- fill: each review finding and how it was resolved. -->

## Change log

- 2026-09-25: created in AH-C078 from the approved rename plan.
- 2026-09-25: implemented; moved to review with the scratch-profile check outstanding.
