---
bmad_id: "AH-S211"
type: "story"
title: "Ledger rows carry a schema version and readers tolerate unknown fields"
lifecycle: "active"
provenance: "authored"
github_issue: 644
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/644"
parent_bmad_id: "AH-E017"
parent_github_issue: 636
updated: "2026-09-23"
---

# AH-S211 — Ledger rows carry a schema version and readers tolerate unknown fields

<!-- bmad-sync:begin -->
- **GitHub issue:** [#644](https://github.com/JakeSelby/agent-harness/issues/644)
- **Primary parent:** [AH-E017](https://github.com/JakeSelby/agent-harness/issues/636)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer who reads or exports the harness's ledgers, I want every ledger row to carry a schema
version and every reader to tolerate fields it does not know, so that an upgrade never leaves old rows
unreadable or breaks an older reader on newer rows.

## Context and value

The usage ledger and decision log are public surface: their schemas and exported attribute names. The
issue records that their rows carry no schema version and that readers do not tolerate every change a
writer might make. AD-11 in the 2026-09-23 spine requires additive schema changes, a version on each row
from the next ledger change on, reader tolerance for unknown fields, and fold maps for renames; the
spine marks AD-11 partial, with the schema version planned under epic #636.

On `main`, the CLI already folds one kind of rename: `renamed_ids()` and `folded_rules()` in
`bin/harness` count a renamed detector's hits under its successor, from the registry's own `RENAMED`
map. AD-11 names this as the precedent.
[Source: https://github.com/JakeSelby/agent-harness/issues/644]

## Acceptance criteria

1. **Given** a row written by a newer writer, **when** the current reader reads it, **then** it is read
   without error.
2. **Given** a renamed field, **when** a reader reads an old row, **then** the field is folded to its
   new name.
3. **Given** each case above, **when** the suite runs, **then** a test covers it.

## Design

### Approach

- Add a schema version to each ledger row, from the next ledger change on.
- Make every ledger reader ignore unknown fields rather than fail on them.
- Ship a fold map with any field rename, on the pattern of the detector-id fold.
- Document the rule.

### Decisions and alternatives

- **Additive only:** schema changes add fields; renames fold; nothing is removed in place (AD-11).
- **Versioning starts at the next change**, not by rewriting existing rows; AD-11 allows only the usage
  worker's `upsert` to rewrite a ledger.

### Do not implement

- Rewriting historical rows to add a version.
- Changes to the export channel, which AD-11 keeps OTLP only and off by default.

## Tasks

- [ ] Add the schema version to rows written by the usage and decision ledgers (AC: 1)
- [ ] Make each reader tolerate unknown fields (AC: 1)
- [ ] Add a field fold map and use it in readers (AC: 2)
- [ ] Tests for a newer-writer row and a renamed field (AC: 3)
- [ ] Document the rule where the ledger schema is documented

## Dev notes

### Bound decisions and requirements

- AD-11, Local ledgers are the system of record, and grow compatibly: the schema rule above.
- FR-23, Cross-runtime usage ledger, and FR-27, Decision log: the two ledgers whose rows change.
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-11]

### Files to touch

The row writers `policy/hooks/usage-log.py` and `policy/hooks/decisions.py`; the readers in `bin/harness`
over `usage.jsonl` and `decisions.jsonl`; tests; the ledger documentation.
[ASSUMPTION: the issue names no files; these are where the ledgers are written and read on `main`.]

### Testing

A newer-writer row read by the current reader; a renamed field folded on read.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/644]
- [Source: https://github.com/JakeSelby/agent-harness/issues/636]
- [Source: bin/harness]
- [Source: policy/hooks/decisions.py]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Not started. Unscheduled: the issue has no milestone.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
