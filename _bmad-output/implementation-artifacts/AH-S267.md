---
bmad_id: "AH-S267"
type: "story"
title: "Add the citizen command and keep harness as an alias"
lifecycle: "active"
provenance: "authored"
github_issue: 877
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/877"
parent_bmad_id: "AH-E020"
parent_github_issue: 875
updated: "2026-09-25"
---

# AH-S267 — Add the citizen command and keep harness as an alias

<!-- bmad-sync:begin -->
- **GitHub issue:** [#877](https://github.com/JakeSelby/agent-harness/issues/877)
- **Primary parent:** [AH-E020](https://github.com/JakeSelby/agent-harness/issues/875)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a user of the renamed product, I want a `citizen` command that does everything `harness` does, so that
the command matches the brand while every script, hook and habit that runs `harness` keeps working.

## Context and value

The product is renamed Model Citizen (AD-24, the 2026-09-25 sprint change proposal). `harness` also collides
with another vendor's CLI, and no Homebrew, PyPI or npm package installs a `citizen` binary. The compatibility
policy lets a minor add a backward-compatible command, and removing `harness` would need a notice, the next
major and 90 days, so `harness` stays as a permanent alias with no deprecation. This is PR A of the plan; the
copy story (AH-S269) follows it so its `citizen` examples run.

## Acceptance criteria

1. **Given** a checkout, **when** `bin/citizen <subcommand>` runs, **then** it behaves exactly as
   `bin/harness <subcommand>`, because `bin/citizen` is a relative symlink to `harness`.
2. **Given** either name, **when** `--help` runs, **then** usage names the invoked command: `usage: citizen`
   through `bin/citizen`, `usage: harness` through `bin/harness`, and `citizen` for any other basename.
3. **Given** either name, **when** `--version` runs, **then** it prints `model-citizen <VERSION>`, and the
   other `agent-harness {VERSION}` banners print `model-citizen`.
4. **Given** a message in `bin/`, `lib/` or `policy/hooks/` that names the product or suggests a command,
   **then** it says Model Citizen and `citizen`, while every on-disk path on the keep list is unchanged.
5. **Given** hooks and the installer, **when** they locate the checkout, **then** they still find it through
   `bin/harness`, and checkout detection works when the CLI is invoked through either name.
6. **Given** the UX specification, **when** this story merges, **then** its command names in output read
   `citizen`, amended through `bmad-ux`'s update intent with a memlog entry.

## Design

### Approach

- Add `bin/citizen` as a relative symlink to `harness`. `bin/harness` stays the real file.
- Derive the parser's `prog` from the basename of `argv[0]`: `citizen` or `harness`, else `citizen`. Lead the
  usage docstring with `citizen`.
- Change the `agent-harness {VERSION}` banners, including the `--version` action, to `model-citizen`.
- Move messages that name the product or suggest a command to Model Citizen and `citizen`; leave every path.
- Add a changelog fragment `changelog.d/877.added.md`: "Added the `citizen` command; `harness` remains a
  supported alias."

### Decisions and alternatives

- **`bin/harness` stays the real file, and `bin/citizen` is the link.** Hooks and the installer locate the
  checkout by `bin/harness`, and lifecycle refusals print its absolute path; making `citizen` the real file
  would move every one of those lookups for no gain.
- **`harness` is a permanent alias with no deprecation notice** (the plan's Decision 1). The alternative,
  keeping `harness` alone, leaves the brand and command mismatched for good.
- **`prog` follows the invoked name,** so usage text matches what the user typed.

### Do not implement

- No rename of `~/.config/agent-harness`, `.agent-harness/`, `~/.local/state/agent-harness/`, launchd labels,
  `# harness:` markers, the `# agent-harness` PATH and ignore blocks, the Codex banner, or the `harness` rule
  and hook link names.
- No change to telemetry's `service.name`.
- No plugin ID change (AH-S268), no README or docs copy (AH-S269), no slug or site URL (AH-C079).
- The AGENTS.md Gate block and CI keep `bin/harness`.

## Tasks

- [x] Add the `bin/citizen` symlink. (AC: 1)
- [x] Derive `prog` from `argv[0]` and lead the usage docstring with `citizen`. (AC: 2)
- [x] Change the version banners and update the banner assertion in `tests/test_harness.py`. (AC: 3)
- [x] Move product-naming and command-suggesting messages in `bin/`, `lib/` and `policy/hooks/`. (AC: 4)
- [x] Test checkout detection through both names. (AC: 5)
- [x] Amend the UX specification's command names through `bmad-ux`, with a memlog entry. (AC: 6)
- [x] Add the changelog fragment; the pull request body carries
  `Landing copy: command examples move to citizen in the copy PR, #879`.

## Dev notes

### Bound decisions and requirements

- AD-24 (the brand and the stable identifiers), AD-19 (no second authority for distribution copy), AD-21
  (standard library on a Python 3.9 floor).
- FR-17 and FR-66; the compatibility policy's rules on adding and removing commands.

### Files to touch

- `bin/citizen` (new symlink), `bin/harness` (prog, docstring, banners, messages).
- Messages only: `lib/harness_core/`, `policy/hooks/`.
- `tests/`: a new test file for the alias, and the banner assertion in `tests/test_harness.py`.
- `_bmad-output/planning-artifacts/ux-designs/ux-agent-harness-2026-09-23/` and its memlog.
- `changelog.d/877.added.md`.

### Testing

- `bin/citizen --help` prints `usage: citizen`; `bin/harness --help` prints `usage: harness`.
- Both `--version` print `model-citizen <VERSION>`.
- Checkout detection works through either name.
- All Python 3.9-safe; the full gate runs.

### References

- [Source: .agent-harness/plans/2026-09-25-model-citizen-rename.md#step-4--pr-a-and-pr-b] PR A. Its line
  numbers were taken at `a9968d9` and have since drifted: `prog` and `--version` are set in the parser
  construction near the end of `bin/harness`, and the banners are the `agent-harness {VERSION}` strings in
  `sync`, `doctor`, `install`, `import` and `init`.
- [Source: policy/hooks/harness-session.py] and [Source: policy/hooks/usage-log.py] locate the checkout
  through `bin/harness`; [Source: scripts/install.sh] runs `bin/harness init` and `install`.
- [Source: .agent-harness/plans/2026-09-25-model-citizen-rename.md#inventory] the keep list.
- [Source: docs/compatibility-policy.md] adding and removing commands.

## Dev agent record

### Agent model used

Agent: Claude Code.

### Completion notes

- `bin/citizen` is a relative symlink to `harness`; `program_name()` derives `prog` from `argv[0]`,
  keeping `citizen` or `harness` and reading `citizen` for anything else. The usage docstring leads
  with `citizen` and names `harness` as the supported alias.
- The five `agent-harness {VERSION}` banners (`--version`, `sync`, `doctor`, `import`, `init`) and the
  `install` header print `model-citizen`; the plan counted six banners at `a9968d9`, the sixth being
  the `install` header, which has no version.
- Command suggestions in CLI and hook messages read `citizen`. The session hook's integration notice
  filter now matches `not installed (citizen`, the text the CLI prints.
- Left as `harness`: every `harness role run` message, because the refusal must match the delegation
  stance's sentence (`tests/test_role_refusal_matches_the_stance.py`), which the copy story owns; the
  `config set` "run `harness sync` to apply it" line, which an open pull request also edits; every
  `bin/harness` path; and the plugin detail in doctor, which AH-S268 owns.
- The installer does not link `harness` onto `PATH`, so there is no `citizen` link to add.
- The UX specification's command names read `citizen`, amended through `bmad-ux` Update with two
  memlog entries; its brand lines are left for AH-S269.

### File list

- `bin/citizen` (new symlink), `bin/harness`
- `policy/hooks/harness-session.py`, `policy/hooks/stop-gate.py`, `policy/hooks/tier-agent-spawns.py`
- `tests/test_citizen_alias.py` (new), `tests/test_harness.py`, `tests/test_band_workers.py`,
  `tests/test_hook_health.py`, `tests/test_init_defaults.py`, `tests/test_integration_surface.py`
- `_bmad-output/planning-artifacts/ux-designs/ux-agent-harness-2026-09-23/DESIGN.md`, `EXPERIENCE.md`
  and `.memlog.md`
- `changelog.d/877.added.md`
- `_bmad-output/implementation-artifacts/AH-S267.md`

## Review findings

- None yet; each review finding is recorded here with its resolution.

## Change log

- 2026-09-25: created in AH-C078 from the approved rename plan.
- 2026-09-25: implemented; tasks checked, dev agent record and file list filled.
