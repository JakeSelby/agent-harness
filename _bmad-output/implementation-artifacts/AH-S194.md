---
bmad_id: "AH-S194"
type: "story"
title: "Trim the harness's 12,607-token standing context"
lifecycle: "active"
provenance: "authored"
github_issue: 430
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/430"
parent_bmad_id: "AH-E013"
parent_github_issue: 632
updated: "2026-09-23"
---

# AH-S194 — Trim the harness's 12,607-token standing context

<!-- bmad-sync:begin -->
- **GitHub issue:** [#430](https://github.com/JakeSelby/agent-harness/issues/430)
- **Primary parent:** [AH-E013](https://github.com/JakeSelby/agent-harness/issues/632)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As the harness maintainer, I want the standing context the harness adds to every API call trimmed,
each change landed separately and measured, so that the harness's fixed per-call overhead shrinks
without changing which skills fire.

## Context and value

The issue measured 12,607 extra tokens in every API call against a bare profile, stable to within 15
tokens across eight task pairs, and reported that prefix as the whole of the harness arm's cost gap on
the first live set. That first set was later recorded as contaminated by the runner's sandbox fence and
a test-suite leak (#369), so the gap itself is not a clean figure; the prefix measurement is the part
this issue rests on. The issue also argues the prefix does not amortise on long tasks: it is re-read
alongside a growing conversation. The measured figure became the lint's always-loaded token cap in PR
#474 (`ALWAYS_LOADED_TOKEN_CAP`, a third of the measured standing context).
[Source: https://github.com/JakeSelby/agent-harness/issues/430]

The issue names three separable changes with a combined ceiling of about 25%:
1. defer the action-gated rules and stances behind the hooks that fire on the act (about -1,474 tokens);
2. trim the output style (about -900);
3. trim the 17 added skill descriptions (about -750).

## Acceptance criteria

1. **Given** the three changes, **when** each lands, **then** it lands separately with a recorded
   history row.
2. **Given** the trims, **when** they are measured, **then** the live prefix drops, stated as a measured
   number from a session transcript and never as an estimate from `benchmarks/static.json`.
3. **Given** trimmed skill and agent descriptions, **when** they change, **then** a before-and-after
   check on triggering accompanies the token count (the issue's "Risk" section).

## Design

### Approach

- **Changes 2 and 3** (delivered in PR #589 under #588): the `scannable` output style keeps all nine
  of its rules and loses its worked examples and register table; fifteen skill and eleven agent
  descriptions lose capability restatements and keep every trigger condition and literal user
  phrasing. A test freezes the trigger phrases per primitive.
- **Change 1** is framed as an unrun spike, `docs/spikes/2026-09-22-deferred-rule-text.md`, with a
  numeric exit criterion written before any run. Nothing is implemented for it.

### Decisions and alternatives

- **Change 1 is an architecture change and gets its own spike** (issue text): the rules layer has not
  adopted the defer-behind-a-hook pattern that `grade-bash` and `brief-guard` use.
- **History rows for static trims live as a table in `docs/benchmarks.md`** (PR #589):
  `benchmarks/history.jsonl` holds replay ratios and cannot take a static row.
- **Trigger phrases are frozen by test** rather than trusting token count alone (PR #589).

### Do not implement

Not recorded in the issue or pull request.

## Tasks

- [x] Trim the output style (change 2; AC: 1) — PR #589
- [x] Trim the added skill and agent descriptions, with a trigger-phrase test (change 3; AC: 1, 3) — PR #589
- [x] Record the static before-and-after rows in `docs/benchmarks.md` (AC: 1) — PR #589
- [ ] Run the deferred-rule-text spike, then build change 1 only if it passes (AC: 1)
- [ ] Measure the live prefix from a paired bare-arm run (AC: 2)

## Dev notes

### Bound decisions and requirements

- NFR-9, Standing-context budget: always-loaded context within 200 lines and 4,202 tokens; the PRD
  names this issue's deferral spike as what decides which rules can move behind their hooks.
- FR-55, Static context budget in CI: the static estimate and the separately reported live prefix.
- AD-7, What a hook may and may not do: change 1 would rely on hook context notices.
  [ASSUMPTION: the fit is to change 1 only; the spike, not the spine, decides whether it holds.]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#NFR-9]

### Files to touch

From PR #589: `primitives/presentation/scannable.md`; `primitives/skills/*/SKILL.md`;
`primitives/roles/*.md` and the regenerated `claude/agents/*.md`; one comment in
`policy/hooks/rule-detectors.py`; `docs/benchmarks.md`; `docs/spikes/`; `CHANGELOG.md`.

### Testing

`tests/test_description_trigger_phrases.py` (PR #589) fails when a trigger phrase is dropped; the PR
records proving it by deleting three phrases from a skill.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/430]
- [Source: https://github.com/JakeSelby/agent-harness/issues/588]
- [Source: https://github.com/JakeSelby/agent-harness/pull/589]
- [Source: https://github.com/JakeSelby/agent-harness/pull/474]
- [Source: docs/benchmarks.md#Recorded trims]
- [Source: docs/spikes/2026-09-22-deferred-rule-text.md]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Partly delivered. PR #589 (merged 2026-09-23, closing #588) took the style from 1,528 to 862 tokens,
the listings from 2,326 to 1,980, and the static always-loaded total from 7,524 to 6,519 estimated
tokens. The PR states its shortfall against this issue's targets: the style is 667 tokens lighter
rather than 900, the descriptions 346 rather than 750. Still open: change 1's spike, and the measured
live prefix, which needs a paired bare-arm run rather than a single session's cold prefix. A 2026-09-23
comment records moving the issue to v0.14.0 so v0.13.0 could close.

### File list

PR #589: `CHANGELOG.md`, `_bmad-output/implementation-artifacts/AH-S205.md`,
`_bmad-output/issue-map.json`, ten `claude/agents/*.md`, `docs/benchmarks.md`,
`docs/spikes/2026-09-22-deferred-rule-text.md`, `docs/spikes/README.md`,
`policy/hooks/rule-detectors.py`, `primitives/presentation/scannable.md`, ten
`primitives/roles/*.md`, thirteen `primitives/skills/*/SKILL.md`,
`tests/test_description_trigger_phrases.py`.

## Review findings

PR #589's "Notes for review" record: the shortfall against targets above; that the client's ranking
over descriptions being unchanged is unproven, since only phrase presence is deterministic; and that
the live prefix figure is unmeasured.

## Change log

- 2026-09-23: written from the issue and pull-request record.
