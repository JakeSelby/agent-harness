---
bmad_id: "AH-S037"
type: "story"
title: "Make delegation topology configurable without weakening role confinement"
lifecycle: "active"
provenance: "reconstructed"
github_issue: 120
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/120"
parent_bmad_id: "AH-E002"
parent_github_issue: 116
updated: "2026-09-23"
---

# AH-S037 — Make delegation topology configurable without weakening role confinement

<!-- bmad-sync:begin -->
- **GitHub issue:** [#120](https://github.com/JakeSelby/agent-harness/issues/120)
- **Primary parent:** [AH-E002](https://github.com/JakeSelby/agent-harness/issues/116)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This file reconstructs planning metadata from the existing GitHub record. It does not imply that a BMad artifact existed when the original work was performed.
<!-- bmad-sync:end -->

## Story

As a developer who runs several agents, I want write topology and delegation depth to be selectable separately from the model-tier stance, so that I can allow isolated parallel writers without weakening role confinement.

## Context and value

The issue says fixed rules prohibit parallel writers and all redelegation regardless of isolation, and that model selection, permission to delegate, write topology and review independence are four separate decisions ([#120](https://github.com/JakeSelby/agent-harness/issues/120)). The epic's 2026-09-21 assessment rated it Not delivered: `delegation/` carries launch and model variants only, and "writes stay single-threaded" is fixed ([#116 comment](https://github.com/JakeSelby/agent-harness/issues/116)).

On `main` today the `delegation` stance still has only `off`, `session-model` and `tiered` [Source: primitives/stances/delegation/], and `primitives/rules/delegation.md` still says "Writes stay single-threaded (tool list, not prompt); no subagent re-delegates its assignment." The issue's problem statement is partly out of date: the builder role now expects a sibling builder in another worktree of the same repository, and carries `delegation: none` [Source: primitives/roles/builder.md]. [ASSUMPTION: the rule now reads as single-threaded per agent rather than a ban on parallel builders in separate worktrees; no document states that reading.]

The issue carries the v0.15.0 milestone.

## Acceptance criteria

1. **Given** parallel writers, **when** they are launched, **then** each has its own worktree and declared ownership, and parallel writes into one shared checkout are rejected.
2. **Given** a delegation depth bound, **when** a child requests a nested spawn, **then** the depth holds, `off` still prevents unsolicited delegation, and a child's authority never exceeds its parent's or its role's.
3. **Given** the isolated role-worker design, **when** the topology is integrated with it, **then** conflict detection, missing isolation, nested requests and native capability gaps are each tested.

## Design

### Approach

The `delegation` stance keeps launch and model preference. Two settings are added: `delegation_writes` with `serial` or `isolated-worktrees`, and `max_delegation_depth`, default 1, meaning the root may spawn and children may not. Constrained roles keep `delegation: none`. An unsupported topology is rejected instead of silently relaxing authority ([#120](https://github.com/JakeSelby/agent-harness/issues/120)).

The plan adds that constrained workers remain unable to redelegate, and that any change to the worker contract needs its own confinement evidence [Source: docs/plans/stance-completeness.md#Step 3 — Existing opinions].

### Decisions and alternatives

- `serial` is the default write topology [Source: docs/plans/stance-completeness.md#Step 3 — Existing opinions].
- Rejecting an unsupported topology was chosen over silently relaxing it ([#120](https://github.com/JakeSelby/agent-harness/issues/120)).

### Do not implement

- Do not let constrained roles delegate.
- Do not widen a child's authority beyond its parent or its role.
- Do not sync an unmerged candidate into the live installation.

## Tasks

- [ ] Add `delegation_writes` and `max_delegation_depth` to the resolved policy (AC: 1, 2)
- [ ] Reject shared-checkout parallel writes and unsupported topologies (AC: 1); declared ownership is carried by #542
- [ ] Bound depth and cap child authority at the parent and role (AC: 2)
- [ ] Test conflict detection, missing isolation, nested requests and native gaps against the role-worker design (AC: 3)

## Dev notes

### Bound decisions and requirements

[ASSUMPTION: the issue was filed on 2026-09-18, before the 2026-09-23 PRD and spine, and names none of the requirements or decisions below; each binding and its gloss reads a later rule onto it.]

- FR-44: A worktree per agent — the worktree tooling criterion 1 relies on.
- AD-8: Constrained roles run only as isolated role workers — constrained roles keep their confinement.
- AD-9: Checks bind subagents; decisions flow up — a check that binds a session binds its subagents. [ASSUMPTION: read onto criterion 2; AD-9 says nothing about capping a child's authority.]
- AD-16: Parallel writers coordinate through shared, recorded state — parallel writers declare their paths. #542, the write-intent ledger, states that it realises this story's first criterion ("parallel writers require distinct worktrees and declared ownership"), and that this story keeps depth and topology ([#542](https://github.com/JakeSelby/agent-harness/issues/542)).

### Files to touch

Not recorded in the issue or pull request. The fixed text lives in `primitives/rules/delegation.md`.

### Testing

Not yet written. The criteria name conflict detection, missing isolation, nested requests and native capability gaps.

### References

- [#120](https://github.com/JakeSelby/agent-harness/issues/120), [#542](https://github.com/JakeSelby/agent-harness/issues/542), [#116](https://github.com/JakeSelby/agent-harness/issues/116), [#117](https://github.com/JakeSelby/agent-harness/issues/117) (dependency)
- [Source: docs/plans/stance-completeness.md#Step 3 — Existing opinions]
- [Source: primitives/rules/delegation.md]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-8: Constrained roles run only as isolated role workers]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Not started. PR #133 carried an earlier implementation and was closed unmerged ([#116 comment](https://github.com/JakeSelby/agent-harness/issues/116)).

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
- 2026-09-23: corrected after sample review: bindings, current behaviour and credits checked against the 2026-09-23 spine, the code and the record.
