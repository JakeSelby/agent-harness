---
bmad_id: "AH-B110"
type: "bug"
title: "Policy-file guard gates commands that only mention a policy path"
lifecycle: "active"
provenance: "authored"
github_issue: 898
github_issue_url: "https://github.com/JakeSelby/model-citizen/issues/898"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-26"
---

# AH-B110 — Policy-file guard gates commands that only mention a policy path

<!-- bmad-sync:begin -->
- **GitHub issue:** [#898](https://github.com/JakeSelby/model-citizen/issues/898)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Reproduction

With a decision provider configured (`governance.provider: local`), run any graded command whose
text names a policy path only as data, for example `gh issue create --body-file - <<'EOF'` with a
body naming `.agent-harness/governance.json`. The hook answers `ask` with "Governance: this
changes the governance policy file agent-harness/governance.json, which is level 1", though
nothing writes the file. Observed on `main` before this fix; the regression test reproduces it.

## Root cause

`_policy_hits` in `policy/hooks/grade-bash.py` first judged the paths the segment walk found
written, and when none was a policy file it fell back to searching the whole command text with
`POLICY_RE` and `CONFIG_RE`. That text includes quoted arguments and here-document bodies, so any
mention matched. The fallback exists to catch writes the walk cannot see: code run by a shell or
an interpreter, through `eval`, `xargs` or `find -exec`, or under a leading assignment, and lines
that do not decompose.

## Acceptance criteria

1. **Given** a configured provider, **when** every command on a line is a data command (`gh`,
   `git`, `echo`, `printf`, `cat`, `grep`, `jq` and the like) and a policy path appears only in a
   quoted argument or a here-document body, **then** the policy-file guard does not gate it and
   the command is put to the provider as usual.
2. **Given** a configured provider, **when** a command writes a policy file through a redirect,
   `tee`, `sed -i`, `perl -i`, a `cp` or `mv` operand, or an unresolved operand ending in
   `governance.json` or `config.json`, **then** it is asked about as a level-1 action.
3. **Given** a configured provider, **when** a line that runs code the walk cannot see into (a
   shell or interpreter anywhere, including as a pipeline consumer or reading a here-document,
   `eval`, `source`, `xargs`, `env`, `find -exec`, a leading assignment, a command or process
   substitution) or a line that does not decompose names a policy path, **then** it is level 1.

## Design

- Fail closed: `_policy_hits` searches the whole text, here-document bodies included, for a
  policy path by name unless the walk decomposed the line and `data_only` holds for it.
- `data_only` holds only when every simple command is run by its bare name from
  `DATA_COMMANDS`, with no leading assignment and no command or process substitution. Those
  commands run no code from their arguments or input and write only through the redirect, `tee`,
  `cp` and `mv` targets the walk already checks.
- Rejected, after two review rounds: recognising writes inside code case by case (inline code
  flags, interpreter arguments, shell here-documents). Each version missed a way in, such as an
  option with a separate value before `-c`, an assignment that carries the path, or a script piped
  into `sh`.
- Not changed: `grade_tokens`, redirect-only segment grading (#899), the provider decision loop,
  the `harness config set governance` text check and `govern_file`.
- Default taken: an interpreter or shell reading a here-document or pipeline runs code, so a
  policy path in its input stays gated, including the issue's python note example, because
  treating that input as data let policy writes through.

## Regression test

`tests/test_policy_mentions.py`, `PolicyMentions.test_regression_a_mention_in_data_is_not_gated`,
fails on the unfixed code (the `gh issue create` here-document is gated at level 1) and passes
after it. `test_a_real_write_is_still_gated` and the `DataOnly` cases pin criteria 2 and 3,
including both review triggers.

## Tasks

- [x] AC 1: exempt a line from the text-wide search only when it is `data_only`.
- [x] AC 2: keep redirect, `tee`, in-place and operand writes on the walk's written paths.
- [x] AC 3: keep the text-wide search for every line that may run code the walk cannot see.
- [x] Tests in `tests/test_policy_mentions.py`; changelog fragment `changelog.d/898.fixed.md`.

## Dev notes

- Files: `policy/hooks/grade-bash.py` (`DATA_COMMANDS`, `data_only`, `_policy_hits`, `govern`),
  `tests/test_policy_mentions.py`, `changelog.d/898.fixed.md`.
- The new tests live in their own module and reuse `Home` from the governance-binding tests, so
  they merge cleanly beside other work on that module.
  [Source: tests/test_governance_binding.py#PolicyWrites]
- The guard and its level-1 rule come from #884. [Source: policy/hooks/grade-bash.py#govern]
- Follow-up, not in this fix: the `harness config set governance` check still searches the whole
  command text, so a here-document body that quotes that command is gated the same way.

## Dev agent record

- Model: Claude Opus 5.5, as a builder in a managed worktree.
- Completion: the fix and tests are in; the governance-binding suite passes unchanged, including
  its `python3 -c` write case. The regression test was shown to fail against the unfixed tree.
- Review round 1: the first version read inline code only up to the first non-option argument,
  so `python3 -W ignore -c CODE`, `python3 -X dev -c CODE` and `node -r mod -e CODE` hid a policy
  write. The second judged every interpreter argument.
- Review round 2: a leading assignment carrying the path to `python3 -c`, and a script piped into
  `sh`, still went through. The case-by-case recognition was replaced by the fail-closed
  `data_only` exemption, which also reverses the earlier interpreter here-document default.
- File list: `policy/hooks/grade-bash.py`, `tests/test_policy_mentions.py`,
  `changelog.d/898.fixed.md`, `_bmad-output/implementation-artifacts/AH-B110.md`,
  `_bmad-output/issue-map.json`, `_bmad-output/implementation-artifacts/sprint-status.yaml`.

## Review findings

- Review (major, security): an interpreter option with a separate value hid the code option
  after it, so a policy write through `python3 -W ignore -c` was not gated. Fixed in round 1, and
  covered by the fail-closed search since round 2.
- Review (major, security): `P=<policy path> python3 -c ...` was not gated because the leading
  assignment was stripped before the interpreter was scanned. Fixed: a line with a leading
  assignment is never `data_only`.
- Review (major, security): `printf '... > <policy path>' | sh` was not gated because only
  here-documents were read as shell input. Fixed: a shell anywhere on the line keeps the search.

## Change log

- 2026-09-26: written with the fix for #898.
- 2026-09-26: interpreter arguments judged whatever their position, after review of #924.
- 2026-09-26: replaced case-by-case recognition with the fail-closed `data_only` exemption and
  reversed the interpreter here-document default, after the second review of #924.
