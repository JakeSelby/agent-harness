---
bmad_id: "AH-S213"
type: "story"
title: "harness usage --rules: list every rule as measured, dark or unmeasured, and load declarative detectors"
lifecycle: "active"
provenance: "authored"
github_issue: 647
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/647"
parent_bmad_id: "AH-E003"
parent_github_issue: 135
updated: "2026-09-23"
---

# AH-S213 — harness usage --rules: list every rule as measured, dark or unmeasured, and load declarative detectors

<!-- bmad-sync:begin -->
- **GitHub issue:** [#647](https://github.com/JakeSelby/agent-harness/issues/647)
- **Primary parent:** [AH-E003](https://github.com/JakeSelby/agent-harness/issues/135)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer measuring my instruction rules inside the harness, I want `harness usage --rules` to list
every rule in my loaded instruction surface as measured, dark or unmeasured, with the share measured,
and to load my declarative detectors, so that I get the rule-level view standalone `ruleprobe` gives
without leaving the harness.

## Context and value

Today `harness usage --rules` reports hits keyed by detector. The rule-level report (each rule measured,
dark or unmeasured with its reason, the share of rules measured, and declarative detectors) exists only
in standalone `ruleprobe` run with `--rules`. The PRD's first-contact path (UJ-3) needs that view inside
the harness too. FR-20 and FR-67 both list this as planned and unscheduled; the re-derived epic map
places it under this epic, whose coverage includes both.
[Source: https://github.com/JakeSelby/agent-harness/issues/647]

## Acceptance criteria

1. **Given** a loaded instruction surface, **when** `harness usage --rules` runs, **then** it lists every
   rule, including rules imported into external roots, as measured, dark or unmeasured.
2. **Given** an unmeasured rule, **when** it is listed, **then** its reason is shown.
3. **Given** the report, **when** it is printed, **then** it states the share of rules measured.
4. **Given** a declarative detector in `.ruleprobe/detectors.yaml`, **when** `harness usage --rules`
   runs, **then** the detector is loaded without a code change and its rule counts as measured.
5. **Given** a detector with zero hits over the reporting window, **when** the report runs, **then** it
   is listed, not omitted (FR-20).

## Design

### Approach

Bring the rule-level classification the vendored `ruleprobe` engine already performs under `--rules`
into `harness usage --rules`, and load `.ruleprobe/detectors.yaml` alongside the built-in detectors.
[ASSUMPTION: reuse of the vendored engine's classification rather than a reimplementation; the issue
names the behaviour, not the mechanism.]

### Decisions and alternatives

Not recorded in the issue or pull request.

### Do not implement

- Grouping by mode and switch state, which FR-20 plans separately for v0.14.0.
- Proposing a detector from a rule's prose, which FR-67 lists as a separate planned item.

## Tasks

- [ ] Classify every loaded rule, including external-root imports (AC: 1)
- [ ] Reasons for unmeasured rules (AC: 2)
- [ ] Share-measured line (AC: 3)
- [ ] Load `.ruleprobe/detectors.yaml` (AC: 4)
- [ ] Keep zero-hit detectors listed (AC: 5)

## Dev notes

### Bound decisions and requirements

- FR-20 (per-rule hit rate), FR-67 (detectors for a developer's own rules), FR-21 (standalone
  instrument, whose `--rules` behaviour is the reference), FR-19 (detector or reason for every rule).
- AD-1: one detector registry shared by the harness and the vendored engine.

### Files to touch

Not recorded in the issue. The command lives in `bin/harness`.

### Testing

Not recorded in the issue.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/647]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-20]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-67]

## Dev agent record

### Agent model used

Not started.

### Completion notes

Not started.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
