---
bmad_id: "AH-S045"
type: "story"
title: "Add local telemetry controls and retention"
lifecycle: "active"
provenance: "reconstructed"
github_issue: 128
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/128"
parent_bmad_id: "AH-E002"
parent_github_issue: 116
updated: "2026-09-23"
---

# AH-S045 — Add local telemetry controls and retention

<!-- bmad-sync:begin -->
- **GitHub issue:** [#128](https://github.com/JakeSelby/agent-harness/issues/128)
- **Primary parent:** [AH-E002](https://github.com/JakeSelby/agent-harness/issues/116)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This file reconstructs planning metadata from the existing GitHub record. It does not imply that a BMad artifact existed when the original work was performed.
<!-- bmad-sync:end -->

## Story

As a developer who treats local collection as a preference, I want switches to disable usage collection, drop repository and branch metadata, and expire old records, so that the harness records only what I agree to keep, for as long as I choose.

## Context and value

Local usage collection and repository and branch metadata are always installed, and local-only storage is still a preference ([#128](https://github.com/JakeSelby/agent-harness/issues/128)). The epic's 2026-09-21 assessment rated the story Partial: collection and metadata exist, with no enable or retention keys and no pruning code ([#116 comment](https://github.com/JakeSelby/agent-harness/issues/116)).

On `main` today:
- The `telemetry` block accepts `export`, `endpoint`, `headers_env`, `headers_file`, `labels`, `native`, `decisions`, `completion_claim` and `allow_sample_rate`, and rejects any other key [Source: policy/hooks/telemetry.py#KNOWN_KEYS]. None of the four keys this story proposes exists.
- `telemetry.decisions: false` already stops the decision log from writing anything [Source: policy/hooks/decisions.py#enabled]. That covers the decision log only, not the usage ledger.
- The usage ledger records `repo` and `branch` for each row [Source: policy/hooks/usage-log.py].
- No retention or pruning code was found in `usage-log.py` or `decisions.py`.

The issue carries the v0.15.0 milestone.

## Acceptance criteria

1. **Given** collection disabled, **when** a session runs, **then** no worker launches and no record is written, and a rescan cannot bypass the setting.
2. **Given** a metadata opt-out, **when** new records are written, **then** they omit the selected fields, and deletion of existing records is documented as a separate operation.
3. **Given** a retention period, **when** pruning runs, **then** it removes only harness-owned records by a defined UTC timestamp, and concurrency, invalid configuration, expiry boundaries and disabled hooks are tested.

## Design

### Approach

The issue proposes `observability.enabled`, `retention_days`, `include_repo` and `include_branch`. Collection and metadata stay on by default, and `retention_days=0` means no automatic expiry, for migration compatibility. The controls apply to both native collectors, detached workers, rescans and reports. The issue says commands and message bodies are never collected ([#128](https://github.com/JakeSelby/agent-harness/issues/128)); AH-D009 narrows that rule to the collectors this story names, below.

The plan adds that opting out of metadata affects new records only, that purging existing records is a separate explicit operation, and that disabled telemetry must stop detached workers and rescans as well as hook installation [Source: docs/plans/stance-completeness.md#Step 5 — Settings and runtime behavior].

### Decisions and alternatives

- `retention_days=0` preserves current stored history by default ([#128](https://github.com/JakeSelby/agent-harness/issues/128)).
- The issue names the block `observability`; the shipped settings live under `telemetry`, and AD-11 says export settings "live in the `telemetry` block, never in a stance". [ASSUMPTION: the four keys would land in `telemetry` too; no record decides it.]

### Resolved contradiction: message bodies

This issue says "Do not collect commands or message bodies." #543, the session archive, proposes `telemetry.sessions_bodies`, which stores message and tool-result text when on, and applies this story's `retention_days` to it ([#543](https://github.com/JakeSelby/agent-harness/issues/543)).

Decided by the owner on 2026-09-26, AH-D009 option 2, opt-in only ([#685](https://github.com/JakeSelby/agent-harness/issues/685)): the rule here is narrowed to the collectors this story names. The usage ledger's native collectors, their detached workers and rescans never collect command text or message bodies. The session archive is outside this rule: it may store bodies only under `telemetry.sessions_bodies`, off by default, on only by the developer's explicit opt-in, redacted for secret patterns at write. This story's `retention_days` applies to those bodies; #543's build applies it to the archive.

### Do not implement

- Do not collect command text or message bodies in the usage ledger's native collectors, workers or rescans (AH-D009).
- Do not delete existing records as a side effect of a metadata opt-out.
- Do not sync an unmerged candidate into the live installation.

## Tasks

- [ ] Add the enable switch and stop workers, rescans and hooks when it is off (AC: 1)
- [ ] Add the `include_repo` and `include_branch` opt-outs for new records (AC: 2)
- [ ] Add UTC-based retention pruning of harness-owned records (AC: 3)
- [ ] Test concurrency, invalid configuration, expiry boundaries and disabled hooks (AC: 3)

## Dev notes

### Bound decisions and requirements

[ASSUMPTION: the issue was filed on 2026-09-18, before the 2026-09-23 PRD and spine, and names none of the requirements or decisions below; each binding and its gloss reads a later rule onto it.]

- FR-11: Local-first observability — local measurement without remote telemetry.
- FR-23: Cross-runtime usage ledger — the records the controls govern.
- AD-11: Local ledgers are the system of record, and grow compatibly — an upgrade must leave old rows readable.
- NFR-12 Local-first data — the usage ledger and decision log stay on the machine unless export is configured.

### Files to touch

Not recorded in the issue or pull request. The consumers are `policy/hooks/telemetry.py`, `policy/hooks/usage-log.py` and `policy/hooks/decisions.py`.

### Testing

Not yet written.

### References

- [#128](https://github.com/JakeSelby/agent-harness/issues/128), [#543](https://github.com/JakeSelby/agent-harness/issues/543), [#685](https://github.com/JakeSelby/agent-harness/issues/685) (AH-D009), [#116](https://github.com/JakeSelby/agent-harness/issues/116), [#117](https://github.com/JakeSelby/agent-harness/issues/117) (dependency)
- [Source: docs/plans/stance-completeness.md#Step 5 — Settings and runtime behavior]
- [Source: policy/hooks/telemetry.py#KNOWN_KEYS]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-11: Local ledgers are the system of record, and grow compatibly]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Not started as a story. The decision-log switch that exists came from other work. PR #133 carried an earlier implementation, including retention and disable coverage, and was closed unmerged ([#116 comment](https://github.com/JakeSelby/agent-harness/issues/116); [#130 comment](https://github.com/JakeSelby/agent-harness/issues/130)).

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
- 2026-09-23: corrected after sample review: bindings, current behaviour and credits checked against the 2026-09-23 spine, the code and the record.
- 2026-09-26: AH-D009 decided (#685): the no-bodies rule narrowed to the usage ledger's collectors; the session archive's opt-in bodies fall under this story's `retention_days`.
