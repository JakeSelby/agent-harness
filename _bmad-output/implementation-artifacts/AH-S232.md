---
bmad_id: "AH-S232"
type: "story"
title: "Module manifest: claims, surface, instruments, slot, dependencies and conflicts"
lifecycle: "active"
provenance: "authored"
github_issue: 788
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/788"
parent_bmad_id: "AH-E011"
parent_github_issue: 552
updated: "2026-09-24"
---

# AH-S232 — Module manifest: claims, surface, instruments, slot, dependencies and conflicts

<!-- bmad-sync:begin -->
- **GitHub issue:** [#788](https://github.com/JakeSelby/agent-harness/issues/788)
- **Primary parent:** [AH-E011](https://github.com/JakeSelby/agent-harness/issues/552)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a harness maintainer, I want every module to declare its claims, surface, instruments, slot, dependencies and conflicts in a manifest the resolver enforces, so that a flipped switch can be attributed and scored rather than only toggled.

## Context and value

#554 gives every module a switch in one selection document. A switch alone does not say what the module is for, what it costs to carry, how its effect is measured or what it collides with, so a flipped switch cannot be attributed or scored. AD-22 makes each module declare six manifest fields, resolved by `posture.py` as the one resolver: claims, surface, instruments, slot, dependencies and conflicts.

Depends on #554. Traces to FR-15 and AD-22.

## Acceptance criteria

1. Given a module in the selection document, when the selection resolves, then its manifest carries all six fields, and a missing field fails resolution with the module and the field named.
2. Given two selected modules that claim one slot, when the selection resolves, then it fails unless one declares that it cedes the slot.
3. Given a selected module whose dependency is not selected, or that conflicts with another selected module, when the selection resolves, then it fails and names both.
4. Given a module with no instrument, when a report or scorecard lists it, then it shows as unmeasured, never as having no effect.
5. Given the unit tests, then they cover each field for every switch kind, and the slot, dependency and conflict refusals.

## Design

### Approach

<!-- fill: the chosen approach and the shape of the change. -->

### Decisions and alternatives

<!-- fill: each decision taken, the alternatives rejected, and why. -->

### Do not implement

<!-- fill: what is out of scope, so the builder does not widen the change. -->

## Tasks

<!-- fill: a checklist mapped to the acceptance criteria, for example
- [ ] Task (AC: 1)
  - [ ] Subtask -->

## Dev notes

### Bound decisions and requirements

<!-- fill: the architecture decisions (AD) and requirements (FR) this story is bound by. -->

### Files to touch

<!-- fill: files to add or change, one line each. -->

### Testing

<!-- fill: the tests that prove each acceptance criterion, and where they live. -->

### References

<!-- fill: [Source: path#section] for every technical claim above. -->

## Dev agent record

### Agent model used

<!-- fill: the model that implemented the story. -->

### Completion notes

<!-- fill: what was done, and anything that differed from the design. -->

### File list

<!-- fill: every file added or changed. -->

## Review findings

<!-- fill: each review finding and how it was resolved. -->

## Change log

<!-- fill: dated entries for material changes to this file after it was first written. -->
