---
bmad_id: "AH-B071"
type: "bug"
title: "The real-corpus upgrade test fails once the last amended stub is upgraded"
lifecycle: "completed"
provenance: "authored"
github_issue: 666
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/666"
parent_bmad_id: "AH-E012"
parent_github_issue: 616
updated: "2026-09-23"
---

# AH-B071 — The real-corpus upgrade test fails once the last amended stub is upgraded

<!-- bmad-sync:begin -->
- **GitHub issue:** [#666](https://github.com/JakeSelby/agent-harness/issues/666)
- **Primary parent:** [AH-E012](https://github.com/JakeSelby/agent-harness/issues/616)
- **State:** completed

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Reproduction

1. On `main` at the time this was filed, 340 story files are still legacy stubs. Two of them carry
   `## Amendment` sections: AH-C007 (#193) and AH-B008 (#203), both under AH-E004 (#189).
2. Upgrade both files: `python3 scripts/bmad_issue_sync.py upgrade --id AH-C007`, then the same for
   AH-B008. The enrichment batch for #189 (#657) does exactly this.
3. Run `python3 -m unittest tests.test_bmad_story_files.RealCorpusTests`.

Observed: `test_each_conversion_carries_the_original_tail_byte_for_byte` fails on
`assertGreaterEqual(carried_any, 2)`, because no legacy file with a tail is left. Once every stub is
typed, the loop skips every item and asserts nothing about tail carrying.

## Root cause

The test drew its inputs from the real corpus in its current state. It skipped every file already in
the typed format and counted carried tails only among the files that were left. Its reach therefore
shrank with each upgrade, and its floor of two carried tails depended on two specific files staying
legacy. [Source: tests/test_bmad_story_files.py#RealCorpusTests]

## Acceptance criteria

1. **Given** AH-C007 and AH-B008 upgraded, **when** `RealCorpusTests` runs, **then** it passes.
2. **Given** an `upgrade_text` that drops or alters a carried tail, **when** the test runs, **then**
   it fails for every item.
3. **Given** any manifest, **when** the test runs, **then** it has checked a conversion for every
   item, whatever the format of that item's file.

## Design

- For every manifest item, build the item's own legacy stub with `render_legacy_stub(item)` and append
  five tail shapes: a blank-line amendment, one with no blank line and no final newline, one with mixed
  line endings, a whole-file CRLF copy, and a copy behind a BOM. Convert each with `upgrade_text`.
- The expected output is computed independently: the file's BOM, then the typed skeleton in the file's
  own line ending, a separating newline when the tail does not start with one, and the tail byte for
  byte.
- A real file that is still a legacy stub is also converted and checked against its real tail. This
  keeps the original coverage of AH-C007 and AH-B008 while they last.
- The test counts the items it checked and asserts that the count equals the number of items, so it
  can never pass empty.
- **Rejected alternative:** skipping the test when no legacy stub remains. That leaves the real
  corpus's conversion untested, and the gate expects no skipped tests.
- **Must not change:** `upgrade_text`, and the other two real-corpus tests.

## Regression test

`RealCorpusTests.test_each_conversion_carries_the_original_tail_byte_for_byte`.
- **Before the fix:** it fails once AH-C007 and AH-B008 are upgraded.
- **After the fix:** it passes in that state, and passes on the current corpus.
- **Bite check:** it reports 385 subtest failures, one per item, for each of four lossy variants of
  `upgrade_text`:
  - one that normalises CRLF;
  - one that strips trailing whitespace;
  - one that drops two bytes;
  - one that drops the BOM.

## Tasks

- [x] Convert each item's rendered legacy stub, with an appended amendment (AC 1, 3).
- [x] Keep the check against the real tail for files still in legacy form (AC 1).
- [x] Assert that the loop covered every item (AC 3).
- [x] Confirm the test fails on a lossy conversion (AC 2).

## Dev notes

- **Bound decisions:** AD-5 in the 2026-09-23 architecture spine. The story file is the design record,
  and `upgrade` converts stubs without loss. [Source:
  _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-5]
- **Files:** `tests/test_bmad_story_files.py`.
- **Testing:** `python3 -m unittest tests.test_bmad_story_files`, and the full gate in `AGENTS.md`.
- **Context:** the same defect was found in review of the ruleprobe port, and fixed there the same
  way in JakeSelby/ruleprobe#9.

## Dev agent record

Fixed in the pull request that closes #666. The only file changed is `tests/test_bmad_story_files.py`.
The test was run with AH-C007 and AH-B008 upgraded in a scratch state, then restored.

## Review findings

A fresh-context review of the first version found the following. All are resolved:
1. **Should-fix:** every synthetic tail had the same shape, starting with a newline, LF only and one
   final newline. As a result, CRLF normalisation, stripping trailing whitespace, and a skipped
   separating newline all went undetected. *Resolved:* five tail shapes per item.
2. **Should-fix:** the expected text was built in LF. `upgrade_text` keeps a file's own CRLF, so a CRLF
   checkout would fail when nothing was wrong. *Resolved:* the expected skeleton uses the file's line
   ending.
3. **Note:** a real legacy file with a BOM would fail when nothing was wrong. *Resolved:* the BOM is
   part of the expected text, and one variant carries a BOM.
4. **Note:** an empty manifest would pass. *Resolved:* the test asserts that the manifest has items.

## Change log

- 2026-09-23: written with the fix.
- 2026-09-23: hardened after fresh-context review (review findings 1 to 4).
