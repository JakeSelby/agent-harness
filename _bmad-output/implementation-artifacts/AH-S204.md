---
bmad_id: "AH-S204"
type: "story"
title: "Copy: the layer under your agents' skills"
lifecycle: "active"
provenance: "authored"
github_issue: 561
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/561"
parent_bmad_id: "AH-E011"
parent_github_issue: 552
updated: "2026-09-23"
---

# AH-S204 — Copy: the layer under your agents' skills

<!-- bmad-sync:begin -->
- **GitHub issue:** [#561](https://github.com/JakeSelby/agent-harness/issues/561)
- **Primary parent:** [AH-E011](https://github.com/JakeSelby/agent-harness/issues/552)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a prospective user who already runs a skill library, I want the harness's public copy to say it is
the layer under that library, so that I understand what it adds that a skill library cannot claim.

## Context and value

Epic #552's outcome ends with "the copy says what the harness is under a skill library". The direction
was agreed on 2026-09-22 (issue text): lead with what a skill library cannot claim (enforcement, cost
posture, model routing, projection to every runtime); every unit is a switch and a mode is a named set of
them; superpowers installs on top and loses nothing. The issue carries the direction, not the text: a
separate copy session produces the draft, and the owner approves it before anything is applied.
[Source: https://github.com/JakeSelby/agent-harness/issues/561]

## Acceptance criteria

1. **Given** a copy session, **when** it runs, **then** it produces the hero, the description, the GitHub
   description and one new capability group as a draft, approved before apply.
2. **Given** the applied copy, **when** `scripts/sync_about.py --check` runs, **then** it names only the
   description and topics; **and** `.github/scripts/check_landing_copy.py` passes.
3. **Given** `product.json`, **when** the README groups regenerate, **then** there is no drift, and the
   site smoke test passes at the release pin.
4. **Given** the copy, **when** it is checked, **then** no figure appears that is not in
   `benchmarks/history.jsonl`.
5. **Given** the topics, **when** they are applied, **then** they include `superpowers` and
   `agent-skills`.

## Design

### Approach

Draft in a separate copy session; apply the approved text through `product.json`, the README, the GitHub
description and the site pin. `product.json` is the one source of landing, README and About copy.

### Decisions and alternatives

- **Direction, not text, lives in the issue;** the words are drafted and approved separately.
- **Only the figure line waits on #560;** the copy session can run at once.
- **Figures only from `benchmarks/history.jsonl`.** That file does not exist on `main`, so today no figure
  can appear. The only uncontaminated live result (1.052 of bare) failed the publish bar; no saving
  may be stated.

### Do not implement

- Applying any copy before the draft is approved.
- A cost or saving line before a live result passes the publish bar.

## Tasks

- [ ] Run the copy session and produce the draft (AC: 1)
- [ ] Apply the approved copy to `product.json` and regenerate the README groups (AC: 2, 3)
- [ ] Add the `superpowers` topic (AC: 5)
- [ ] Update the GitHub description and topics through `sync_about.py` after approval (AC: 2)
- [ ] Check every figure against `benchmarks/history.jsonl` (AC: 4)

## Dev notes

### Bound decisions and requirements

- FR-54, Landing copy as data: all landing, README and About copy comes from `product.json`; no copy says
  "cheaper" until a same-day ratio passes the publish bar; no em dashes; credit compared projects rather
  than framing them as competition.
- AD-19, Distribution channels carry no second authority: landing, README and About copy come from
  `product.json` only.
- NFR-13, Claims discipline: no copy asserts an unmeasured saving.
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-54]

### Current state checked

`product.json` topics on `main` already include `agent-skills`; `superpowers` is not present.

### Files to touch

`product.json`, `README.md`; the GitHub description and topics are applied through
`scripts/sync_about.py`.

### Testing

`.github/scripts/check_landing_copy.py` and `tests/test_landing_copy.py`; `sync_about.py --check`; the
site smoke test at the release pin.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/561]
- [Source: https://github.com/JakeSelby/agent-harness/issues/552]
- [Source: product.json]
- [Source: scripts/sync_about.py]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Not started.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
