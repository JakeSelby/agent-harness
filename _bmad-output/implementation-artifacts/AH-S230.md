---
bmad_id: "AH-S230"
type: "story"
title: "Replay: judge a one-policy arm pair on cost per passed task, decision calls included"
lifecycle: "active"
provenance: "authored"
github_issue: 754
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/754"
parent_bmad_id: "AH-E003"
parent_github_issue: 135
updated: "2026-09-23"
---

# AH-S230 — Replay: judge a one-policy arm pair on cost per passed task, decision calls included

<!-- bmad-sync:begin -->
- **GitHub issue:** [#754](https://github.com/JakeSelby/agent-harness/issues/754)
- **Primary parent:** [AH-E003](https://github.com/JakeSelby/agent-harness/issues/135)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a harness maintainer, I want to replay two arms that differ in one policy and compare their cost
per passed task, decision calls included, so that a routing or decision-layer change is judged on what
the whole task costs rather than on agreement with today's rule.

## Context and value

The four-arm set (#559, #560) reports cost per passed task for four fixed arms. #145, the band
recommendation, can only report agreement with the current routing heuristic, which shows imitation
rather than improvement, and it has no promotion criterion. A one-policy pair, default routing against
the recommendation, is the comparison a criterion can rest on. A selector's own calls count in the arm
that makes them, since a cheaper worker bought with an expensive decision saves nothing. keel 0.2.0
([codejunkie99/keel](https://github.com/codejunkie99/keel)) names the same comparison, selector time, worker time, retries and review effort, but ships
no implementation of it.

## Acceptance criteria

1. **Given** two profiles that differ in one named policy, **when** the pair is replayed, **then** the
   parity check passes only when nothing else differs.
2. **Given** a finished pair, **when** it is summarised, **then** each arm reports passes, cost per
   passed task, wall time and re-spawns at a higher band.
3. **Given** decision-provider calls during an arm, **when** the arm is costed, **then** their ledger cost
   and latency count in that arm, and an unpriced call is named, never counted as zero.
4. **Given** a fake launcher, **when** the unit tests run, **then** they cover the parity refusal, the
   re-spawn count and the decision-call roll-up.

## Design

### Approach

Build on AH-S202's arms-as-profiles and its parity check, once #559 lands. A pair is two built
profiles plus the one policy key they may differ in, and parity refuses any other difference. The
per-arm summary adds wall time and re-spawns at a higher band, and joins the decision ledger's rows for
the arm's sessions, by session id, to add their cost and latency.

### Decisions and alternatives

- Score on cost per passed task, the four-arm set's own unit, so pairs and arms compare on one scale.
- Rejected as a criterion basis: agreement with the current heuristic (AH-S057 AC5 before its
  2026-09-23 amendment), which can only show imitation.
- Rejected: pricing an unpriced decision call at zero. The ledger names unpriced rows rather than
  zeroing them, and the pair follows that rule.

### Do not implement

- No new arms in the four-arm set, and no change to its threshold.
- No promotion of #145: the point stays in `shadow`, and this story only supplies the measurement.
- No live runs in the unit tests.

## Tasks

- [ ] Pair definition and parity refusal (AC: 1)
- [ ] Per-arm roll-up: passes, cost per passed task, wall time, higher-band re-spawns (AC: 2)
- [ ] Decision-call roll-up from the ledger by session id, unpriced calls named (AC: 3)
- [ ] Unit tests on the fake launcher (AC: 4)
- [ ] A `docs/benchmarks.md` section on pairs

## Dev notes

### Bound decisions and requirements

- AD-12, measurement invariants; AD-11, local ledgers are the system of record.
- FR-49: evidence gates each stage, and a point without a criterion stays in `shadow`.

### Files to touch

- The replay arms and per-arm summary that AH-S202 adds; not on `main` yet.
- `lib/harness_core/decisions/ledger.py`, read only, for the join.
- `docs/benchmarks.md`.
- A test module beside AH-S202's replay tests.

### Testing

Unit tests with a fake launcher and fake ledger rows: a pair that differs in two keys is refused, a
re-spawn at a higher band is counted, and decision rows join by session id with an unpriced row named.

### References

- [Source: _bmad-output/implementation-artifacts/AH-S202.md#acceptance-criteria]
- [Source: _bmad-output/implementation-artifacts/AH-S057.md#acceptance-criteria]
- [Source: lib/harness_core/decisions/ledger.py], the unpriced-row rule
- keel 0.2.0 ([codejunkie99/keel](https://github.com/codejunkie99/keel)), its proposed replay design

## Dev agent record

### Agent model used

<!-- fill: the model that implemented the story. -->

### Completion notes

<!-- fill: what was done, and anything that differed from the design. -->

### File list

<!-- fill: every file added or changed. -->

## Review findings

<!-- fill: each review finding and how it was resolved. -->

## Change log

- 2026-09-23: written, per #756.
