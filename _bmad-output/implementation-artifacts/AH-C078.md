---
bmad_id: "AH-C078"
type: "chore"
title: "Record the Model Citizen rename's sprint change and reserve its stories"
lifecycle: "active"
provenance: "authored"
github_issue: 876
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/876"
parent_bmad_id: "AH-E020"
parent_github_issue: 875
updated: "2026-09-25"
---

# AH-C078 — Record the Model Citizen rename's sprint change and reserve its stories

<!-- bmad-sync:begin -->
- **GitHub issue:** [#876](https://github.com/JakeSelby/agent-harness/issues/876)
- **Primary parent:** [AH-E020](https://github.com/JakeSelby/agent-harness/issues/875)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

Record the Model Citizen rename in the BMad corpus before any code or copy changes, and reserve the stories
that deliver it, so each delivery pull request starts from a mapped issue and a filled design. This is step 3
of the maintainer's approved rename plan of 2026-09-25, which settled that the rename ships inside the
v0.14.0 milestone.

## Acceptance criteria

1. `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-25.md` records the rename, the keep list
   and why each item stays, Decision 2 as settled with no re-scope, and the mapping of S0 to S4 to their BMad
   IDs and issues.
2. The architecture spine carries AD-24 with a status tag and a memlog entry, and the spine linter is clean.
3. The epic and its five stories are filed with their `type::*` labels on the v0.14.0 milestone, reserved in
   `_bmad-output/issue-map.json`, linked to the epic as sub-issues, and listed in `epics.md`.
4. The epic and this story are filled completely; S1 to S4 carry their context, acceptance criteria, design,
   tasks and dev notes, and leave the dev agent record to their delivery pull requests.
5. Sprint status is regenerated by the sync tool, and `python3 scripts/bmad_issue_sync.py audit` is clean.

## Tasks

- [x] File the epic #875 and S0 to S4 as #876 to #880 with `new`, complete each reservation with
  `reserve --issue`, and link #876 to #880 to #875 as sub-issues. (AC: 3)
- [x] Run `bmad-correct-course` in batch mode and write the proposal. (AC: 1)
- [x] Add AD-24 through `bmad-architecture`'s update intent, with memlog entries, and run `lint_spine.py`.
  (AC: 2)
- [x] Add AH-E020 and its stories to `epics.md`, and AH-E020 to the configuration lifecycle and release
  coverage rows. (AC: 3)
- [x] Fill the epic, this story and S1 to S4 from the plan's Step 4, Step 5 and Step 7. (AC: 4)
- [x] Regenerate sprint status and run the audit and the gate. (AC: 5)

## Dev notes

- **Source:** the approved plan, `.agent-harness/plans/2026-09-25-model-citizen-rename.md`, committed with
  this change; its Step 3, Inventory, Sequencing and Deferred sections drive this story.
- **Label mapping:** the plan's feature and docs stories file as `type::story`, its chores as `type::chore`;
  the repository has no feature or docs type label.
- **IDs:** `new` filed every issue and then failed its reservation on list lag; `reserve --issue` completed
  each. AH-S266 is held by a sibling branch, so S1 took AH-S267 with `--advance`.
- **The reviewer lenses** of `bmad-architecture`'s gate were not dispatched, because this run had no subagent
  tool; `lint_spine.py` ran clean and the pull request review stands in for them, as the memlog records.
- **No code changes,** so no new tests; the gate still runs in full.
- **After merge:** `python3 scripts/bmad_issue_sync.py apply` projects the planning blocks onto the six
  issues.

## Dev agent record

- Agent: Claude Code. Issues filed with `scripts/bmad_issue_sync.py new` and `reserve`;
  sub-issue links made through the REST API.
- Gate: `python3 bin/harness lint`, `bin/harness generate --check`,
  `python3 scripts/bmad_issue_sync.py audit` and `python3 -m unittest discover -s tests`, each read by its
  exit code.
- Files: `.agent-harness/plans/2026-09-25-model-citizen-rename.md`, `_bmad-output/issue-map.json`,
  `_bmad-output/implementation-artifacts/sprint-status.yaml`, the six story files AH-E020, AH-C078, AH-S267
  to AH-S269 and AH-C079, `_bmad-output/planning-artifacts/epics.md`,
  `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-25.md`, and the architecture spine and its
  memlog.
- The committed plan copy leaves out the approved plan's outreach and maintainer-setup detail (its step 8,
  gated items G10 to G19 and three open questions), which stays outside this repository. Steps 1 to 7 and
  every decision are as approved.

## Change log

- 2026-09-25: created with the rename's filings.
