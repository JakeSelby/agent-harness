---
bmad_id: "AH-S254"
type: "story"
title: "Price every rule, stance variant, skill and agent description individually in the static tier, and report a change's token delta in CI"
lifecycle: "active"
provenance: "authored"
github_issue: 509
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/509"
parent_bmad_id: "AH-E013"
parent_github_issue: 632
updated: "2026-09-25"
---

# AH-S254 — Price every rule, stance variant, skill and agent description individually in the static tier, and report a change's token delta in CI

<!-- bmad-sync:begin -->
- **GitHub issue:** [#509](https://github.com/JakeSelby/agent-harness/issues/509)
- **Primary parent:** [AH-E013](https://github.com/JakeSelby/agent-harness/issues/632)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a harness maintainer, I want the static tier to price every counted file on its own and to name each file a change moved, so that a rule edit reads as that file's token and dollar delta rather than a moved total.

## Context and value

The static tier (FR-55) records one total for the always-loaded layer and the listings, so a rule change
reads as "the total moved" and CI can refuse growth only in aggregate. The cost side of a rule edit is
fully determined by its text and needs no model, which makes a per-file figure the cheapest tier of
testing rule permutations at cents per change.

Traces to FR-55. Parent: AH-E013.

## Acceptance criteria

1. **Given** a checkout, **when** `scripts/cost_bench.py static` runs, **then** its output carries a `files`
   map with each always-loaded rule, selected stance variant, output style and listed agent, skill and
   command description: its group, characters, estimated tokens, and USD per session start and per later
   turn for every model the totals are priced on.
2. **Given** that map, **when** it is summed, **then** characters equal the total exactly and tokens and
   dollars equal it within rounding, for this repository's committed figure too.
3. **Given** a seeded 200-token growth in one rule, **when** `static --check` runs, **then** its output names
   that file and its delta in tokens and dollars; **and when** `benchmarks/allow.json` records the new total,
   **then** the check passes and still names the file.
4. **Given** a file added or removed since the committed figure, **when** the check runs, **then** the line
   says so; **given** nothing moved, **then** it prints nothing.
5. **Given** a committed figure written before this change, **when** the check runs, **then** it says the
   baseline has no per-file figures and still gates the total.
6. `python3 bin/harness lint` is clean and the suite is green under `python3` and `/usr/bin/python3`.

## Design

### Approach

- `measure` builds a `files` map from the same rows `total` sums, keyed by repository path, and prices
  each file with the same `price` helper, now given the model table once rather than rereading it.
- `file_deltas` compares that map with the committed `benchmarks/static.json` and returns one line per
  file whose estimated tokens changed, largest move first, marking new and removed files. `--check`
  prints those lines to stdout before the growth gate, then the gate runs unchanged on the total.
- `schema_version` becomes 2, since the shape gained a map; `scopes` gains a `files` entry so the file
  alone says what the map covers.
- The committed figure is regenerated from the `v0.13.1` tag with the new code, so its totals are the
  release's own and only the `files` map and scope line are new.

### Decisions and alternatives

- **The gate stays on the total.** A per-file gate would refuse every rule edit until an allow entry
  named it; the issue asks for the delta to be visible, and the 5% budget stays the one refusal.
- **Deltas are priced on the model with the highest cache-read rate, named on each line.** One figure
  per line keeps CI output readable; the highest rate states the most a change costs per later turn.
  Every model per line was rejected as noise; a fixed model id would rot when the price table changes.
- **Per-file dollars for every priced model are written to the file**, so the map sums to the recorded
  totals model by model. The figure is written only at a release, so its length costs no review churn.
- **Regenerate the baseline from the tag, not from `main`.** Writing it from `main` would move the
  committed release figure mid-cycle and hide the growth since the release.

### Do not implement

- No per-file gate, and no change to the `allow.json` entry format.
- No change to the `harness lint` caps or to what the static tier counts.
- No live replay tier change.

## Tasks

- [x] Add the per-file `files` map to `measure`, priced per model, and its scope line (AC: 1)
  - [x] Test the map's entries and its sums on a hand-countable tree and on the committed figure (AC: 1, 2)
- [x] Add `file_deltas` and print it from `static --check` before the total gate (AC: 3, 4, 5)
  - [x] Test the seeded 200-token growth, the allow entry, new and removed files, an unmoved tree, the priced model and an old baseline (AC: 3, 4, 5)
- [x] Regenerate `benchmarks/static.json` from the `v0.13.1` tag and confirm its totals are unchanged (AC: 2)
- [x] Document the per-file view in one bullet in `docs/benchmarks.md` and add the changelog entry (AC: 1)
- [x] Run the gate under both interpreters (AC: 6)

## Dev notes

### Bound decisions and requirements

- FR-55, Static context budget in CI: CI computes the static estimate on every pull request and fails
  on unexplained growth over 5%; this story keeps that gate and adds the per-file view.
  [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-55]
- AD-12, Measurement invariants: a modelled estimate is never read as a measurement, and dollars are
  list-price equivalents; the figure keeps its characters-over-four estimate and says so in `scopes`.
  [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-12]

### Files to touch

- `scripts/cost_bench.py` — the `files` map, `file_deltas`, the `--check` output.
- `benchmarks/static.json` — regenerated from the `v0.13.1` tag.
- `docs/benchmarks.md` — one bullet on the per-file view.
- `tests/test_cost_bench_files.py` (new) — tests.
- `changelog.d/509.added.md`.

### Testing

`tests/test_cost_bench_files.py` reuses the hand-countable tree from `tests/test_cost_bench.py`: one class
for the map and its sums, including the committed figure, and one for the deltas, driving `main` with
`--check` for the seeded growth, the allow entry and the old-baseline case.

### References

- [Source: scripts/cost_bench.py#measure] — the rows `total` sums and the scopes block.
- [Source: scripts/cost_bench.py#check] — the 5% gate and the allow entry.
- [Source: policy/prices.json] — the cache-read and cache-write rates the figure is priced on.
- [Source: docs/benchmarks.md#Static context figure] — the reader-facing description.

## Dev agent record

### Agent model used

Claude Opus 5.5 (claude-opus-5-5), as the builder in an unattended run.

### Completion notes

All six criteria implemented as designed. Default taken: none. The committed figure was regenerated by
running the new `measure` over `git archive v0.13.1`; every field except `schema_version`, `scopes` and
the new `files` map matched the committed file exactly, total 6751 estimated tokens.

### File list

- `scripts/cost_bench.py`
- `benchmarks/static.json`
- `docs/benchmarks.md`
- `tests/test_cost_bench_files.py`
- `changelog.d/509.added.md`
- `_bmad-output/implementation-artifacts/AH-S254.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`

## Review findings

None yet.

## Change log

- 2026-09-25: story, design, tasks and dev notes filled at implementation.
