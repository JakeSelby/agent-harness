---
bmad_id: "AH-S264"
type: "story"
title: "Say whether /land waits on checks that are not required"
lifecycle: "active"
provenance: "authored"
github_issue: 824
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/824"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-26"
---

# AH-S264 — Say whether /land waits on checks that are not required

<!-- bmad-sync:begin -->
- **GitHub issue:** [#824](https://github.com/JakeSelby/agent-harness/issues/824)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As an agent running `/land`, I want step 1 to say which checks it waits on, so that a status that is not required, such as a review bot's, neither blocks the merge nor passes unmentioned.

## Context and value

Step 1 required every required check green, then said a pending, failing or stale check stops the workflow. It did not say whether "check" there meant a required one. CodeRabbit's commit status is not required, yet it sits pending for several minutes after each review request, so one agent would wait on it and another would ignore it silently. The review itself is gated elsewhere, by the repository's own merge conditions, so the status adds nothing a required check does not.

## Acceptance criteria

1. **Given** a pull request whose required checks are green and whose not-required status is pending, **when** `/land` runs step 1, **then** it names that status and its state and proceeds.
2. **Given** a required check, or an issue-ownership or closing-link check, that is pending, failing or stale on the current head, **when** `/land` runs step 1, **then** it stops and names the check.
3. **Given** the command's body, **when** the command tests run, **then** it stays within the 40-line body cap.

## Design

### Approach

Rewrite step 1 of `primitives/workflows/land.md` in place, keeping its five lines so the body cap holds once the other in-flight edit to this file lands, and regenerate the projections with `bin/harness generate`.

### Decisions and alternatives

Decided by the owner on 2026-09-26: `/land` names each check that is not required, with its state, but gates only on required checks, with the issue-ownership or closing-link check counted among them where the repository runs one. CodeRabbit's commit status is the motivating case.

- Rejected: waiting on every check. A review bot's status is pending for minutes after each request, and the review is already a merge condition of its own.
- Rejected: ignoring a check that is not required without naming it. A failing optional check can still be news the reader acts on.

### Do not implement

Other steps of `land.md`, `primitives/workflows/build.md`, `AGENTS.md`, and any code that queries check status.

## Tasks

- [x] Rewrite step 1 to gate on required checks and name the rest (AC: 1, 2)
- [x] Regenerate the projections (AC: 1, 2)
- [x] Add a test pinning the new wording, keeping the body cap (AC: 3)
- [x] Add a changelog fragment

## Dev notes

### Bound decisions and requirements

- FR-42 (delivery-loop workflows); the text stays runtime-neutral so Codex projects it as a skill.

### Files to touch

- `primitives/workflows/land.md` (step 1)
- `claude/commands/land.md` (generated)
- `tests/test_commands.py`
- `changelog.d/824.changed.md`

### Testing

`tests/test_commands.py`: `test_land_names_a_check_that_is_not_required_but_never_waits_on_it` pins AC 1 and 2; the existing body-cap test covers AC 3.

### References

- [Source: primitives/workflows/land.md#1-verify-the-head-that-will-merge]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#fr-42-delivery-loop-workflows]

## Dev agent record

### Agent model used

Claude Opus 5.5.

### Completion notes

Step 1 now gates on required checks and the issue-ownership or closing-link check, and names every other check or status with its state without waiting on it. Step 1 kept its five lines; the closing clause was reworded to "a human resolves their own thread" to fit.

### File list

- `primitives/workflows/land.md`
- `claude/commands/land.md`
- `tests/test_commands.py`
- `changelog.d/824.changed.md`
- `_bmad-output/implementation-artifacts/AH-S264.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`

## Review findings

None yet.

## Change log

- 2026-09-26: filled from the owner's decision and implemented.
