---
bmad_id: "AH-T033"
type: "task"
title: "Keep plans committed by /build lint-clean in this repository"
lifecycle: "active"
provenance: "authored"
github_issue: 821
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/821"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-26"
---

# AH-T033 — Keep plans committed by /build lint-clean in this repository

<!-- bmad-sync:begin -->
- **GitHub issue:** [#821](https://github.com/JakeSelby/agent-harness/issues/821)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

Keep a plan written for this repository lint-clean from the moment it is posted, so the copy
`/build` commits into the pull request is the copy the reviewer approved. The lint rejects project
names it allows only under `docs/` and any email address; #782 and #786 both had to reword their
plans at build time, and the committed copies drifted from the approved ones.
[Source: https://github.com/JakeSelby/model-citizen/issues/821]

Decided by the owner on 2026-09-26, through the v0.14.0 finish-run brief (no numbered option was
offered): put the fix where plans are authored, as a general rule to lint the plan before posting
it wherever the repository names a lint, rather than hardcoding this repository's names into
shared content.

## Acceptance criteria

1. **Given** the `plan-authoring` skill, **when** an author reads it, **then** it says to run the
   repository's named lint on the plan before posting the review message and after each revision,
   and its self-check carries that item.
2. **Given** the skill's guidance, **when** a plan needs to name a withheld project or a trailer,
   **then** it says to name the check by what it does and to write "the Co-Authored-By trailer"
   without the address.
3. **Given** a plan copied alone into an empty directory, **when** `citizen lint <that directory>`
   runs, **then** a docs-only project name and an email address are findings with a non-zero
   exit, and a plan written around them prints `0 finding(s)` and exits zero.
4. **Given** the change, **when** the gate runs, **then** lint, `generate --check` and the suite
   are clean.

## Tasks

- [x] Add the lint-the-plan section and self-check item to the skill source (AC: 1, 2)
- [x] Regenerate projections with `citizen generate` (AC: 4)
- [x] Add `tests/test_plan_lint_clean.py` for the skill text and the documented command (AC: 1, 3)
- [x] Add the changelog fragment and regenerate sprint status (AC: 4)

## Dev notes

- **Where the rule lives.** `primitives/skills/plan-authoring/SKILL.md` gains a section, "A plan
  that gets committed passes the repository's lint as approved", placed before the self-check. It
  is general: any repository that names a lint in its agent instructions. The harness-specific part
  is only how to point the harness lint at one file, since `lint` takes a root directory and an
  untracked file under `.agent-harness/` is skipped by the tree walk.
- **Not changed.** `primitives/workflows/build.md` is owned by another issue in this run and is at
  its line cap; `/plan` already routes through the skill, so no workflow line is needed.
- **Files touched:** `primitives/skills/plan-authoring/SKILL.md`, `tests/test_plan_lint_clean.py`
  (new), `changelog.d/821.changed.md`, this story, `_bmad-output/implementation-artifacts/sprint-status.yaml`.
- **Testing:** `tests/test_plan_lint_clean.py` runs `bin/harness lint` in a subprocess under a
  temporary home with `HARNESS_LINT_TERMS` naming a docs-only project, and pins the skill text in
  the source and its projection.
- [Source: docs/bmad-governance.md#keep-the-corpus-current-in-every-change]

## Dev agent record

- **Model:** Claude Opus 5.5, as a builder subagent.
- **Completion notes:** content change plus a test of the command the skill documents. No
  `product.json` change: the skill's guidance is not a new advertised capability.
- **File list:** `primitives/skills/plan-authoring/SKILL.md`, `tests/test_plan_lint_clean.py`,
  `changelog.d/821.changed.md`, `_bmad-output/implementation-artifacts/AH-T033.md`,
  `_bmad-output/implementation-artifacts/sprint-status.yaml`.

## Change log

- 2026-09-26: filled the story from the stub for the delivery pull request.
