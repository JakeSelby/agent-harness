---
bmad_id: "AH-S197"
type: "story"
title: "One selection document and one resolver"
lifecycle: "active"
provenance: "authored"
github_issue: 554
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/554"
parent_bmad_id: "AH-E011"
parent_github_issue: 552
updated: "2026-09-25"
---

# AH-S197 — One selection document and one resolver

<!-- bmad-sync:begin -->
- **GitHub issue:** [#554](https://github.com/JakeSelby/agent-harness/issues/554)
- **Primary parent:** [AH-E011](https://github.com/JakeSelby/agent-harness/issues/552)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer using the harness, I want every installed unit selectable from one selection document
resolved by one function, so that a stance, rule, hook, skill, workflow or role can be chosen in one
place and every consumer acts on the same answer.

## Context and value

Epic #552 makes the harness the configurable layer under a skill library: every unit becomes a
dimension with variants, and a mode is a named set of selections [Source: #552]. This story is the foundation: #555,
#556 and #557 are blocked on it. Its principle from the epic: one selection document shape in five
places, one resolver, no second path. [Source: #552]

Today only stances are selectable. `policy/hooks/posture.py` resolves the stance ladder (defaults,
user config, the `HARNESS_PROJECT_CONFIG` file, then `HARNESS_STANCE_*`), and `bin/harness` keeps its
own `load_config` ladder beside it. There is no `posture.selection()`, no `HARNESS_SESSION_CONFIG` and
no `harness selection` command on `main`. `catalog.KINDS` exists, but as a map from kind to source
directory only.
[Source: https://github.com/JakeSelby/agent-harness/issues/554]

## Acceptance criteria

1. **Given** any installed unit, **when** `harness selection --json` runs, **then** it lists every unit
   of every kind with its value and source; `harness stances --json` is unchanged for existing keys.
2. **Given** a project file, **when** it sets `rules`, **then** the setting is honoured; **when** it
   sets `identity`, **then** it is refused with a message naming the key.
3. **Given** `HARNESS_SESSION_CONFIG`, **when** selections resolve, **then** it is read after the
   project file and before the environment sugar, and a worker run resolves the same selection as the
   session that launched it.
4. **Given** the unit tests, **when** they run, **then** they cover precedence for each of the five
   sources and the refusal of non-selection keys.
5. **Given** the docs, **when** the shape is documented, **then** `docs/preferences.md` states it once
   and other docs point at it.
6. **Given** the change, **when** the gate runs, **then** `bin/harness lint` is clean and the suite is
   green under both a current Python and the system Python 3.9.

## Design

### Approach

- **A unit is a dimension with variants.** Stance dimensions keep named variants; every other kind is a
  switch kind with exactly `on` and `off`, default `on`. Kinds: `stances`, `rules`, `hooks`, `skills`,
  `workflows`, `roles`.
- **One JSON shape in five places:** a mode file, the user's `config.json`, the project file
  `HARNESS_PROJECT_CONFIG` names, the session file `HARNESS_SESSION_CONFIG` names, and the output of
  `harness selection --json`. The issue's example carries `mode`, then one object per kind.
- **One resolver:** `posture.selection()` returns every unit with its value and source (`default`,
  `mode:<name>`, `user`, `project`, `session`), in that precedence. `HARNESS_MODE` and
  `HARNESS_STANCE_*` stay as session sugar that resolves through it.
- **One reader set:** sync, `lifecycle.invoke`, `workers.py`, `harness selection`, `harness stances`
  (kept as an alias) and `catalog` all read that function.
- **A kind is one entry in `catalog.KINDS`:** source directory, value type (`variant` or `switch`) and
  projection. Adding a kind adds no config-schema or resolver code.

### Decisions and alternatives

- **Selections carry selections only.** Identity, permissions, runtime `manage` flags,
  `primitive_roots` and telemetry stay top-level with their own validation.
- **`harness stances` is kept as an alias**, so its JSON output keeps its shape; AD-2 relies on this for
  the session hook.
- **The mode key may resolve to nothing until #557 lands** (issue: "that is fine").

### Do not implement

- Modes themselves (#557), hook ids (#555), and per-file rule links (#556).
- Any selection key outside the six kinds and `mode`.

## Tasks

- [x] Extend `catalog.KINDS` with value type and projection per kind (AC: 1)
- [x] Add `posture.selection()` with the five-source precedence and session sugar (AC: 1, 3)
- [x] Read `HARNESS_SESSION_CONFIG`; pass the selection to workers (AC: 3)
- [x] Refuse non-selection keys in project and session files, naming the key (AC: 2)
- [x] Add `harness selection [--json]`; keep `harness stances` as an alias (AC: 1)
- [x] Route sync, `lifecycle.invoke`, `workers.py` and `catalog` through the resolver (AC: 1)
- [x] Unit tests for each source and each refusal (AC: 4)
- [x] Document the shape once in `docs/preferences.md` (AC: 5)
- [x] Gate under both Pythons (AC: 6)

## Dev notes

### Bound decisions and requirements

- FR-15, Selection model with switches for every primitive: planned for v0.14.0 under #554, #555, #556.
- FR-2, One effective selection: user, project and session layers resolved into one effective policy.
- AD-2, One resolver, fixed invariants: `posture.py` is the only resolver; project configuration may
  select stances only; #294 and #554 carry the migration of `bin/harness` onto the resolver.
- AD-6, One effective policy for every consumer: no component parses selection files itself.
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-2]

### Recorded contradiction

AD-2's rule says "Project configuration may select stances only", and FR-2 says the same. This issue's
acceptance requires a project file setting `rules` to be honoured. The two cannot both hold once this
story lands; the spine or the issue needs amending. Neither was changed here.

Resolved 2026-09-25 by a default taken for the owner to confirm or override: the code follows the
issue, and dated amendments are appended under AD-2's rule and FR-2's consequence. The original
text of both stays as written.

### Files to touch

From the issue's design: `policy/hooks/posture.py`, `lib/harness_core/catalog.py`,
`lib/harness_core/lifecycle.py`, `lib/harness_core/workers.py`, `bin/harness`, `docs/preferences.md`,
and new tests under `tests/`.

### Testing

Precedence tests for each of the five sources and refusal tests for non-selection keys, beside the
existing `tests/test_posture.py`. [ASSUMPTION: placement beside `test_posture.py`; the issue names no
test file.]

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/554]
- [Source: https://github.com/JakeSelby/agent-harness/issues/552]
- [Source: policy/hooks/posture.py]
- [Source: lib/harness_core/catalog.py]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-15]

## Dev agent record

### Agent model used

Claude Opus 5.5, as an unattended builder.

### Completion notes

- Default taken: a project file's `rules` (and every other selection key) is honoured, as the issue
  requires, which contradicts AD-2 and FR-2. The code keeps the issue's behaviour, and a dated
  amendment is appended to AD-2 and to FR-2. The owner confirms or overrides this on the PR.
- `posture.selection()` resolves every unit of every kind over `default`, `mode:<name>`, `user`,
  `project`, `session`. `HARNESS_MODE` and `HARNESS_STANCE_*` are the session layer's last word.
  `resolve()`, `selected()` and `cost_table()` read the same ladder for stances only, so the hot
  path walks no unit directories.
- `catalog.KINDS` entries now carry `directory`, `pattern`, `value` and `projection`; `posture.py`
  loads that map by file, so a new kind is one entry there. A hook copied without its catalog
  knows `stances` only.
- The output of `harness selection --json` is the selection document plus a `sources` object in
  the same shape; `sources` is accepted and ignored on read, so the output reads back unchanged as
  a session file.
- The mode layer reads `modes/<name>.json` from the primitive roots when one exists and otherwise
  resolves to nothing; no mode ships, and validation of mode metadata is left to the modes story.
- `hooks` is a switch kind with no enumerated units until the hook-id story: a value a layer sets
  for a hook is resolved and reported with its source.
- `bin/harness load_config` takes its stances from the resolver (strict), so a project file that
  `HARNESS_PROJECT_CONFIG` names but that does not exist is now an error in the CLI, as it already
  was in the hooks. Sync and drift drop `HARNESS_SESSION_CONFIG` and `HARNESS_MODE` with the other
  session-scoped variables.
- `workers.resolution` records the launching session's selection, and a worker run's status file
  carries it. `lifecycle` reads through `posture.selected`, which now reads the resolver;
  `lifecycle.invoke` gains no switch check, which is the hook-id story's.
- `harness stances --json` keeps every key and adds `sources`. The session-start hook's
  effective-stance notice now also fires for `HARNESS_SESSION_CONFIG` and `HARNESS_MODE`.
- A project file now selects every kind, as this issue requires; AD-2 and FR-2 carry a dated
  amendment saying so.

### File list

- `policy/hooks/posture.py`
- `policy/hooks/harness-session.py`
- `lib/harness_core/catalog.py`
- `lib/harness_core/workers.py`
- `bin/harness`
- `docs/preferences.md`
- `docs/primitive-authoring.md`
- `product.json`, `README.md`
- `changelog.d/554.added.md`
- `tests/test_selection.py` (new); `tests/test_integrations.py` and
  `tests/test_primitive_catalog.py` (refusal message)
- `_bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md`
  and `_bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md` (dated amendments)

## Review findings

CodeRabbit, round 1 on PR #836:

- `workers.run()` still injects a skill a session selection switches `off`. Declined here: sync and
  workers acting on an `off` rule, skill, workflow or role is #556; this story resolves and reports
  switch kinds only.
- A project or session file with a non-object kind value, such as `{"rules": []}`, was silently
  ignored. Fixed: `selection()` refuses it when strict and drops only that kind otherwise, with a
  test.
- Review body, outside the diff: the `precedence` list in `harness stances --json` omits the mode
  layer. Not changed: the acceptance keeps that output unchanged for existing keys, and the mode
  layer shows in `sources`.

CodeRabbit, round 2:

- An explicit `null` kind (`{"rules": null}`) still passed strict resolution. Fixed with the
  non-object refusal: a present kind that is not an object is refused.
- A switch value of `null`, an empty string or a non-string was skipped before the on/off check,
  so strict resolution reported `on`. Fixed: every supplied switch value is checked first, with a
  test. Stance values keep their existing handling.

## Change log

- 2026-09-23: written from the issue and pull-request record.
- 2026-09-23: corrected after sample review: explicit sources for claims taken from the epic or a sibling
  issue, and the fit of each binding and current-state claim rechecked against the record.
- 2026-09-25: implemented; tasks, completion notes and file list filled.
- 2026-09-25: default taken on the AD-2 and FR-2 contradiction; amendments appended to both.
- 2026-09-25: CodeRabbit round 1 findings recorded; the non-object kind value is refused.
- 2026-09-25: CodeRabbit round 2 findings recorded and fixed.
