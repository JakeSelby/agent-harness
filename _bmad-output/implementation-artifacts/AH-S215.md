---
bmad_id: "AH-S215"
type: "story"
title: "Tighten the two detectors the corpus records under the precision floor"
lifecycle: "active"
provenance: "authored"
github_issue: 602
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/602"
parent_bmad_id: "AH-E013"
parent_github_issue: 632
updated: "2026-09-23"
---

# AH-S215 — Tighten the two detectors the corpus records under the precision floor

<!-- bmad-sync:begin -->
- **GitHub issue:** [#602](https://github.com/JakeSelby/agent-harness/issues/602)
- **Primary parent:** [AH-E013](https://github.com/JakeSelby/agent-harness/issues/632)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As the harness maintainer, I want the two repository-specific detectors that fall under the precision
floor tightened, so that a rule's hit rate is not inflated by false positives a stranger's session can
trigger, and the corpus job holds the floor without exceptions.

## Context and value

PR #601 [Source: PR #601] scored all seventeen detectors against a labelled corpus in CI and kept the 0.9 precision floor
rather than lowering it. Two detectors measured precision 0.83 at recall 1.00, and the PR recorded both
in `known_below_floor` with their measured pair, filing the fix separately as this issue:

- `secrets/git-add-secret-file`: any basename containing `id_rsa` is a hit, so
  `git add docs/id_rsa-rotation.md` fires.
- `autonomy/denied-by-grade`: the hook's signature `(grade-bash hook,` matches anywhere in a Bash
  result, so a grep over a test file that prints it fires.

Both behaviours are confirmed in `policy/hooks/rule-detectors.py` on `main`: the secret-file check tests
`"id_rsa" in base`, and the grade check tests `GRADE_SIGNATURE in` the event text.
[Source: https://github.com/JakeSelby/agent-harness/issues/602]

## Acceptance criteria

1. **Given** the committed corpus, **when** it is scored, **then** both detectors clear the 0.9 floor
   with the false-positive cases still labelled negative.
2. **Given** the fix, **when** the corpus job runs, **then** both `known_below_floor` records are removed
   and the job stays green.

## Design

### Approach

Narrow each detector's match so the labelled near-misses stop firing while the positives still do.
The job itself enforces the second criterion: PR #601 made it fail on a stale below-floor record, so a
record left behind after a detector clears the floor turns it red.

### Decisions and alternatives

- **Keep the floor; fix the detector** (issue "Why"): the floor is not lowered, and each detector stays
  listed until it is fixed and its record removed.
- The specific narrowing for each detector is not recorded in the issue or pull request.

### Do not implement

- Relabelling the false-positive cases as positive to raise precision (criterion 1 forbids it).
- Changes to the floor.

## Tasks

- [ ] Tighten `secrets/git-add-secret-file` in `policy/hooks/rule-detectors.py` (AC: 1)
- [ ] Tighten `autonomy/denied-by-grade` in `policy/hooks/rule-detectors.py` (AC: 1)
- [ ] Remove both `known_below_floor` records from `tests/fixtures/detector-corpus/labels.yaml` (AC: 2)
- [ ] Run `scripts/detector_corpus.py --floor 0.9` and the detector tests (AC: 1, 2)

## Dev notes

### Bound decisions and requirements

- FR-22, Detector validity: CI scores detectors against a labelled corpus and fails when a detector falls
  below 0.9 precision. The PRD records these two detectors as under the floor (#602, v0.14.0).
- FR-20, Per-rule hit rate: a false positive inflates the rule's hit rate.
  [ASSUMPTION: the issue argues from the stranger's-session false positive, not from FR-20.]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-22]

### Files to touch

`policy/hooks/rule-detectors.py`; `tests/fixtures/detector-corpus/labels.yaml`. The corpus sessions are
generated by `tests/fixtures/detector-corpus/build_sessions.py`; any new session case goes through that
builder rather than a hand edit.

### Testing

`tests/test_detector_corpus.py` and the `corpus` CI job running `scripts/detector_corpus.py`.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/602]
- [Source: https://github.com/JakeSelby/agent-harness/pull/601]
- [Source: policy/hooks/rule-detectors.py]
- [Source: tests/fixtures/detector-corpus/labels.yaml]
- [Source: scripts/detector_corpus.py]

## Dev agent record

### Agent model used

Not recorded in the issue or pull request.

### Completion notes

Not started. The two records sit in `known_below_floor` on `main`.

### File list

None yet.

## Review findings

None recorded.

## Change log

- 2026-09-23: written from the issue and pull-request record.
- 2026-09-23: corrected after sample review: explicit sources for claims taken from the epic or a sibling
  issue, and the fit of each binding and current-state claim rechecked against the record.
