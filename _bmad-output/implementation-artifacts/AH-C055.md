---
bmad_id: "AH-C055"
type: "chore"
title: "Align product.json on-the-way entries with the PRD and drop em dashes from README copy"
lifecycle: "active"
provenance: "authored"
github_issue: 648
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/648"
parent_bmad_id: "AH-E006"
parent_github_issue: 212
updated: "2026-09-25"
---

# AH-C055 — Align product.json on-the-way entries with the PRD and drop em dashes from README copy

<!-- bmad-sync:begin -->
- **GitHub issue:** [#648](https://github.com/JakeSelby/agent-harness/issues/648)
- **Primary parent:** [AH-E006](https://github.com/JakeSelby/agent-harness/issues/212)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

Make the published "on the way" list claim only what the PRD backs, and make the README copy follow the
no-em-dash rule. `product.json` is the one source for the landing page, the README and GitHub About.
When the issue was filed its "on the way" list advertised two things the PRD does not back: "Budget
nudges mid-run", which the spike rejected, and "Six runtimes at equal depth", where the PRD commits
only to equal depth with no count. The issue is open.
[Source: https://github.com/JakeSelby/agent-harness/issues/648]
[Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-54]

The issue has no milestone.

## Acceptance criteria

1. **Given** `product.json`'s `on_the_way`, **when** it is read, **then** every entry names an open issue
   on a milestone.
2. **Given** every entry, **when** it is compared with the PRD's roadmap, **then** the PRD backs it; no
   entry names a runtime count.
3. **Given** the README and other published copy, **when** it is checked, **then** it holds no em dash.

## Tasks

- [x] Every `on_the_way` entry names an open issue on a milestone (AC: 1): "Measured against bare"
      names #799 (v0.15.0) and "The superpowers mode" names #558 (v0.16.0)
- [x] Remove "Budget nudges mid-run": already gone at `origin/main`. #651, merged after this issue was
      filed, says in its body that it removed the `on_the_way` entries for closed issues #321 and #322,
      and this was one of them (AC: 2) [Source: https://github.com/JakeSelby/agent-harness/pull/651]
- [x] Remove "Grok and Cursor adapters" and "Close the loop" (AC: 1, 2): the 2026-09-24 roadmap
      amendment drops additional runtimes from every milestone and moves #135 to the backlog, so
      neither can name an open issue on a milestone. The owner chose the replacement entries.
- [x] Remove the README's em dashes (AC: 3): the one left was generated from the compatibility
      note in `lib/harness_core/catalog.py`; fixed there and regenerated with `harness generate`
- [x] Mirror the README's "On the way" list to `product.json` and test it (AC: 1, 3)

## Dev notes

### State at `origin/main`

- `on_the_way` holds two entries. "Grok and Cursor adapters" still reads "Six runtimes at equal depth
  is the target" and names a catalog entry (`grok`) rather than an issue. "Close the loop" names #135,
  which is open on v0.15.0.
- As of commit b9a87a5, `README.md` holds seven em dashes on five lines, and `product.json` holds
  none.
- The PRD's roadmap puts "additional runtimes, each at equal depth" under v0.16.0, with no count.

### Bound decisions and requirements

- FR-54, Landing copy as data: all landing, README and About copy comes from `product.json`, and
  published copy uses no em dashes. The PRD records the latter as partial because the README still has
  some and no test checks copy for them.
- AD-19, Distribution channels carry no second authority: landing, README and About copy come from
  `product.json` only.
- NFR-13, Claims discipline: every public claim is labelled implemented, validated, proposed,
  historical or unknown.
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-agent-harness-2026-09-23/ARCHITECTURE-SPINE.md#AD-19]

### Files to touch

`product.json`, `README.md`, and any README lines rendered from `on_the_way`. The repository rule that
`on_the_way` is capped at five entries, each naming the issue, planned client or document it stands
for, is in `AGENTS.md`; this issue's criterion 1 is stricter, requiring an open issue on a milestone.

### Testing

Not recorded in the issue. `ProductCopyTests` in `tests/test_release.py` and
`scripts/sync_about.py` are the existing copy checks
([Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-53]). An em-dash assertion does not exist yet,
per the PRD.

### References

- [Source: https://github.com/JakeSelby/agent-harness/issues/648]
- [Source: https://github.com/JakeSelby/agent-harness/pull/651]
- [Source: product.json]
- [Source: _bmad-output/planning-artifacts/prds/prd-agent-harness-2026-09-23/prd.md#FR-54]

## Dev agent record

### Agent model used

Claude Opus 5.5, builder role.

### Completion notes

- The story's tasks predated the PRD's 2026-09-24 roadmap amendment, which supersedes §9.2. Under it
  neither original entry could name an open issue on a milestone, so the work parked for a product call.
  The owner approved replacing both with two drafted entries, "Measured against bare" (#799) and "The
  superpowers mode" (#558).
- The README's "On the way" list had drifted from `product.json`: it carried a third entry, "The
  instrument, standalone", with no issue. AD-19 makes `product.json` the only source, so the README
  now lists exactly its two entries, and `tests/test_readme_copy.py` keeps them equal.
- The README's last em dash came from the generated compatibility note; `docs/compatibility.md`
  shares that note and loses its generated em dashes too. Its hand-written ones are outside this
  issue.
- Tests: `tests/test_readme_copy.py` checks the README and the generated block for em dashes and the
  README's list against `product.json`.

### File list

- `product.json`
- `README.md`
- `docs/compatibility.md` (generated)
- `lib/harness_core/catalog.py`
- `tests/test_readme_copy.py`
- `changelog.d/648.changed.md`
- `_bmad-output/implementation-artifacts/AH-C055.md`

## Review findings

- CodeRabbit, round 1 on PR #845 (nitpick in the review body, no thread): the README list test only
  collected bullets in the `- **title:** line` form, so a malformed bullet would slip past the
  equality check. Fixed: the test now parses every bullet in the section and fails on one that does
  not match.

## Change log

- 2026-09-23: written from the issue and pull-request record.
- 2026-09-25: implemented; on_the_way entries replaced per the owner's call.
- 2026-09-25: review finding recorded and fixed.
