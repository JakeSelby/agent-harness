---
bmad_id: "AH-S263"
type: "story"
title: "Name the review-complete signal in /build step 6 and revisit its ten-minute wait"
lifecycle: "active"
provenance: "authored"
github_issue: 822
github_issue_url: "https://github.com/JakeSelby/agent-harness/issues/822"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-26"
---

# AH-S263 — Name the review-complete signal in /build step 6 and revisit its ten-minute wait

<!-- bmad-sync:begin -->
- **GitHub issue:** [#822](https://github.com/JakeSelby/agent-harness/issues/822)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As an agent running `/build` step 6, I want to know what a finished bot review looks like and how long to wait for it, so that I never report a round as clean before the bot has reviewed it.

## Context and value

On #786, the first run of `/build` step 6 misreported CodeRabbit's second round as clean. When the bot chat-replies in a resolved thread, GitHub wraps the reply in an empty COMMENTED review, and a "new review since T" check fired on it. The reliable signal is the bot's commit status on the head commit: `pending: Review in progress`, then `success: Review completed`. The `Review skipped` status posted on every push is not a pass either. A requested re-review took about 8.5 minutes, and the repository's own instructions put a pass at seven to eleven minutes, so the ten-minute wait was too tight.

## Acceptance criteria

1. **Given** `/build` step 6, **when** an agent waits for the bot, **then** the step names the completed commit status on the head commit (`success: Review completed` for CodeRabbit) as the only sign a pass arrived.
2. **Given** a bot thread reply, **when** GitHub records an empty review, **then** the step says it is not a pass; **given** a `Review skipped` status or no review starting, **then** the step requests a pass with the bot's own review command before waiting.
3. **Given** the wait, **when** an agent reads step 6, **then** it waits up to fifteen minutes, not ten.
4. **Given** the 40-line command body cap, **when** the change lands, **then** `build.md` still fits it and no other step changed.

## Design

### Approach

Rewrite step 6 of `primitives/workflows/build.md` in the same seven lines: shorten the bot-detection clause, add one sentence naming the completed head-commit status as the pass signal and the two false signals, and raise the wait to fifteen minutes. Regenerate `claude/commands/build.md` with `bin/harness generate`.

### Decisions and alternatives

- Decided by the owner on 2026-09-26: build the issue as filed, naming the commit-status signal and allowing fifteen minutes, which matches the repository's own seven-to-eleven-minute figure plus margin.
- Name the signal generically ("the bot's status on the head commit completes") with CodeRabbit's text as the example, since step 6 also serves Greptile and other bots. Rejected: a CodeRabbit-only rule, which would leave other bots with no signal.
- Stay inside the cap by tightening step 6's own wording rather than reflowing other steps, which later builders in the same release depend on.

### Do not implement

- No change to `land.md`, `AGENTS.md` or any other `/build` step.
- No polling script or hook; the step stays prose.

## Tasks

- [x] Rewrite step 6 with the signal, the false signals and the fifteen-minute wait (AC: 1, 2, 3)
- [x] Keep the body within 40 lines and regenerate the projection (AC: 4)
- [x] Add `tests/test_build_review_signal.py` (AC: 1, 2, 3)
- [x] Add `changelog.d/822.changed.md`

## Dev notes

### Bound decisions and requirements

- The command body cap of 40 lines, pinned by `tests/test_commands.py`.
- Generated projections are never hand-edited; `bin/harness generate --check` enforces it.

### Files to touch

- `primitives/workflows/build.md` (step 6)
- `claude/commands/build.md` (generated)
- `tests/test_build_review_signal.py` (new)
- `changelog.d/822.changed.md` (new)

### Testing

- `tests/test_build_review_signal.py` asserts the signal, both false signals and the fifteen-minute wait in the source and its projection (AC 1 to 3).
- `tests/test_commands.py` keeps the 40-line cap and step 6's existing phrases (AC 4).

### References

- [Source: AGENTS.md#CodeRabbit review and merging]
- [Source: primitives/workflows/build.md#Build]
- [Source: tests/test_commands.py]

## Dev agent record

### Agent model used

Claude Opus 5.5

### Completion notes

Step 6 rewritten in its original seven lines; the body stays at 40 lines. The bot-detection clause was shortened to make room. No deviation from the design. Review on #922 found that a skipped first review would only be waited out; step 6 now requests a pass with the bot's review command when the status reads skipped or no review starts, as `AGENTS.md` requires for drafts, `chore(release)` titles and Dependabot, and the test pins the request before the wait. A second review pass on #922 found the request named only CodeRabbit's command and the thread IDs had lost their source; the request now uses the bot's own command with CodeRabbit's as the example, and the threads come from GraphQL `reviewThreads` again.

### File list

- `primitives/workflows/build.md` (step 6)
- `claude/commands/build.md` (generated)
- `tests/test_build_review_signal.py` (new)
- `changelog.d/822.changed.md` (new)
- `_bmad-output/implementation-artifacts/AH-S263.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (regenerated)

## Review findings

- #922, CodeRabbit (major): a `Review skipped` first pass was waited out and never requested. Fixed: step 6 requests one before the wait.
- #922, CodeRabbit (minor): the request named only `@coderabbitai review`. Fixed: it uses the bot's own command, CodeRabbit's as the example.
- #922, CodeRabbit (minor): the rewrite dropped GraphQL `reviewThreads` as the source of thread IDs. Fixed: restored before `resolveReviewThread`.

## Change log

- 2026-09-26: story filled with the owner's decision and implemented.
- 2026-09-26: acceptance criterion 2 and the dev record amended for the skipped-review request from review on #922.
- 2026-09-26: request made bot-generic and `reviewThreads` restored, from the second review pass on #922.
