---
scope: Agent Harness platform
purpose: Keep shared primitives, hooks, adapters, measurement and planning consistent across independently built units
altitude: product
updated: 2026-09-25T19:40
---

- (event) Update run opened for issue #619 in a new dated folder; the 2026-09-19 spine is superseded, not rewritten; AD-1 to AD-5 keep their ids
- (decision) (carried) AD-1 binds one shared primitive authority with thin native adapters to prevent runtime-specific policy forks
- (decision) (carried) AD-2 binds resolved stances as explicit policy choices while truth, authorization, secret protection and native restrictions remain invariants
- (decision) (carried) AD-3 binds reversible ownership records and structural configuration edits to preserve user-controlled settings
- (decision) (carried) AD-4 binds qualification claims to versioned native evidence for an exact source candidate
- (decision) (carried) AD-5 binds public planning to repository-local BMad artifacts and GitHub-backed work item traceability
- (change) AD-2 amended in place: one resolver, precedence defaults, user, project, session; project configuration selects stances only; core switches need an acknowledgement (PRD FR-2, FR-15)
- (change) AD-4 amended in place: evidence is invalidated per target by source paths; a round is frozen on a release branch and runs a model-free smoke tier first (PRD FR-51, FR-52)
- (change) AD-5 amended in place: GitHub owns delivery state and the summary; the story file owns the design; the sync tool owns only frontmatter and the managed block (PRD FR-64, #620)
- (decision) AD-6 one effective policy read by every consumer: the audit that promoted #117 found a hook, the output style and telemetry each reading raw selections
- (constraint) AD-7 enforcement relies only on pre-tool deny, pre-tool input rewrite, isolated workers and the local ledger; no hook can replace a built-in tool result or trigger compaction, and Codex has no ask (runtime research)
- (decision) AD-8 constrained roles run only as isolated offline role workers; every spawn path is enumerated and guarded or declared unguarded (Workflow tool gap #576 named)
- (decision) AD-9 checks bind subagents and decisions flow up; only the top session asks the user (#284)
- (decision) AD-10 integration contract: frameworks emit intents, descriptors are data, policy binds at harness hooks, confinement by session-level signal not prompt text (#291, #349)
- (decision) AD-11 usage ledger is the system of record; backends are replicas rebuilt by replay; OTLP only; export never blocks a hook
- (decision) AD-12 measurement invariants: ratios not dollars across days, arms differ only by environment, unknown never zero, errored never failed, session never summed with its subagents
- (decision) AD-13 detector logic lives in the vendored ruleprobe package; the harness binds rules to detectors and never forks detector logic
- (decision) AD-14 cost enforcement is routing and the frontier refusal only; budgets are information; roles name classes and adapters bind models
- (decision) AD-15 decision providers are subordinate: stages, act tighten-only, fail open to the deterministic answer, thresholds keyed to pinned model and pack version, empty egress allowlist by default, no distillation
- (decision) AD-16 parallel writers coordinate through shared recorded state: declared path claims including new paths, overlaps logged, no peer messaging (coordination research)
- (decision) AD-17 behaviour read from runtime-client internals lives behind version-pinned adapters with doctor checks and stays outside the v1 stable surface (Remote Control, Workflow tool)
- (decision) AD-18 shared-machine concurrency: per-session state, atomic writes with mode preserved, ID reservation surveys every reachable map, the live checkout carries only merged content
- (decision) AD-19 the plugin channel carries no hooks; enforcement needs harness sync
- (assumption) Codex hook-event support is probed per client version before any design relies on prompt-submit or subagent events (disputed between recorded evidence and current docs)
- (event) Rubric review round 1: 1 critical, 6 high, 11 medium, 7 low; the domain model placed the resolver in lib/harness_core and reversed the real load-by-path direction
- (change) Paradigm rewritten to the code: policy kernel in policy/hooks (posture.py is the resolver), dispatcher in lifecycle.py, core library and CLI load the kernel by path, adapters' hook.py is a shim
- (change) AD-2 records two precedence ladders and the #294/#554 migration plus the init-default constraint on modes; AD-7 lists ask, stop block and notices; AD-11 adds additive schema rules; AD-15 settles act on Codex; AD-18 scopes writes; AD-19 to AD-21 added for distribution, continuity and dependencies
- (event) Review round 2: 20 of 25 round-1 findings fixed; 8 high, 13 medium, 11 low new, mostly present-tense claims about planned work
- (change) Every AD carries a status tag; AD-11 names the usage worker's upsert rewrite; AD-15 defines act at the stop point and extends the no-ask case to Claude Code auto and bypass; AD-4 states the shared-file carve-out; AD-8 names the plugin channel as an unguarded path; hook ids and the decision-point authority defined
- (event) Spine finalized after two review rounds; review files kept under reviews/
- (event) Update run opened 2026-09-24 for issue #783 (headless): record the measurable-harness roadmap's invariants; AD ids stay stable
- (decision) AD-22 added [PLANNED: v0.14.0, #554]: every module declares a manifest (claims, surface, instruments, slot, dependencies, conflicts); no instrument means unmeasured; the profile fingerprint derives from the switched-on set
- (decision) AD-23 added [PLANNED: v0.14.0]: observation runs in every arm and adds nothing to model context, proved by a bare-arm test with observation on and off; every ledger row carries the fingerprint and per-module attribution of context tokens and hook decisions; adherence events are observation
- (change) AD-12 amended in place rather than a new AD, since it already holds the measurement invariants: every figure is labelled measured, soft estimate or unmeasured, and the soft series is never pooled (planned for v0.15.0)
- (change) AD-12 Binds extended to published evidence, and Prevents extended to a modelled estimate read as a measurement
- (change) AD-16 re-dated to the backlog, since the roadmap parks #542 and #543; the capability map adds AD-22 and AD-23 to selection, ledgers and benchmarks
- (event) Spine lint clean after the update
- (event) Review round 3 (the first for #783): fail, 11 findings (2 high, 5 medium, 4 low); recorded with resolutions in reviews/review-round3.md
- (change) AD-23 fixed: an observation-only hook path that appends, prints nothing, adds no context and returns no decision; only it is installed in the bare arm; context-emitting handlers (neutralize-tool-output, harness-session notices, stop-gate) are never observation; the test compares the first model request byte for byte after normalising timestamps and session ids, per arm; title and tag (#482) updated
- (change) AD-22 fixed: manifests live with each primitive in the catalog (AD-1) and posture.py resolves them (AD-2, AD-6); a conflict never switches off an invariant without acknowledgement; a shared slot is a resolution error unless one module cedes it; the fingerprint is a digest of modules with versions, stance variants, model- or hook-facing configuration and the harness version; tag split across v0.14.0, v0.15.0 and v0.16.0
- (change) Attribution: every new ledger row carries the fingerprint, additive under AD-11, older rows unattributed; per-module token attribution carries the soft-estimate label; decision-log rows carry hook-decision attribution only
- (change) AD-12 defines measured, soft estimate and unmeasured, orthogonal to FR-11's availability words; the status-words convention lists both; the ablation manifest is distinguished from AD-22's module manifest; the measured-rules map row adds AD-22
- (event) Spine lint clean after the round-3 fixes; all 11 findings fixed
- (correction) The previous entry overstated: round 3's fixes left S1 and S5 only partly fixed
- (event) Review round 3, second pass: fail, 7 findings (2 high, 5 medium); recorded with resolutions in reviews/review-round3.md
- (change) AD-22: a conflict never switches off a fixed invariant; it switches off a core hook only with AD-2's acknowledgement
- (change) AD-23: observation has its own registered entry point beside hook.py, outside the dispatcher, and fails open and silent to a local error log; Hook ids convention amended and an observation Failure posture row added
- (change) AD-23: the zero-footprint test drives a scripted session through every installed hook event and compares every model request, per arm
- (change) AD-22: each manifest sits beside its module's source, primitives in the catalog and hooks in policy/hooks/; the one resolver reads both
- (change) AD-12: measured covers direct row counts and run estimates, each naming its fingerprint; unattributed qualifies figures from unfingerprinted rows and supports only whole-profile statements
- (change) AD-12: labels tagged v0.14.0 with attribution; the soft-estimate report and series stay v0.15.0
- (event) Review round 3, 2026-09-24: all seven second-pass findings resolved, no new high or critical finding; review passes
- (change) AD-23 registration: the observation entry point reads its event list from the event table AD-7 names as the one declaration, the one medium the final round noted
- (change) AD-8, 2026-09-25: the Workflow tool's named gap amended; its launch is guarded (#576), so what remains there is band routing of the script's agent() calls, not confinement
- (event) Update run opened 2026-09-25 for issue #876 (headless): record the Model Citizen rename's invariant from the approved rename plan; AD ids stay stable
- (decision) AD-24 added [PLANNED: v0.14.0, #875]: the brand is Model Citizen and the slug model-citizen; citizen is the command and harness stays a supported alias with no deprecation, bin/harness remaining the real file; the plugin ID becomes model-citizen@model-citizen and doctor recognizes the old one; on-disk names, AH- IDs, the issue-map slug, planning directory names, telemetry's service.name and dated records keep agent-harness
- (change) Capability map: distribution gains AD-24; the spine's own title and frontmatter name stay as dated text for this update, left to the copy story (#879)
- (event) Spine lint clean after the AD-24 update; the reviewer lenses were not dispatched in this run, which had no subagent tool, so the pull request review stands in for them
